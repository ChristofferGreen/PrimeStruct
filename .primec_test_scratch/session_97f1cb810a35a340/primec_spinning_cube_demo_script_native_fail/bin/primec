#!/usr/bin/env bash
set -euo pipefail
for arg in "$@"; do
  if [[ "$arg" == "--emit=native" ]]; then
    echo "synthetic native compile failure" >&2
    exit 1
  fi
done
echo "unexpected invocation" >&2
exit 99
