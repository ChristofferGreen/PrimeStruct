#!/usr/bin/env bash
set -euo pipefail
out=""
while [[ $# -gt 0 ]]; do
  if [[ "$1" == "-o" ]]; then
    out="$2"
    shift 2
    continue
  fi
  shift
done
cat > "$out" <<'EOS'
#!/usr/bin/env bash
exit 0
EOS
chmod +x "$out"
