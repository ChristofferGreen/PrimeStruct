# PrimeStruct Spec ↔ Code TODO

Legend:
  ○ Not started
  ◐ Started
  ✓ Finished

Finished items are periodically moved to `docs/todo_finished.md`.

**Types & Semantics**
Roadmap note: completed map/vector migration, compatibility-cleanup, and graph perf slices now live in `docs/todo_finished.md`. Keep this live file focused on unfinished ownership/runtime substrate work, SoA bring-up, and the remaining graph/boundary items.

**Architecture / Type-resolution graph**
**Group 11 - Near-term graph queue**
Blocked by Group 13 rollout constraints until the remaining collection-helper/runtime predecessor items are finished and the return-kind pilot path is stable enough to resume broader graph work.
- ◐ Implement the next non-template inference-island migrations now that the graph-backed cutover contract is documented. Progress: the remaining non-template cutover is now split into direct-call, receiver-call, and collection bridge inference seams instead of one umbrella migration bullet.
  - ○ Migrate the next direct-call/callee non-template inference island onto the graph path.
    - ○ Select the next direct-call inference island to move onto the graph path.
    - ○ Land the migration plus matching graph conformance coverage for that island.
  - ○ Migrate the next receiver/method-call non-template inference island onto the graph path.
    - ○ Select the next receiver/method-call inference island to move onto the graph path.
    - ○ Land the migration plus matching graph conformance coverage for that island.
  - ○ Migrate the next collection helper/access fallback non-template inference island onto the graph path.
    - ○ Select the next collection helper/access fallback inference island to move onto the graph path.
    - ○ Land the migration plus matching graph conformance coverage for that island.
- ◐ Implement the graph/CT-eval interaction contract now that the boundary is documented. Progress: the remaining work is now split into shared dependency-state consumption, explicit adapter seams, and conformance coverage instead of one broad CT-eval handoff item.
  - ○ Route graph-backed consumers onto shared dependency-state consumption where CT-eval and graph validation overlap.
    - ○ Identify the next shared dependency state consumed by both CT-eval and the graph.
    - ○ Move that dependency state onto the shared graph-backed surface.
  - ○ Add one explicit adapter for the remaining CT-eval-only paths that still cannot consume shared graph dependency state directly.
    - ○ Select the CT-eval-only dependency state that needs an adapter.
    - ○ Implement the adapter and wire it into the graph/CT-eval boundary.
  - ○ Add conformance coverage for the graph/CT-eval handoff boundary.
    - ○ Add a positive conformance fixture that exercises the CT-eval adapter seam.
    - ○ Add a negative diagnostic fixture that proves the adapter boundary rejects mismatches.
- ◐ Implement the graph-backed explicit/implicit template-inference migrations now that the cutover contract is documented. Progress: the remaining template cutover is now split into explicit template-argument, implicit inference, and revalidation/monomorph follow-up slices instead of one umbrella migration bullet.
  - ○ Migrate the next explicit template-argument inference surface onto the graph path.
    - ○ Select the explicit template-argument inference surface to migrate.
    - ○ Land the migration plus matching graph conformance coverage for that surface.
  - ○ Migrate the next implicit template-inference surface onto the graph path.
    - ○ Select the implicit template-inference surface to migrate.
    - ○ Land the migration plus matching graph conformance coverage for that surface.
  - ○ Migrate the matching revalidation/monomorph follow-up for those template-inference surfaces onto the graph path.
    - ○ Identify the revalidation/monomorph follow-up needed for the migrated surfaces.
    - ○ Land the follow-up and conformance coverage.
- ◐ Implement the next omitted-envelope and local-`auto` graph expansions now that the widening contract is documented. Progress: the widening work is now split into omitted-envelope facts, new initializer families, and control-flow join widening slices instead of one umbrella expansion bullet.
  - ○ Materialize omitted-envelope graph facts for the next widening slice.
    - ○ Select the next omitted-envelope family to model in the graph (proposed: omitted struct initializer envelopes).
    - ○ Add graph facts and coverage for that omitted-envelope family.
  - ○ Expand local-`auto` graph support across the next initializer-family surface.
    - ○ Select the next initializer-family surface to widen in the graph (proposed: `if`-branch local `auto` binds).
    - ○ Land the widening plus matching coverage for that initializer-family surface.
  - ○ Expand local-`auto` graph support across the next control-flow join surface.
    - ○ Select the next control-flow join surface to widen in the graph (proposed: `if` join return inference).
    - ○ Land the widening plus matching coverage for that join surface.

**Group 12 - Semantics/lowering boundary**
Progress note: the first semantic-product publication, dump surface, and temporary lowerer adapter are finished. The remaining live boundary work is to make `SemanticProgram` the only lowering truth, retire the remaining AST-derived fallbacks, and finish the graph-backed fact handoff needed by later Group 11 migrations.
- ◐ Turn missing semantic-product facts into explicit conformance failures instead of silent AST fallbacks. Progress: production `prepareIrModule(...)` / `IrLowerer::lower(...)` entrypoints now fail immediately when the semantic product is absent; callable summary, entry return-fact, `on_error`, direct-call routing, method-call routing, binding classification for parameters/local bindings, local-`auto` lowering facts, entry-parameter/count setup, and `on_error` bound-arg completeness now fail explicitly when semantic-product publication is incomplete instead of silently falling back to AST transforms.
  - ◐ Add compile-pipeline/backend conformance checks that assert the required semantic-product fact families are present before lowering starts. Progress: source-guard and backend-registry tests now assert the production lowering entrypoints reject missing semantic products, missing callable summaries, missing direct-call and method-call semantic ids or routing facts, missing bridge-path routing facts for collection-helper bridge calls, missing binding facts for parameters/local bindings, missing local-`auto` facts, missing entry return facts, missing entry parameter facts, missing `on_error` facts, mismatched `on_error` bound-arg publication, missing callable result metadata, empty return binding types, and incomplete query/`try(...)` result facts before lowering begins, but broader per-fact-family completeness coverage is still pending.
- ◐ Move resolved call routing to semantic-product-only ownership. Progress: semantic-product call targets are published, direct-call canonical path routing now requires semantic-product facts instead of falling back to scope/import resolution, receiver/method-call routing now requires semantic-product method targets instead of re-inferring receiver/helper ownership in lowering, and helper-vs-canonical bridge-path ownership now requires published bridge-path choices for collection-helper bridge calls instead of silently reusing duplicated direct-call targets.
  - ✓ Move receiver/method-call target routing to semantic-product-only ownership.
  - ✓ Move helper-vs-canonical bridge-path routing to semantic-product-only ownership, including same-path helper-shadow choices.
- ◐ Make graph-backed local-`auto`, query, `try(...)`, and `on_error` facts required lowering inputs. Progress: these fact families are already published on `SemanticProgram`; lowering now requires semantic-product entry parameter facts for count/argv setup, published `on_error` `boundArgTexts` for handler setup, published local-`auto` facts for implicit/`auto` local type setup, published query facts for semantic-id-backed generic call-result metadata, and published `try(...)` facts for semantic-id-backed `try(...)` lowering/inference instead of treating those facts as optional adapter hints.
  - ✓ Make graph-backed local-`auto` facts required for lowerer local type setup.
  - ✓ Make graph-backed query and `try(...)` facts required for lowerer call/result setup.
- ◐ Reshape `SemanticProgram` into module-scoped resolved artifacts instead of one flat whole-program fact bag. Progress: the current semantic product is deterministic, but it is still one flat container rather than a module-ordered resolved boundary that mirrors imported source structure.
  - ○ Define the module-scoped semantic-product container shape and deterministic module ordering contract.
  - ○ Move existing fact families under module-scoped resolved artifacts without changing their lowering-visible meaning.
  - ○ Update lowerer/testing consumers to read module-scoped semantic artifacts instead of flat whole-program vectors.
- ◐ Add CI budget gates for graph invalidation and graph-build metrics using the `type-graph` output. Progress: the graph dump already reports invalidation counts and budget fields, but there is no sustained CI gate that treats those metrics as a contract.
  - ○ Select the representative graph corpora and baseline thresholds for prepare/build time and invalidation fan-out.
  - ○ Wire the graph-budget checks into CI/CTest so regressions fail by default.
  - ○ Document the workflow for intentionally revising graph budgets and metric baselines.
- ◐ Retire AST-derived lowering fallbacks until the AST is provenance-only at the lowering boundary. Progress: production lowering entrypoints now require a `SemanticProgram` for semantic meaning, but several helper paths still use AST/import fallback logic after the boundary is crossed.
  - ○ Remove the remaining transform-based binding-kind and helper-alias fallback paths once semantic-product fact coverage is complete.
    - ◐ Thread collection-helper/access receiver metadata through the experimental-map lowering boundary. Progress: direct constructors, typed map bindings, pointer/reference wrappers, and variadic map args-pack elements now preserve specialized experimental-map struct metadata deeply enough for the lowerer to route more canonical helper calls through the experimental runtime surface; explicit builtin `at` / `at_unsafe` tail-dispatch alias cleanup now shares the same borrowed-receiver rewrite path, direct builtin experimental-map access now lowers through the shared native lookup loader instead of bouncing through synthesized helper calls, materialized map call receivers now keep canonical builtin shapes while preserving wrapped map receiver metadata through local rewrites, semantic-product bridge classification now recognizes the borrowed experimental-map helper family (`mapCountRef` / `mapContainsRef` / `mapTryAtRef` / `mapAtRef` / `mapAtUnsafeRef`) instead of only `mapInsertRef`, lowerer-side direct helper alias normalization now maps experimental `map*` / `map*Ref` helper calls back onto the canonical builtin helper names so borrowed helper calls stay on the native path, rooted stdlib helper spellings like `/std/collections/mapCount` / `/std/collections/mapContains` / `/std/collections/mapTryAt` / `/std/collections/mapAt` / `/std/collections/mapAtUnsafe` / `/std/collections/mapInsert` now normalize through that same lowerer alias path too, lowerer-side canonical borrowed map wrapper paths (`count_ref` / `contains_ref` / `tryAt_ref` / `at_ref` / `at_unsafe_ref` / `insert_ref`) now normalize through the same builtin helper classifiers, lowerer-side direct-call builtin classification now treats `insert` the same way as the other canonical map helpers so bare/rooted insert spellings stop falling back to helper-definition resolution, lowerer-side inline-native builtin classification now treats direct bare/rooted canonical `insert` spellings the same way too instead of stopping at `count` / `contains` / `tryAt`, lowerer-side unresolved method-call inline fallback now treats bare `insert` the same way as bare `contains` / `tryAt` instead of rejecting it before the later builtin/native paths can handle it, method-call builtin receiver fallback now treats bare `insert` the same way as bare `contains` / `tryAt` instead of surfacing it as a non-builtin receiver miss, receiver-target fallback/probe logic and canonical map method-path preference now treat `insert` as part of that same canonical helper family instead of leaving it on a narrower method-only branch, lowerer receiver-target definition lookup now keeps canonical `/std/collections/map/insert` on the builtin path instead of aliasing it back onto `/map/insert`, shared lowerer/semantics compatibility-path builders now also keep canonical `/std/collections/map/insert` off the reverse `/map/insert` alias path instead of reintroducing that detour through generic fallback candidate generation, struct-return/pointer-like/statement-helper compatibility paths now do the same instead of reintroducing `/map/insert` through their own reverse alias fallbacks, direct semantics-side `map/...` helper compatibility recognition now includes `insert`, bare map helper target preference now keeps `insert` on the canonical `/std/collections/map/insert` path instead of preferring `/map/insert` when the compatibility alias exists, and the lowerer’s shared removed-map-compatibility family now includes `insert` too instead of relying on local one-off guards to keep those reverse alias paths and explicit compatibility aliases in sync, and missing map `insert` receiver definitions now fall through the same builtin-miss path as `contains` / `tryAt` instead of surfacing a hard unknown-method error, semantics-side builtin insert rewrites now also collapse explicit `/map/insert`, `/map/insert_ref`, `mapInsert`, `mapInsertRef`, and rooted stdlib/experimental insert helper spellings onto `/std/collections/map/insert_builtin` before lowering, lowerer-side synthetic insert-helper rewrites now collapse remaining canonical/experimental map insert spellings onto `/std/collections/map/insert_builtin` while clearing stale semantic call ids so rooted helper rewrites resolve on their rewritten builtin path, statement-form map insert helpers now take that same canonical builtin rewrite for both direct and wrapped map receivers instead of relying on helper-specific direct-call resolution, inline lowering now recovers wrapped map insert receiver locals from the original ordered receiver argument so grow-on-insert can write back through reference/pointer wrappers instead of falling through to the synthetic runtime trap, inline canonical insert growth now also falls back to the callee’s own map local when there is no named caller receiver local to update, which keeps temporary/value-passed canonical insert receivers on the grow path instead of requiring a synthetic runtime trap, and the dead synthetic runtime trap path is now retired from inline canonical insert lowering and from the runtime-helper surface so missing write-back targets fail lowering instead of surfacing as a synthetic runtime error, bridge-path snapshot/lowerer validation now classify the full canonical and experimental insert helper family (`insert`, `insert_ref`, `mapInsert`, `mapInsertRef`) consistently instead of special-casing only part of that surface, rooted `/map/map` constructor rewrites now clear stale semantic call ids across expression/inline/packed/binding rewrite paths so semantic-product routing keeps the rewritten builtin constructor target instead of the pre-rewrite stdlib helper path, canonical-to-experimental helper retargeting in tail dispatch now clears stale semantic call ids so rewritten experimental helper paths survive semantic-product call routing, method-call inline-native experimental-map helper classification now includes `insert` too instead of leaving it off the canonical helper family, canonical map helper alias rewrites in lowerer inference/expression/tail-dispatch paths now include `insert` too instead of leaving those helper-normalization branches at `count` / `contains` / `tryAt` / `at` / `at_unsafe`, template-monomorph canonical map-helper unknown-target and experimental-helper mapping now include the borrowed canonical `*_ref` family too, template-monomorph wrapper-helper retargeting now includes `/std/collections/mapInsert`, and initializer inference now recognizes canonical stdlib map helper calls across the borrowed `*_ref` family too instead of stopping at value-surface `insert`, borrowed canonical `count_ref` now participates in the same collection-dispatch setup and builtin count validator path as canonical `count`, materialized temporary/call-returned map receivers now keep canonical `insert` helper shapes too instead of leaving rooted `insert_ref` / `mapInsert` / `mapInsertRef` spellings on a helper-only detour, semantics-side builtin method-target wrapper validation now recognizes the borrowed canonical map helper family, semantics-side builtin helper availability and pre-dispatch unknown-target checks now keep borrowed canonical map helpers on the same builtin path instead of stopping at the value-only surface, and builtin map helper return-kind inference now covers borrowed canonical `count_ref`, `contains_ref`, `at_ref`, and `at_unsafe_ref` too. Remaining shared semantics map helper-family tables, compatibility helpers, call-path aliases, body-argument target selection, effect-free helper preference, and diagnostics now also treat `contains`, `tryAt`, and `insert` as part of the canonical map helper surface instead of preserving the older `count` / `at` / `at_unsafe` subset, borrowed canonical `tryAt_ref` now participates in result-type and `try` inference on the same builtin path as canonical `tryAt`, and explicit borrowed canonical `at_ref` / `at_unsafe_ref` helper calls now bypass the scalar-pointer builtin validator the same way the value-surface canonical map access helpers already do. Borrowed canonical method-call builtin validation now also treats `contains_ref`, `tryAt_ref`, `at_ref`, and `at_unsafe_ref` the same way as the value-surface canonical map helpers instead of leaving those method-call validation and unknown-target branches on the older value-only surface, and late fallback return-kind inference plus the map/soa `contains` builtin validator now carry the borrowed canonical helper surface too instead of only the value-surface canonical helpers. Semantics-side pre-dispatch logical canonicalization now treats direct borrowed experimental-map helper calls as builtin canonical map helper paths, including `insert_ref`, indexed args-pack map receiver method inference now treats `insert` the same way as `count` / `contains` / `tryAt` for canonical method-target selection, builtin wrapper-validation heuristics now recognize the borrowed `Reference/...` and `..._ref` map wrapper definitions consistently, both late unknown-target fallback and statement body-argument target selection now keep the borrowed canonical map method family on the same preferred map-method target path as the value helpers instead of dropping `*_ref` methods onto ad hoc fallback routing, the remaining shared removed-map compatibility helpers in builtin-path parsing, method-target alias handling, and template-monomorph compatibility-path selection now treat the borrowed canonical `*_ref` family as first-class map helpers too instead of preserving a value-only compatibility surface, the remaining struct-return, effect-free, pointer-like, statement-helper, and pre-dispatch compatibility-path builders now follow that same borrowed canonical map helper family instead of using older value-only alias gates, and the remaining method-resolution fallback, map helper target-selection, call-building, initializer helper recognition, and snapshot helper-family tables now keep borrowed canonical `*_ref` helpers on the same canonical map surface as the value helpers instead of leaving those entrypoints on value-only helper lists. Collection-dispatch setup and late builtin access return-kind inference now also keep canonical borrowed `at_ref` / `at_unsafe_ref` on the same stdlib/direct/resolved map-access path as value-surface `at` / `at_unsafe` instead of leaving those setup branches on the older value-only access subset, collection-access target resolution now keeps canonical borrowed `at_ref` / `at_unsafe_ref` on that same direct/namespaced/resolved map-access routing path instead of dropping those calls back onto the older value-only access resolver, collection-access fallback validation now keeps canonical borrowed `at_ref` / `at_unsafe_ref` on the same builtin access validation and stdlib diagnostic path as value-surface `at` / `at_unsafe` instead of leaving those checks on the older value-only access subset, and the late wrapper-validation and fallback inference path now keeps canonical borrowed `at_ref` / `at_unsafe_ref` on the same bare-access validation gate and late map-access builtin/fallback path as value-surface `at` / `at_unsafe` instead of leaving those last late-access branches on the older value-only surface. The remaining work is the final runtime parity follow-up.
  - ○ Remove the final AST-derived semantic fallback slices so the AST remains lowering-visible only for provenance/debug/source-map ownership.
