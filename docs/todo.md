# PrimeStruct Spec ↔ Code TODO

Legend:
  ○ Not started
  ◐ Started

Finished items are periodically moved to `docs/todo_finished.md`.

**Types & Semantics**
Roadmap note: completed map/vector migration, compatibility-cleanup, graph queue, and semantics/lowering boundary slices now live in `docs/todo_finished.md`.
Sizing note: each leaf `○` item should fit in one code-affecting commit plus focused conformance updates for that slice. If a leaf needs multiple behavior changes, split it first.

**Group 14 - SoA bring-up and end-state cleanup**
- ◐ Retire remaining compiler-owned builtin `soa_vector` semantics/lowering/backend scaffolding as the stdlib `.prime` substrate becomes authoritative.
  - ◐ Migrate Family S2 canonical `to_aos(_ref)` bridge-shim dependence onto shared stdlib helper/conversion paths. Progress: canonical `/std/collections/soa_vector/to_aos` now lowers through canonical `/std/collections/soa_vector/count|get` loop substrate instead of directly forwarding to `/std/collections/experimental_soa_vector_conversions/soaVectorToAos`, and canonical `/std/collections/soa_vector/to_aos_ref` now lowers through canonical `/std/collections/soa_vector/count_ref|get_ref` loop substrate instead of routing through `to_aos(dereference(values))`, while remaining compatible with borrowed `SoaVector<T>` receivers; template-monomorph receiver resolution no longer hard-routes canonical `to_aos_ref` through direct experimental conversion helper mapping.
    - ◐ Remove remaining compiler-owned inline-parameter SoA bridge shim dependence for direct experimental conversion helper callees (`/std/collections/experimental_soa_vector_conversions/soaVectorToAos(_Ref)`). Progress: direct experimental conversion-helper bridge matching is now removed from inline-parameter lowering; only canonical `/std/collections/soa_vector/count|get` bridge paths remain, with focused IR and compile-run coverage locking the reject contract for direct experimental helper calls on builtin `soa_vector`.
      - ◐ Repoint canonical `/std/collections/soa_vector/to_aos(_ref)` internals onto the canonical count/get substrate (or equivalent lowering substrate) so canonical conversion no longer depends on direct experimental helper callee bridge matching. Progress: `/std/collections/soa_vector/to_aos` now loops via canonical `/std/collections/soa_vector/count|get`, and `/std/collections/soa_vector/to_aos_ref` now loops via canonical `/std/collections/soa_vector/count_ref|get_ref`.
      - ◐ Remove direct experimental helper callee bridge matching from inline-parameter lowering and lock the behavior with focused IR + compile-run coverage. Progress: inline-parameter bridge matching no longer treats `/std/collections/experimental_soa_vector_conversions/soaVectorToAos(_Ref)` as bridge-eligible for builtin `soa_vector`, and focused IR + compile-run coverage now lock the mismatch diagnostic.
  - ◐ Migrate Family S3 pending borrowed field-view diagnostics onto finalized stdlib helper contracts. Progress: direct pending borrowed/field-view diagnostics now canonicalize onto `unknown method: /std/collections/soa_vector/ref(_ref)` and `unknown method: /std/collections/soa_vector/field_view/<field>` across validator, return-inference, and monomorph fallback paths, with source-lock + semantic/IR validation coverage updated to assert the canonical contract; the dead optional pending-diagnostic helper path, direct pending unavailable-method wrapper, and unused borrowed-helper visibility probe argument are removed from semantics helpers, and callsites now route through single-argument `soaUnavailableMethodDiagnostic(...)` so these paths only use the canonical unknown-method contract; `splitSoaFieldViewHelperPath(...)` now only recognizes the canonical `/std/collections/soa_vector/field_view/` prefix (legacy `/soa_vector/field_view/` matching removed), with source-lock coverage pinning that contract; pending helper canonicalization now normalizes templated resolved helper spellings before legacy `/soa_vector/ref(_ref)` remapping, semantic + source-lock coverage now includes canonical `ref_ref` unknown-method rejects, SoA builtin dispatch now canonicalizes legacy `/soa_vector/get|get_ref|ref|ref_ref` resolved spellings before helper-shape matching in expr-map validation, statement-return SoA ref/ref_ref escape classification now canonicalizes legacy resolved helper spellings before helper-family checks, statement-binding SoA standalone-ref classifiers now canonicalize resolved helper paths before ref/ref_ref helper-family detection, infer-collection dispatch SoA method return-kind inference now canonicalizes legacy resolved helper spellings before helper-family dispatch, build-initializer SoA access helper classification now canonicalizes legacy resolved helper spellings before helper-family matching, and infer-collection return-inference SoA helper gating now canonicalizes legacy resolved helper spellings before old-surface helper-family checks, and resolved-call argument SoA standalone ref/ref_ref escape classifiers now canonicalize legacy resolved helper spellings before helper-family checks, and expr-mutation borrow SoA receiver ref/ref_ref helper-path detection now canonicalizes legacy resolved helper spellings before helper-family checks, and template-monomorph implicit-template SoA pending ref/ref_ref helper gating now canonicalizes legacy resolved helper spellings before helper-family checks, and canonical ref/ref_ref helper-family gating there now routes through shared `isCanonicalSoaRefLikeHelperPath(...)` checks instead of direct canonical-path disjunctions, and template-monomorph fallback stdlib implicit-template path gating now canonicalizes legacy resolved SoA ref/ref_ref helper spellings before helper-family checks, and template-monomorph expression-rewrite SoA helper-family gating now canonicalizes templated/legacy resolved ref/ref_ref helper spellings before helper-family checks in both call and method dispatch paths, and ir-lowerer collection-mutation SoA ref write-target same-path visibility probes now canonicalize templated/legacy resolved helper spellings before builtin fallback gating, and SoA ref/ref_ref legacy-path canonicalization is now shared through `canonicalizeLegacySoaRefHelperPath(...)` in semantics helpers and reused by monomorph implicit-template pending-helper gating, and template-monomorph fallback stdlib implicit-template path gating now also reuses that shared helper instead of a local canonicalization lambda, and template-monomorph expression-rewrite SoA helper-family gating now also reuses that shared helper instead of a local canonicalization lambda, and statement-return SoA ref/ref_ref escape classification now also reuses that shared helper instead of a local canonicalization lambda, and statement-binding SoA standalone-ref classifiers now also reuse that shared helper instead of a local canonicalization lambda, and infer-collection return-inference SoA helper gating now reuses that shared helper for ref/ref_ref canonicalization while retaining explicit legacy get/get_ref remapping, and canonical ref/ref_ref helper-family old-surface gating there now routes through shared `isLegacyOrCanonicalSoaHelperPath(...)` checks instead of direct canonical-path comparisons, and ir-lowerer collection-mutation SoA same-path ref write-target visibility probes now also reuse that shared helper instead of a local canonicalization lambda, and infer-collection dispatch SoA method return-kind inference now also reuses that shared helper for ref/ref_ref canonicalization while retaining explicit legacy get/get_ref remapping, and canonical `ref`/`ref_ref` helper-family detection there now routes through shared `isCanonicalSoaRefLikeHelperPath(...)` checks instead of direct canonical-path comparisons, and expr-map SoA builtin dispatch now also reuses that shared helper for ref/ref_ref canonicalization while retaining explicit legacy get/get_ref remapping, and canonical ref/ref_ref helper-family gating there now routes through shared `isCanonicalSoaRefLikeHelperPath(...)` checks instead of direct canonical-path comparisons, and build-initializer SoA access helper classification now also reuses that shared helper for ref/ref_ref canonicalization while retaining explicit legacy get/get_ref remapping, and resolved-call argument SoA standalone ref/ref_ref escape classifiers now also reuse that shared helper instead of a local canonicalization function, and expr-mutation borrow SoA receiver ref/ref_ref helper-path detection now also reuses that shared helper instead of a local canonicalization lambda, and ir-lowerer non-method SoA ref write-target builtin-path matching now canonicalizes candidate callee paths through that shared helper before builtin ref-family checks, and method-target SoA builtin-helper classification now canonicalizes legacy resolved ref/ref_ref spellings through that shared helper before builtin import/local-definition gating, and canonical ref/ref_ref helper-family detection there now routes through shared `isCanonicalSoaRefLikeHelperPath(...)` checks instead of direct canonical-path comparisons, and template-monomorph expression-rewrite synthetic same-path SoA template-carry gating now removes duplicated inline helper-family ref/ref_ref path lists and reuses the shared helper-family predicate for both call and method paths, and that helper-path predicate now routes canonical ref/ref_ref matching through shared `isCanonicalSoaRefLikeHelperPath(...)` checks after canonicalization, and it now factors legacy/canonical non-ref helper-path matching through `isSyntheticSamePathSoaCarryNonRefHelperPath(...)` to remove duplicated helper-path disjunctions while preserving canonicalized ref/ref_ref checks, and template-monomorph fallback stdlib implicit-template path gating now reuses shared `isLegacyOrCanonicalSoaHelperPath(...)` semantics-helper matching for legacy/canonical `get_ref` helper paths instead of a local helper-path predicate, canonical `ref`/`ref_ref` `pathCanonical` gating there now routes through shared `isCanonicalSoaRefLikeHelperPath(...)` helper checks instead of a local predicate, and pending unknown-method helper canonicalization now also reuses that shared `isCanonicalSoaRefLikeHelperPath(...)` helper for canonical ref-family detection, and template-monomorph implicit-template canonical builtin ref/ref_ref call detection now routes resolved helper-path matching through shared `isLegacyOrCanonicalSoaHelperPath(...)` checks instead of direct canonical-path comparisons, and statement-return/statement-binding standalone SoA ref helper-family gating now routes canonical helper-path detection through shared `isCanonicalSoaRefLikeHelperPath(...)` checks instead of duplicated canonical-path prefix comparisons, and infer-collection/method-target `to_aos(_ref)` helper-family gating now canonicalizes legacy resolved helper spellings (`/to_aos`, `/to_aos_ref`, `/soa_vector/to_aos_ref`) through shared `canonicalizeLegacySoaToAosHelperPath(...)` + `isCanonicalSoaToAosHelperPath(...)` checks instead of duplicated direct resolved-path comparisons, and expr-map `to_aos(_ref)` late-builtin resolved-path gating now canonicalizes those same legacy helper spellings through the shared `canonicalizeLegacySoaToAosHelperPath(...)` + `isCanonicalSoaToAosHelperPath(...)` checks instead of duplicated direct resolved-path comparisons, and expr late-map/soa builtin pre-dispatch gating now canonicalizes resolved `to_aos(_ref)` helper spellings through the shared helper while requiring canonical `/std/collections/soa_vector/` namespace spellings before forcing late builtin validation so same-path `/to_aos(_ref)` helper contracts remain unchanged, and method-target builtin SoA helper import/local-definition gating now canonicalizes resolved `to_aos(_ref)` helper spellings through shared `canonicalizeLegacySoaToAosHelperPath(...)` + `isCanonicalSoaToAosHelperPath(...)` checks instead of duplicated direct canonical-path comparisons, and template-monomorph expression-rewrite canonical stdlib collection helper whitelisting now routes `to_aos(_ref)` canonical-path checks through shared `isCanonicalSoaToAosHelperPath(...)` matching instead of duplicated direct canonical-path disjunctions, and template-monomorph method-target `to_aos(_ref)` fallback preference now canonicalizes same-path helper spellings through shared `canonicalizeLegacySoaToAosHelperPath(...)` plus `isCanonicalSoaToAosHelperPath(...)` guards instead of hardcoded canonical-path literals, and template-monomorph expression-rewrite root-builtin `to_aos(_ref)` shadow-receiver fallback now canonicalizes same-path helper spellings through shared `canonicalizeLegacySoaToAosHelperPath(...)` + `isCanonicalSoaToAosHelperPath(...)` guards before root-builtin helper visibility probes, and expr/expr-map borrowed canonical `to_aos_ref` gating now routes through shared `isLegacyOrCanonicalSoaHelperPath(..., "to_aos_ref")` checks instead of direct canonical-path equality checks, and infer-collection/expr-method-target `to_aos(_ref)` vector-target inference now routes through per-helper `isLegacyOrCanonicalSoaHelperPath(...)` checks for `to_aos` and `to_aos_ref` instead of broad canonical-family predicate checks, and expr/expr-map canonical `to_aos` helper-family gating now routes through shared `isLegacyOrCanonicalSoaHelperPath(..., "to_aos")` checks instead of broad `isCanonicalSoaToAosHelperPath(...)` family predicates, and method-target builtin SoA helper import/local-definition `to_aos(_ref)` matching now routes through explicit shared `isLegacyOrCanonicalSoaHelperPath(...)` checks for both `to_aos` and `to_aos_ref` instead of broad canonical-family predicates, and template-monomorph method-target same-path `to_aos(_ref)` fallback family gating now routes through explicit shared `isLegacyOrCanonicalSoaHelperPath(...)` checks for `to_aos` and `to_aos_ref` instead of broad `isCanonicalSoaToAosHelperPath(...)` family predicates, and template-monomorph expression-rewrite canonical stdlib helper whitelisting plus root-builtin same-path `to_aos(_ref)` fallback family gating now route through explicit shared `isLegacyOrCanonicalSoaHelperPath(...)` checks for `to_aos` and `to_aos_ref` instead of broad `isCanonicalSoaToAosHelperPath(...)` family predicates, and the now-unused shared broad `isCanonicalSoaToAosHelperPath(...)` semantics-helper declaration/definition surface is removed, and build-initializer SoA helper same-path target synthesis now routes through shared `preferredSamePathSoaHelperTarget(...)` and treats both `to_aos` and `to_aos_ref` as root same-path helpers (`/to_aos(_ref)`) instead of synthesizing legacy `/soa_vector/to_aos_ref`, and `canonicalizeLegacySoaToAosHelperPath(...)` now only remaps root same-path aliases (`/to_aos`, `/to_aos_ref`) and no longer remaps `/soa_vector/to_aos_ref`, and duplicated legacy `/soa_vector/get(_ref)` remap ternaries in build-initializer classification, expr-map builtin dispatch, infer-collection dispatch, and infer-collection return inference now route through shared `canonicalizeLegacySoaGetHelperPath(...)` helper canonicalization, and method-target builtin SoA collection helper get/get_ref matching now canonicalizes resolved helper spellings through that shared helper while reusing the canonicalized path for imported/local helper visibility gating, and template-monomorph fallback stdlib implicit-template path gating now canonicalizes helper paths through shared `canonicalizeLegacySoaGetHelperPath(...)` before legacy/canonical `get_ref` and canonical `ref/ref_ref` helper-family checks, and template-monomorph expression-rewrite same-path `get/get_ref` fallback now canonicalizes the same-path helper through shared `canonicalizeLegacySoaGetHelperPath(...)` before same-path/canonical helper-family definition probes, and template-monomorph expression-rewrite synthetic same-path SoA non-ref helper carry-path detection now uses shared `isLegacyOrCanonicalSoaHelperPath(...)` helper-family matching plus `canonicalizeLegacySoaGetHelperPath(...)` for `get/get_ref` checks instead of local prefix parsing, and template-monomorph expression-rewrite same-path `ref/ref_ref` fallback now uses shared `"/soa_vector/" + helperName` target synthesis with receiver-family plus same-path definition guards, and template-monomorph expression-rewrite same-path `count/push/reserve` fallback now uses shared helper-family matching with a single synthesized `"/soa_vector/" + helperName` target before same-path visibility probes, and template-monomorph expression-rewrite same-path `get/get_ref` plus `ref/ref_ref` fallback synthesis now also uses that shared `"/soa_vector/" + helperName` target shape instead of helper-specific ternary path construction, and this fallback block now computes `receiverFamily` once and reuses it across same-path `count/push/reserve`, `get/get_ref`, `ref/ref_ref`, and downstream canonical-preference checks instead of repeated `inferCollectionReceiverFamily(...)` calls, and template-monomorph expression-rewrite same-path `to_aos/to_aos_ref` fallback synthesis now uses shared `"/" + helperName` target construction instead of helper-specific ternary same-path targets, and template-monomorph method-target same-path `to_aos/to_aos_ref` fallback synthesis now also uses shared `"/" + std::string(helperName)` target construction instead of helper-specific ternary same-path targets, and duplicated `normalizedTypeName == "soa_vector"` method-target dispatch branches for `to_aos` and `to_aos_ref` are now merged into one helper-family branch in `resolveMethodCallTemplateTarget(...)`, and template-monomorph method-target `preferredSoaToAosMethodTarget(...)` now drops dead canonical helper-family guard booleans before returning the canonical fallback path, and template-monomorph method-target `preferredSoaMethodTarget(...)` now drops the dead `hasDefinitionFamilyPath(canonical)` branch while preserving same-path preference plus canonical fallback behavior, and template-monomorph expression-rewrite canonical-helper and same-path-root-builtin `to_aos(_ref)` gates now inline shared `isLegacyOrCanonicalSoaHelperPath(...)` disjunctions directly instead of temporary `matchesSoaToAosHelperPath` booleans, and template-monomorph expression-rewrite `receiverFamily == "soa_vector"` preferred-canonical helper fallback now merges duplicate resolvesVectorFamilyPath/import-visibility branches into one guard while preserving import-gated behavior for non-vector-family paths, and template-monomorph implicit-template canonical SoA ref/ref_ref call detection now inlines `isLegacyOrCanonicalSoaHelperPath(...)` checks for `resolvedSoaCanonical` directly into canonical-call booleans instead of temporary resolved-match booleans, and template-monomorph expression-rewrite same-path `to_aos(_ref)` root-builtin fallback now drops redundant `canonicalizeLegacySoaToAosHelperPath(...)` helper-family guards after helper-name normalization and only checks root-helper visibility plus shadow-receiver shape, and template-monomorph expression-rewrite same-path `count/push/reserve` fallback now drops redundant `isLegacyOrCanonicalSoaHelperPath(...)` checks after helper-name gating while keeping receiver-family plus same-path definition guards, and template-monomorph expression-rewrite same-path `get/get_ref` fallback now drops redundant `isLegacyOrCanonicalSoaHelperPath(canonicalGetHelper, helperName)` checks after helper-name gating while keeping receiver-family plus same-path definition guards, and template-monomorph expression-rewrite same-path `ref/ref_ref` fallback now drops redundant `isCanonicalSoaRefLikeHelperPath(canonicalRefHelper)` checks after helper-name gating while keeping receiver-family plus same-path definition guards, and template-monomorph method-target `preferredSoaToAosMethodTarget(...)` now drops redundant `canonicalizeLegacySoaToAosHelperPath(samePath)` canonicalization and builds the canonical `/std/collections/soa_vector/` path directly after helper-name gating, and template-monomorph method-target `preferredSoaMethodTarget(...)` now drops redundant canonical temporary storage and returns canonical `/std/collections/soa_vector/` helper paths directly after same-path definition checks, and method-target helper-family lookup `hasDefinitionFamilyPath(...)` now drops a redundant per-definition `defPath == path` equality branch that was already covered by the direct `ctx.sourceDefs.count(...)` probe before prefix-family scans, and that helper now caches `path` into one `pathString` for direct lookup plus prefix synthesis instead of repeating `std::string(path)` materialization in each probe, and method-target same-path SoA fallback helpers now cache `helperName` into `helperNameString` before composing same-path/canonical targets so they avoid repeated helper-name string materialization, and those same-path SoA fallback helpers now share `soaCanonicalMethodPath(...)` for canonical `/std/collections/soa_vector/` target assembly instead of duplicating that prefix composition in each lambda, and duplicated same-path SoA method-target fallback control flow is now centralized in shared `preferredSamePathSoaMethodTarget(...)` with per-call same-path prefixes (`/` vs `/soa_vector/`) while preserving canonical fallback behavior, and the now-trivial wrapper lambdas for `to_aos(_ref)` plus `push/reserve` method-target fallback dispatch are removed so those SoA method callsites invoke `preferredSamePathSoaMethodTarget(...)` directly, and that shared helper now caches `samePathPrefix` into one `samePathPrefixString` before constructing `samePath` so fallback path synthesis avoids repeated temporary prefix materialization, and `soaCanonicalMethodPath(...)` is now explicitly captureless (`[]`) because it no longer uses outer state, and method-target `soa_vector` same-path fallback dispatch now consolidates `to_aos(_ref)` plus `push/reserve` prefix selection into one `samePathPrefix` branch before `preferredSamePathSoaMethodTarget(...)`.
    - S3 refactor micro-slices `[S3-01]`, `[S3-02a]`, and `[S3-02b]` are archived in `docs/todo_finished.md` (April 11, 2026).

**Group 15 - Semantic memory footprint and multithread compile substrate**
Order rule: execute leaves top-to-bottom. Each leaf below is scoped to one code-affecting commit.

P0 - Reproducible measurement and attribution

P1 - Immediate peak-RSS reductions in existing pipeline

P2 - Traversal and allocation churn reductions
- ○ [P2-12] Extend scoped scratch/canonical-fragment reuse to one additional high-churn method/call target resolution path and lock it with source-lock coverage.
