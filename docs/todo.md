# PrimeStruct TODO Log

## Operating Rules (Human + AI)

1. This file contains only open work (`[ ]` or `[~]`).
2. Use one task block per item with a stable ID: `TODO-XXXX`.
3. Keep newest items at the top of `## Open Tasks`.
4. Every task must include:
   - clear scope
   - acceptance criteria
   - owner (`human` or `ai`)
5. When a task is completed:
   - mark it `[x]`
   - add `finished_at` and short evidence note
   - move the full block to `docs/todo_finished.md`
   - remove it from this file (do not keep completed tasks here)
6. `docs/todo_finished.md` is append-only history. Do not rewrite old entries except to fix factual mistakes.
7. Each implementation task SHOULD include `phase` and `depends_on` to keep execution order explicit.
8. Prefer small, testable tasks over broad epics; split before starting if acceptance cannot be verified in one PR.
9. Keep the `Execution Queue` and coverage snapshots current when adding/removing tasks.
10. Keep an explicit `Ready Now` shortlist synced with dependencies; only items with no unmet TODO dependencies belong there.
11. When splitting broad tasks, update parent task scope to avoid duplicated acceptance criteria across child tasks.
12. Keep `Priority Lanes` aligned with queue order so critical-path tasks remain visible.
13. For phase-level tracking tasks, pair planning trackers with explicit acceptance-gate tasks before marking a phase complete.
14. Every active leaf must include a stop rule and deliver at least one of:
   - user-visible behavior change
   - measurable perf/memory improvement
   - deletion of a real compatibility subsystem
15. Avoid standalone micro-cleanups (alias renames, trivial bool rewrites, local dedup) unless bundled into one value outcome above.
16. If a leaf misses its value target after 2 attempts, archive it as low-value and replace it with a different hotspot.
17. Keep the active queue short: no more than 8 live leaves at once.

Status legend:
- `[ ]` queued
- `[~]` in progress
- `[x]` completed (must be moved to `docs/todo_finished.md`)

Task template:

```md
- [ ] TODO-<id>: Short title
  - owner: ai|human
  - created_at: YYYY-MM-DD
  - phase: Group/Phase name (optional but recommended)
  - depends_on: TODO-XXXX, TODO-YYYY (optional but recommended)
  - scope: ...
  - acceptance:
    - ...
    - ...
  - stop_rule: ...
  - notes: optional
```

## Open Tasks

### Ready Now (No Unmet TODO Dependencies)

1. TODO-0408
2. TODO-0401
3. TODO-0402
4. TODO-0405

### Immediate Next 10 (After Ready Now)

1. TODO-0409
2. TODO-0406
3. TODO-0403

### Priority Lanes (Current)

- P1 Collection stdlib ownership cutover (`vector`, `map`, `soa_vector`): TODO-0408, TODO-0409
- P2 SoA canonicalization + semantic memory/perf + multithread substrate + semantic-product boundary hardening: TODO-0401, TODO-0402, TODO-0405, TODO-0406
- P3 Queue/snapshot governance: TODO-0403

### Execution Queue (Recommended)

Wave A (collection stdlib ownership cutover):
1. TODO-0408
2. TODO-0409

Wave B (SoA completion):
1. TODO-0401

Wave C (semantic memory/perf):
1. TODO-0402
2. TODO-0405
3. TODO-0406

Wave D (queue hygiene):
1. TODO-0403

### PrimeStruct Coverage Snapshot

| PrimeStruct area | Primary TODO IDs |
| --- | --- |
| Collection stdlib ownership cutover (`vector`, `map`, `soa_vector`) | TODO-0408, TODO-0409 |
| SoA bring-up and stdlib-authoritative `soa_vector` end-state cleanup | TODO-0401 |
| Semantic memory footprint and multithread compile substrate | TODO-0402 |
| Semantic-product contract/index boundary hardening | TODO-0405, TODO-0406 |
| TODO queue quality gates and dependency/coverage synchronization | TODO-0403 |

### Validation Coverage Snapshot

| Validation area | Primary TODO IDs |
| --- | --- |
| Release gate (`./scripts/compile.sh --release`) discipline | TODO-0401, TODO-0402, TODO-0405, TODO-0406, TODO-0408, TODO-0409 |
| Collection conformance and alias-deletion checks (`vector`/`map`/`soa_vector`) | TODO-0408, TODO-0409 |
| Benchmark/runtime regression checks (`./scripts/benchmark.sh`) | TODO-0402 |
| Semantic-product contract/index and deterministic conformance checks | TODO-0405, TODO-0406 |
| TODO/open-vs-finished hygiene (`docs/todo.md` vs `docs/todo_finished.md`) | TODO-0403 |

### Task Blocks

- [ ] TODO-0409: Remove C++ name routing for `map` + `soa_vector`
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 14
  - depends_on: TODO-0408
  - scope: Complete stdlib-authoritative collection migration by routing `map` and `soa_vector` behavior through `.prime` definitions, then delete remaining production compiler/runtime C++ name/path alias logic for these collections in semantics/lowering (`src/` + `include/primec`, excluding tests/testing-only facades).
  - acceptance:
    - Production `src/` + `include/` collection routing no longer hardcodes `map`/`soa_vector` symbol/path aliases for normal call/access classification; dispatch is generic or intrinsic-driven.
    - Release-mode collection conformance/tests for `map` and `soa_vector` pass, and any changed diagnostics are updated via deterministic snapshots.
    - At least one real compatibility subsystem (legacy alias/canonicalization branch family) is deleted rather than renamed.
    - Focused release-mode suites pass for the migration surface: `./build-release/PrimeStruct_semantics_tests --test-suite=primestruct.semantics.calls_flow.collections`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.ir.pipeline.validation`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.vm.maps`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.vm.collections`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.native_backend.collections`, and `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.emitters.cpp`.
    - Production guardrail check confirms no hardcoded collection compatibility routing remains in semantics/lowerer/emitter code paths (for example via `rg` over `src/semantics`, `src/ir_lowerer`, and emitter sources), with any remaining mentions justified as canonical stdlib or test-only references.
    - Final release gate passes with `./scripts/compile.sh --release`.
  - stop_rule: If landing both collections together is too risky for one commit, split into TODO-0410 (`map`) and TODO-0411 (`soa_vector`) with independent acceptance before continuing.

- [~] TODO-0408: Make `vector` stdlib-authoritative and delete vector-specific C++ classifiers
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 14
  - scope: Move `vector` constructor/method/member behavior to stdlib `.prime` definitions and remove vector-specific production compiler/runtime C++ path/name checks in semantics plus IR lowerer classification/mutation helpers (`src/` + `include/primec`, excluding tests/testing-only facades).
  - acceptance:
    - `vector` semantics/IR behavior is resolved through standard symbol/type flow without dedicated vector-name branches for normal production call/access classification.
    - Release-mode vector semantics + compile-run suites pass after deleting vector-specific compatibility branches.
    - Removed code includes at least one explicit vector alias/canonical-path branch from lowerer/semantics helpers.
    - Focused release-mode suites pass for vector behavior and routing: `./build-release/PrimeStruct_semantics_tests --test-suite=primestruct.semantics.calls_flow.collections`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.ir.pipeline.validation`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.vm.collections`, `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.native_backend.collections`, and `./build-release/PrimeStruct_backend_tests --test-suite=primestruct.compile.run.emitters.cpp`.
    - Tests cover both canonical helper success paths and removed-alias rejection paths (unknown-target/unknown-method diagnostics) so deleted compatibility routing cannot silently regress.
    - Final release gate passes with `./scripts/compile.sh --release`.
  - stop_rule: If shared classifier rewrites destabilize `map`/`soa_vector`, isolate the generic mechanism in this leaf and defer remaining shared deletions to TODO-0409.
  - notes: This leaf should leave a reusable generic path that TODO-0409 can apply to `map` and `soa_vector`.
  - progress: Completed `TODO-0824` by deleting the one-use `preferVisibleVectorHelperMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining shared helper-route-and-finalize scaffold now owns that helper-target normalization and visibility check inline.
  - progress: Completed `TODO-0823` by deleting the one-use `tryResolveVisibleVectorHelperMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining shared helper-route-and-finalize scaffold now owns that visible helper-hit probe inline.
  - progress: Completed `TODO-0822` by deleting the one-use `tryResolveCollectionMethodTargetFromHelperRoute` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining shared helper-route-and-finalize scaffold now owns that visible-helper-hit versus helper-miss dispatch inline.
  - progress: Completed `TODO-0821` by deleting the one-use `routesThroughStdNamespacedVectorCapacityHelper` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper now carries that stdlib helper-path probe inline at the only sites that used the local.
  - progress: Completed `TODO-0820` by deleting the one-use `assignStdNamespacedVectorCapacityMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper now carries that stdlib target assignment inline at the only sites that used the local.
  - progress: Completed `TODO-0819` by deleting the one-use `finalizeCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count receiver helper now passes its finalization logic inline at the only sites that used the local.
  - progress: Completed `TODO-0818` by deleting the one-use `finalizeCapacityMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper now passes its finalization lambda inline at the only site that used the local.
  - progress: Completed `TODO-0817` by deleting the one-use `assignCountMethodTargetAfterResolveMiss` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count receiver helper now carries that resolve-miss fallback body inline at its only site.
  - progress: Completed `TODO-0816` by deleting the one-use `promoteUnknownCapacityMethodTargetIfNeeded` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity finalizer now carries that unknown-target promotion pre-check inline at its only site.
  - progress: Completed `TODO-0815` by deleting the one-use `tryResolveCapacityMethodTargetWithValidation` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper now carries that `tryResolveCollectionMethodTargetOrElse(...)` validation path inline at its only miss site.
  - progress: Completed `TODO-0814` by deleting the one-use `tryResolveCountMethodTargetWithFallback` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count receiver helper now carries that `tryResolveCollectionMethodTargetOrElse(...)` fallback inline at its only miss site.
  - progress: Completed `TODO-0813` by introducing shared `tryResolveCollectionMethodTargetFromHelperRouteOrFinalize` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same helper-route-plus-finalization scaffold.
  - progress: Completed `TODO-0812` by introducing shared `tryResolveCollectionMethodTargetFromHelperRoute` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same visible-helper-hit versus helper-miss control flow.
  - progress: Completed `TODO-0811` by introducing shared `promoteUnknownCapacityMethodTargetIfNeeded` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper no longer duplicates the same unknown-target builtin-promotion pre-check inline.
  - progress: Completed `TODO-0810` by introducing shared `finalizeCollectionMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same post-resolution normalization and ordered failure-tail scaffold.
  - progress: Completed `TODO-0809` by introducing shared `tryResolveCollectionMethodTargetOrElse` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same direct `resolveMethodTarget(...)` success path before their miss-specific handling.
  - progress: Completed `TODO-0808` by introducing shared `failUnknownCollectionMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same unknown-method failure tail.
  - progress: Completed `TODO-0807` by introducing shared `tryResolveVisibleVectorHelperMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same vector-helper-target resolution plus visible-helper acceptance step.
  - progress: Completed `TODO-0806` by introducing shared `tryResolveCapacityMethodTargetWithValidation` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper no longer duplicates the same direct capacity-target validation branch.
  - progress: Completed `TODO-0805` by introducing shared `tryResolveCountMethodTargetWithFallback` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count receiver helper no longer duplicates the same direct count-target resolve-or-fallback branch.
  - progress: Completed `TODO-0804` by reintroducing shared `routesThroughStdNamespacedVectorCapacityHelper` and `assignStdNamespacedVectorCapacityMethodTarget` inside `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity receiver helper no longer duplicates the same stdlib-capacity assignment branch.
  - progress: Completed `TODO-0803` by introducing shared `preferVisibleVectorHelperMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same helper-path normalization and visible-definition acceptance block.
  - progress: Completed `TODO-0802` by introducing shared `isUnknownCollectionMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity receiver helpers no longer duplicate the same inline unknown-method predicate.
  - progress: Completed `TODO-0801` by introducing shared `assignCountMethodTargetAfterResolveMiss` inside `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count target helper no longer duplicates the same resolve-miss fallback body across its helper and direct branches.
  - progress: Completed `TODO-0800` by introducing shared `normalizeInstantiatedCollectionMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity target helpers no longer duplicate the same monomorphized-target normalization block.
  - progress: Completed `TODO-0799` by renaming the stale `tryResolveCollectionMethodFromSurfaceOrReturn` helper back to `tryResolveCollectionMethodFromSurface` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the generic surface helper name now matches its inlined surface-routing body.
  - progress: Completed `TODO-0798` by deleting the one-use `tryResolveCollectionMethodFromSurface` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so `tryResolveCollectionMethodFromSurfaceOrReturn` now owns the shared surface-routing body directly.
  - progress: Completed `TODO-0797` by deleting the mirrored `tryResolveCountMethodFromSurfaceRoute` and `tryResolveCapacityMethodFromSurfaceRoute` wrappers in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the resolver now calls the existing generic post-surface helper directly at those four sites.
  - progress: Completed `TODO-0796` by introducing shared `tryResolveCapacityMethodFromSurfaceRoute` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver no longer duplicates the same post-surface helper call scaffold across its helper and direct routes.
  - progress: Completed `TODO-0795` by introducing shared `tryResolveCountMethodFromSurfaceRoute` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver no longer duplicates the same post-surface helper call scaffold across its single-arg and multi-arg routes.
  - progress: Completed `TODO-0794` by introducing shared `tryResolveCollectionMethodFromSurfaceOrReturn` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver no longer duplicates the same post-surface `handledOut` tail across its four surface-routing sites.
  - progress: Completed `TODO-0793` by deleting the shared `resolveCollectionMethodTargetFromReceiver` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared surface helper now owns the handled/receiver setup directly instead of bouncing through one extra scaffold.
  - progress: Completed `TODO-0792` by introducing shared `tryResolveCollectionMethodFromSurface` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver no longer duplicates the same surface-checked branch body across its four count/capacity sites.
  - progress: Completed `TODO-0791` by introducing shared `routesThroughVectorCountMethodSurface` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver no longer duplicates the same inline entry gate across its single-arg and multi-arg branches.
  - progress: Completed `TODO-0790` by introducing shared `routesThroughVectorCapacityMethodSurface` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver no longer duplicates the same inline entry gate across its helper and direct branches.
  - progress: Completed `TODO-0789` by introducing shared `resolveCollectionMethodTargetFromReceiver` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver no longer duplicates the same handled/receiver/commit scaffolding across its four branch sites.
  - progress: Completed `TODO-0788` by introducing shared `failRemovedRootedVectorDirectCall` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity target helpers no longer duplicate the same removed-rooted-vector diagnostic tail.
  - progress: Completed `TODO-0787` by introducing shared `resolveCapacityMethodTargetFromReceiver` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver no longer duplicates the same target-resolution body across its helper and direct branches.
  - progress: Completed `TODO-0786` by introducing shared `resolveCountMethodTargetFromReceiver` routing in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver no longer duplicates the same target-resolution body across its single-arg and multi-arg branches.
  - progress: Completed `TODO-0785` by deleting the shared `resolveCountMethod` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver now carries its two remaining single-arg and multi-arg routing branches inline at the call sites.
  - progress: Completed `TODO-0784` by deleting the shared `resolveCapacityMethod` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver now carries that helper/direct routing inline at its two remaining call sites.
  - progress: Completed `TODO-0783` by deleting the shared `hasResolvableDefinitionPath` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count/capacity visibility checks now call the declared/imported path predicates inline at each live branch.
  - progress: Completed `TODO-0782` by deleting the shared `failCollectionCountCapacityDiagnostic` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count/capacity diagnostics now call `failExprDiagnostic(expr, ...)` directly at each live branch.
  - progress: Completed `TODO-0781` by deleting the shared `finalizeResolvedCollectionMethodTargetAfterRemovedRootedVectorCheck` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver now carries that removed-rooted and commit tail inline at its two remaining call sites.
  - progress: Completed `TODO-0780` by deleting the shared `normalizeResolvedCollectionMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver now carries that monomorphized-target normalization inline at its two remaining call sites.
  - progress: Completed `TODO-0779` by deleting the one-use `assignCountMethodTargetAfterResolveMiss` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver now carries that resolve-miss fallback inline at its two remaining call sites.
  - progress: Completed `TODO-0778` by deleting the shared `tryResolveCollectionMethodAttempt` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count/capacity resolver now carries that resolve-or-handledOut control flow inline at its four call sites.
  - progress: Completed `TODO-0777` by deleting the one-use `lacksVisibleStdlibMapCountDefinition` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count-side map-count visibility checks now live inline at the only branches that use them.
  - progress: Completed `TODO-0776` by deleting the one-use `isConcreteCountCapacityInstantiation` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the top-level count/capacity instantiation fast path now performs that probe inline.
  - progress: Completed `TODO-0775` by deleting the one-use `finalizeResolvedCollectionMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the removed-rooted shared finalizer now owns the unknown-method/commit tail directly.
  - progress: Completed `TODO-0774` by deleting the one-use `failRemovedRootedVectorDirectCallIfPresent` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared count/capacity finalizer now fetches that removed-rooted-vector diagnostic inline.
  - progress: Completed `TODO-0773` by deleting the one-use `isCapacityMethodTargetMissing` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity promotion/reject flow now carries that missing-target predicate inline again.
  - progress: Completed `TODO-0772` by deleting the one-use `tryResolveCapacityMethodFromHelperRouting` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver now carries that helper/direct entry routing inline.
  - progress: Completed `TODO-0771` by deleting the one-use `tryResolveCapacityMethodWithValidation` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity entry-routing helper now carries that resolve-and-diagnose path inline.
  - progress: Completed `TODO-0770` by deleting the one-use `assignStdNamespacedVectorCapacityMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity entry-routing helper now carries that builtin-target assignment inline.
  - progress: Completed `TODO-0769` by hoisting the duplicated inline capacity missing-target predicate in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `isCapacityMethodTargetMissing`, so the capacity promotion/reject flow no longer repeats that predicate inline.
  - progress: Completed `TODO-0768` by deleting the one-use `isCapacityMethodTargetMissing` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity promotion/reject flow now carries that missing-target predicate inline.
  - progress: Completed `TODO-0767` by deleting the one-use `routesThroughStdNamespacedVectorCapacityHelper` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity entry-routing helper now carries that stdlib-helper-path probe inline.
  - progress: Completed `TODO-0766` by deleting the one-use `tryResolveCapacityMethodAfterHelperMiss` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity entry-routing helper now carries that helper-miss routing inline.
  - progress: Completed `TODO-0765` by deleting the one-use `tryResolveCapacityMethodAfterHelperHit` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity entry-routing helper now carries that helper-hit normalization-and-fallback logic inline.
  - progress: Completed `TODO-0764` by hoisting the remaining capacity helper/direct entry branch in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `tryResolveCapacityMethodFromHelperRouting`, so the capacity helper/direct routing now enters through one shared prelude.
  - progress: Completed `TODO-0763` by hoisting the duplicated capacity helper-hit tail in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `tryResolveCapacityMethodAfterHelperHit`, so the capacity helper/direct routing no longer repeats the same helper-hit normalization-and-fallback branch.
  - progress: Completed `TODO-0762` by hoisting the duplicated capacity helper-miss tail in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `tryResolveCapacityMethodAfterHelperMiss`, so the capacity helper/direct routing no longer repeats the same helper-miss branch tail.
  - progress: Completed `TODO-0761` by hoisting the duplicated `/std/collections/vector/capacity` builtin-target assignment in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `assignStdNamespacedVectorCapacityMethodTarget`, so the capacity helper/direct routing no longer repeats that assignment inline.
  - progress: Completed `TODO-0760` by hoisting the duplicated capacity resolve-miss handling in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `tryResolveCapacityMethodWithValidation`, so the capacity helper/direct routing no longer repeats the same resolve-and-diagnose path.
  - progress: Completed `TODO-0759` by hoisting the duplicated capacity stdlib-helper-path probe in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `routesThroughStdNamespacedVectorCapacityHelper`, so the capacity helper/direct routing no longer repeats that compatibility-path check inline.
  - progress: Completed `TODO-0758` by hoisting the duplicated capacity missing-target predicate in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `isCapacityMethodTargetMissing`, so the capacity unknown-method flow no longer repeats that `!isBuiltinMethod && !hasResolvableDefinitionPath(methodResolved)` check inline.
  - progress: Completed `TODO-0757` by deleting the one-use `methodResolvedMissing` control variable in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity unknown-method flow now carries that missing-target predicate inline at the promotion and reject sites.
  - progress: Completed `TODO-0756` by deleting the one-use `resolveCapacityMethodTarget` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the capacity resolver now carries its helper/direct compatibility routing inline.
  - progress: Completed `TODO-0755` by hoisting the duplicated count resolve-miss fallback body in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `assignCountMethodTargetAfterResolveMiss`, so the count resolver no longer repeats the same map-target-or-pointer-like fallback block across helper and direct resolve misses.
  - progress: Completed `TODO-0753` by deleting the `needsCountMethodResolveOrFallback` control flag in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver now carries its resolve-versus-fallback routing inline across the remaining helper and direct resolve branches.
  - progress: Completed `TODO-0754` by deleting the `bareMapCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count fallback and reject sites now carry the `/map/count` path inline at the only branches that use it.
  - progress: Completed `TODO-0752` by deleting the `routesThroughMapCountCompatibility` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count target-shape and surface gates now carry that map-count compatibility bundle inline at the only branches that use it.
  - progress: Completed `TODO-0751` by deleting the `resolvesMapCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count fallback routing now carries that map-target probe inline at the only branches that use it.
  - progress: Completed `TODO-0750` by deleting the `resolvesExistingCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count target-shape gate now carries that `defMap_.find(resolved)` predicate inline at the only reject branch that uses it.
  - progress: Completed `TODO-0749` by deleting the one-use `tryAssignPointerLikeCountMethodTarget` helper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolve-miss fallback now carries that pointer-like target inference inline at the only branch that uses it.
  - progress: Completed `TODO-0747` by deleting the one-use `usesNamespacedVectorCountFallbackShape` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count target-shape gate now keeps that namespaced-vector fallback predicate inline inside `matchesResolvedCountMethodTargetShape`.
  - progress: Completed `TODO-0746` by deleting the one-use `canUseCountResolveMissFallback` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared count resolve gate now keeps that resolve-miss fallback-eligibility check inline at the only branch that uses it.
  - progress: Completed `TODO-0748` by deleting the remaining one-use `allowsSingleArgUnresolvedCountMethodTarget` and `matchesResolvedCountMethodTargetShape` locals in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count target-shape gate now carries that resolved-target predicate inline at the only reject branch that uses it.
  - progress: Completed `TODO-0745` by deleting the one-use `routesThroughResolvableVectorCountHelperFallback` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver now carries that vector-helper fallback probe inline at the `else if` branch.
  - progress: Completed `TODO-0744` by deleting the one-use `routesThroughBuiltinStdlibMapCountFallback` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count resolver now carries that builtin stdlib map-count fallback-route predicate inline at the branch.
  - progress: Completed `TODO-0743` by deleting the one-use `resolvesStdlibMapCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now carries that stdlib map-count target comparison inline.
  - progress: Completed `TODO-0742` by deleting the one-use `resolvesBareMapCountMethodTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now carries that bare-map target comparison inline.
  - progress: Completed `TODO-0741` by deleting the one-use `targetsDirectNamedBareMapCountCall` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now carries that direct bare-map call-shape predicate inline.
  - progress: Completed `TODO-0740` by deleting the one-use `rejectsUnimportedBareMapCountCallTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now carries that unimported bare-map reject predicate inline.
  - progress: Completed `TODO-0739` by deleting the one-use `rejectsBuiltinStdlibMapCountCallTarget` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now carries that builtin stdlib map-count reject predicate inline.
  - progress: Completed `TODO-0738` by deleting the one-use `rejectsResolvedMapCountCallTarget` wrapper in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining count reject branch now uses the two shared reject predicates directly.
  - progress: Completed `TODO-0736` by hoisting the remaining direct bare-map count call-shape check in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `targetsDirectNamedBareMapCountCall`, so the count reject flow no longer repeats that inline call-shape predicate inside `rejectsUnimportedBareMapCountCallTarget`.
  - progress: Completed `TODO-0737` by routing the remaining count `/std/collections/map/count` reject diagnostic in `SemanticsValidatorExprCollectionCountCapacity.cpp` through shared `stdlibMapCountMethodTarget`, so the count reject branch no longer carries that raw canonical path literal inline.
  - progress: Completed `TODO-0735` by hoisting the remaining vector-helper count fallback probe in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `routesThroughResolvableVectorCountHelperFallback`, so the count resolver no longer open-codes that helper-routing branch condition.
  - progress: Completed `TODO-0734` by hoisting the remaining count resolve-miss body-argument fallback gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `canUseCountResolveMissFallback`, so the count resolver no longer carries that inline fallback-eligibility condition.
  - progress: Completed `TODO-0733` by hoisting the remaining unnamespaced stdlib map-count fallback routing predicate in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `routesThroughBuiltinStdlibMapCountFallback`, so the count resolver no longer carries that branch-local routing condition inline.
  - progress: Completed `TODO-0732` by hoisting the remaining count map target path literals in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `bareMapCountMethodTarget` and `stdlibMapCountMethodTarget`, so the count resolver no longer repeats those raw target strings across assignment and reject-tail path checks.
  - progress: Completed `TODO-0731` by hoisting the remaining count reject-tail path checks in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `resolvesBareMapCountMethodTarget` and `resolvesStdlibMapCountMethodTarget`, so the count reject locals no longer compare those raw `/map/count` strings inline.
  - progress: Completed `TODO-0730` by hoisting the remaining count `/map/count` reject flow in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `rejectsUnimportedBareMapCountCallTarget`, `rejectsBuiltinStdlibMapCountCallTarget`, and `rejectsResolvedMapCountCallTarget`, so the count resolver no longer carries that long reject condition inline.
  - progress: Completed `TODO-0729` by hoisting the remaining count method-target shape gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `resolvesExistingCountMethodTarget`, `allowsSingleArgUnresolvedCountMethodTarget`, and `matchesResolvedCountMethodTargetShape`, so the count resolver no longer open-codes its split single-arg/multi-arg target-shape branch.
  - progress: Completed `TODO-0728` by hoisting the remaining namespaced-vector count fallback shape in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `usesNamespacedVectorCountFallbackShape`, so the single-arg count gate no longer carries that long fallback predicate inline.
  - progress: Completed `TODO-0727` by hoisting the repeated map-count compatibility bundle in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `routesThroughMapCountCompatibility`, so the remaining count routing gates no longer repeat the same four map-count context bits.
  - progress: Completed `TODO-0726` by hoisting the duplicated removed-rooted-vector finalization branch in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `finalizeResolvedCollectionMethodTargetAfterRemovedRootedVectorCheck`, so both count and capacity now reuse one post-routing removed-rooted check before final commit.
  - progress: Completed `TODO-0725` by hoisting the final count/capacity method-target commit path in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `finalizeResolvedCollectionMethodTarget`, so both branches now reuse one unknown-method-or-commit tail.
  - progress: Completed `TODO-0724` by hoisting the duplicated handled-method branch tail in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind shared `tryResolveCollectionMethodAttempt`, so both count and capacity now reuse one `resolveX(...)` / `handledOut` driver instead of repeating the same outer control flow.
  - progress: Completed `TODO-0723` by deleting the one-use `tryAssignCountMethodFallbackAfterResolveMiss` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared count resolve gate now carries its resolve-miss fallback body inline.
  - progress: Completed `TODO-0722` by deleting the one-use `tryResolveCountMethod` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared count resolve gate now inlines its `resolveMethodTarget(..., "count", ...)` probe directly.
  - progress: Completed `TODO-0721` by collapsing the duplicated count resolve-or-fallback tail in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind one shared `needsCountMethodResolveOrFallback` gate, so the count path no longer keeps two separate `tryResolveCountMethod()` / `tryAssignCountMethodFallbackAfterResolveMiss()` branches.
  - progress: Completed `TODO-0720` by hoisting the duplicated monomorphized method-target normalization in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `normalizeResolvedCollectionMethodTarget`, so both count and capacity now reuse one post-routing normalization step.
  - progress: Completed `TODO-0719` by deleting the one-use `rejectsStdlibMapCountTargetAsUnknownCallTarget` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count reject branch now carries its combined `/map/count` and canonical-stdlib unknown-target condition inline.
  - progress: Completed `TODO-0718` by hoisting the duplicated inline count fallback body in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `tryAssignCountMethodFallbackAfterResolveMiss`, so both surviving count branches now reuse one post-resolve-miss fallback path.
  - progress: Completed `TODO-0717` by hoisting the repeated `resolveMethodTarget(..., "count", ...)` probe in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `tryResolveCountMethod`, so both surviving count branches now reuse one resolve step while keeping the fallback body inline.
  - progress: Completed `TODO-0716` by deleting the one-use `tryResolveCountMethodOrFallback` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the two surviving count branches now carry their resolve-or-fallback flow inline.
  - progress: Completed `TODO-0715` by deleting the one-use `tryAssignCountMethodFallbackTarget` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so `tryResolveCountMethodOrFallback` now owns the full count fallback path directly.
  - progress: Completed `TODO-0714` by deleting the one-use `assignBuiltinStdlibMapCountMethodTarget` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the unnamespaced builtin map-count fallback now writes the canonical stdlib target inline.
  - progress: Completed `TODO-0712` by hoisting the repeated `resolveMethodTarget(...)/fallback` control flow in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `tryResolveCountMethodOrFallback`, so both surviving count routing branches now reuse one resolve-or-fallback path.
  - progress: Completed `TODO-0713` by deleting the one-use `assignResolvedStdlibMapCountMethodTarget` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count fallback path now writes the non-builtin `/std/collections/map/count` target inline.
  - progress: Completed `TODO-0711` by hoisting the repeated count fallback routing in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `tryAssignCountMethodFallbackTarget`, so both count fallback branches now reuse one body-argument check plus map-target-or-pointer-like fallback path.
  - progress: Completed `TODO-0710` by hoisting the unnamespaced builtin `/std/collections/map/count` assignment in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `assignBuiltinStdlibMapCountMethodTarget`, so every surviving map-count target assignment in the count resolver now routes through a named helper.
  - progress: Completed `TODO-0709` by hoisting the repeated pointer/reference count-target inference block in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `tryAssignPointerLikeCountMethodTarget`, so both non-map count fallback branches now reuse one pointer-like fallback path.
  - progress: Completed `TODO-0708` by hoisting the repeated non-builtin `/std/collections/map/count` fallback assignment in `SemanticsValidatorExprCollectionCountCapacity.cpp` behind `assignResolvedStdlibMapCountMethodTarget`, so both count method fallback branches now reuse one assign-and-clear path.
  - progress: Completed `TODO-0707` by hoisting `receiver` plus `resolvesMapCountMethodTarget` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the unnamespaced fallback and both count method fallback branches now reuse one map-target probe instead of repeating `context.resolveMapTarget(...)`.
  - progress: Completed `TODO-0706` by moving `lacksVisibleStdlibMapCountDefinition` up to the first unnamespaced map-count fallback in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so both the fallback routing and the later unknown-target rejects now share the same stdlib map-count visibility check.
  - progress: Completed `TODO-0705` by folding the two adjacent `/map/count` unknown-target returns in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `rejectsStdlibMapCountTargetAsUnknownCallTarget`, so the collection-side count map-target failure routing now reuses one reject predicate.
  - progress: Completed `TODO-0704` by hoisting the duplicated visible-`/std/collections/map/count` failure gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `lacksVisibleStdlibMapCountDefinition`, so the two adjacent count map-target reject branches now reuse one visibility check.
  - progress: Completed `TODO-0696` by hoisting the duplicated named-user-receiver probe in `SemanticsValidatorExprCollectionDispatchSetup.cpp` into one shared local `probesNamedStdNamespacedVectorUserReceiver`, so the dispatch-side count and capacity gates now reuse the same condition.
  - progress: Completed `TODO-0700` by deleting the one-use `resolveCountMethodTarget` wrapper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method fallback chain now runs inline instead of routing through a private helper lambda.
  - progress: Completed `TODO-0703` by deleting the standalone `removedRootedVectorDirectCallDiagnostic` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared `failRemovedRootedVectorDirectCallIfPresent` helper now fetches that diagnostic directly.
  - progress: Completed `TODO-0702` by deleting the trivial `hasVisibleStdlibMapCountDefinition` helper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side map-count visibility checks now use the direct declared/imported pair inline.
  - progress: Completed `TODO-0701` by deleting the `resolveBodyArgumentCountTarget` wrapper from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count body-argument fallback now runs inline at its two remaining call sites.
  - progress: Completed `TODO-0699` by deleting the one-use collection-side `stdNamespacedVectorCountDiagnosticMessage` local in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the count diagnostic path now performs the shared selector call directly in the failure branch.
  - progress: Completed `TODO-0698` by deleting the one-use dispatch-side `stdNamespacedVectorCountDiagnosticMessage` local in `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the count diagnostic path now performs the shared selector call directly in the branch head.
  - progress: Completed `TODO-0697` by hoisting the duplicated removed-rooted-vector diagnostic return in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `failRemovedRootedVectorDirectCallIfPresent`, so the count and capacity method paths now reuse one failure helper instead of repeating the same diagnostic branch.
  - progress: Completed `TODO-0695` by deleting the dead `defMap_.count("/std/collections/map/count")` branch from `resolveBodyArgumentCountTarget(...)` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the map-target body-argument fallback now assigns the canonical count path directly.
  - progress: Completed `TODO-0694` by hoisting the repeated shared `vector/count` diagnostic selector call in `SemanticsValidatorExprCollectionDispatchSetup.cpp` into one local `stdNamespacedVectorCountDiagnosticMessage`, so the dispatch-side count diagnostic path no longer recomputes the same selector twice.
  - progress: Completed `TODO-0693` by hoisting the repeated shared `vector/count` diagnostic selector call in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one local `stdNamespacedVectorCountDiagnosticMessage`, so the collection-side count diagnostic path no longer recomputes the same selector twice.
  - progress: Completed `TODO-0690` by deleting the last stale `allowStdNamespacedVectorUserReceiverProbe` reference from the capacity-side branch in `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count/capacity diagnostic gate now keeps that user-receiver probe inline at both surviving branch heads.
  - progress: Completed `TODO-0692` by deleting the one-use `matchesCountMethodCallShape` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-shape gate now keeps its single-arg and non-single-arg checks inline at the branch site.
  - progress: Completed `TODO-0691` by deleting the one-use `allowStdNamespacedVectorUserReceiverProbe` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count/capacity diagnostic gate now keeps that user-receiver probe inline at the branch head.
  - progress: Completed `TODO-0689` by deleting the still-present one-use `stdNamespacedVectorCountTargetDiagnosticMessage` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count diagnostic path now performs the shared selector call directly inside the failure block.
  - progress: Completed `TODO-0688` by deleting the still-present one-use `stdNamespacedVectorCountDiagnosticMessage` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count diagnostic path now performs the shared selector call directly inside the failure branch.
  - progress: Completed `TODO-0687` by deleting the one-use `resolvesMapCountSurface` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count call-shape gate now keeps that map-count surface predicate inline inside `matchesCountMethodCallShape(...)`.
  - progress: Completed `TODO-0686` by deleting the one-use `resolvesExplicitCountMethodTarget` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count call-shape gate now keeps the explicit method-target check inline inside `matchesCountMethodCallShape(...)`.
  - progress: Completed `TODO-0685` by deleting the one-use `tryResolveCountMethod` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-routing path now runs the `resolveCountMethod(true/false)` sequence directly instead of routing through a wrapper lambda.
  - progress: Completed `TODO-0684` by deleting the one-use `isCountLike` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-routing gate now keeps that count-surface predicate inline at the early return.
  - progress: Completed `TODO-0683` by deleting the one-use `resolvesStdNamespacedVectorCountMapTarget` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count diagnostic path now keeps that map-target probe inline inside the shared selector call.
  - progress: Completed `TODO-0682` by deleting the one-use `stdNamespacedVectorCountTargetDiagnosticMessage` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count diagnostic path now performs its shared selector call directly inside the failure block.
  - progress: Completed `TODO-0681` by deleting the one-use `stdNamespacedVectorCountDiagnosticMessage` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count diagnostic path now performs its shared selector call directly inside the failure branch.
  - progress: Completed `TODO-0680` by deleting the one-use `callsStdNamespacedVectorCountHelper` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count wrapper-map target now uses the shared direct-call helper inline instead of carrying a dedicated alias.
  - progress: Completed `TODO-0679` by deleting the one-use `isNamespacedVectorCountCall` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-shape gate now keeps that canonical namespaced-count condition inline inside `matchesCountMethodCallShape(...)`.
  - progress: Completed `TODO-0678` by deleting the now-dead `isNamespacedVectorCountCall` bit from `ExprCollectionDispatchSetup`, so the dispatch-setup path no longer stores a canonical count call-shape that nothing consumes.
  - progress: Completed `TODO-0677` by deleting the still-threaded `isNamespacedVectorCountCall` bit from `ExprCollectionCountCapacityDispatchContext`, so the collection-side count method-shape gate now recomputes that canonical namespaced-count condition locally instead of depending on private context handoff from `SemanticsValidatorExpr.cpp`.
  - progress: Completed `TODO-0676` by deleting the redundant `!expr.isMethodCall` recheck from the dispatch-side `setupOut.isNamespacedVectorCountCall` assignment in `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so that count call-shape gate now relies directly on `!callsStdNamespacedVectorCountHelper`.
  - progress: Completed `TODO-0675` by deleting the dead `callsStdNamespacedVectorCountHelper` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count compatibility path no longer carries an unused canonical count direct-call alias.
  - progress: Completed `TODO-0674` by deleting the one-use `callsInvisibleStdNamespacedVectorCountHelper` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side count diagnostic selector now inlines the shared invisible direct-call predicate directly.
  - progress: Completed `TODO-0673` by deleting the one-use `callsUnimportedStdNamespacedVectorCountHelper` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-routing gate now inlines the shared unimported direct-call predicate directly.
  - progress: Completed `TODO-0672` by deleting the one-use `callsUndeclaredStdNamespacedVectorCountHelper` and `callsUnresolvableStdNamespacedVectorCountHelper` locals from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count diagnostic selector now inlines those shared helper predicates directly.
  - progress: Completed `TODO-0671` by deleting the redundant `!callsStdNamespacedVectorCountHelper` recheck from `matchesCountMethodCallShape(...)` in `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the collection-side count method-routing gate now relies directly on `context.isNamespacedVectorCountCall`.
  - progress: Completed `TODO-0670` by hoisting the remaining unimported canonical `vector/count` direct-call gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `callsUnimportedStdNamespacedVectorCountHelper`, so the collection-side count method-routing boundary no longer keeps that count-specific import gate open-coded.
  - progress: Completed `TODO-0669` by adding shared `classifyStdNamespacedVectorCountDiagnosticMessage(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining dispatch/count-capacity canonical `vector/count` diagnostic selection through it, so those paths no longer keep split inline unknown-target versus requires-vector-target message logic.
  - progress: Completed `TODO-0668` by adding shared `isInvisibleStdNamespacedVectorCompatibilityDirectCall(...)`, `isUndeclaredStdNamespacedVectorCompatibilityDirectCall(...)`, and `isUnresolvableStdNamespacedVectorCompatibilityDirectCall(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining dispatch/count-capacity canonical `vector/count` availability checks through them, so those paths now express helper-visibility intent directly instead of reusing the generic unavailable-direct-call helper inline.
  - progress: Completed `TODO-0667` by routing the remaining dispatch-side canonical `vector/capacity` direct-call plus helper-availability checks in `SemanticsValidatorExprCollectionDispatchSetup.cpp` through shared `isStdNamespacedVectorCompatibilityDirectCall(...)` and `isUnavailableStdNamespacedVectorCompatibilityDirectCall(...)`, so that path no longer hand-rolls a raw stdlib capacity probe.
  - progress: Completed `TODO-0666` by adding shared `isUnavailableStdNamespacedVectorCompatibilityDirectCall(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining direct stdlib `vector/count` plus helper-availability checks in `SemanticsValidatorExprCollectionDispatchSetup.cpp` and `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those count compatibility paths no longer hand-roll the same direct-call-with-missing-helper condition.
  - progress: Completed `TODO-0665` by deleting the redundant `!expr.isMethodCall` guards that still wrapped `callsStdNamespacedVectorCountHelper` in `SemanticsValidatorExprCollectionDispatchSetup.cpp`, so the dispatch-side wrapper-map and unknown-target count branches now rely on the shared direct-call probe alone.
  - progress: Completed `TODO-0664` by folding the repeated collection-side canonical `vector/count` direct-call probe in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `callsStdNamespacedVectorCountHelper`, so that file no longer repeats the same `isStdNamespacedVectorCompatibilityDirectCall(...)` call across count-target diagnostics and count method-call-shape handling.
  - progress: Completed `TODO-0663` by folding the repeated dispatch-side canonical `vector/count` direct-call probe in `SemanticsValidatorExprCollectionDispatchSetup.cpp` into shared local `callsStdNamespacedVectorCountHelper`, so that file no longer repeats the same `isStdNamespacedVectorCompatibilityDirectCall(...)` call across namespaced-count, wrapper-map, and unknown-target handling.
  - progress: Completed `TODO-0662` by deleting the one-use `isStdNamespacedVectorCountCall` local from `SemanticsValidatorExprCollectionDispatchSetup.cpp` and routing its remaining uses directly through shared `isStdNamespacedVectorCompatibilityDirectCall(...)`, so the dispatch setup path no longer carries a redundant canonical count call-shape alias.
  - progress: Completed `TODO-0661` by deleting the one-use `isStdNamespacedVectorCountCall` local from `SemanticsValidatorExprCollectionCountCapacity.cpp` and routing its remaining uses directly through shared `isStdNamespacedVectorCompatibilityDirectCall(...)`, so the collection count-capacity path no longer carries a redundant canonical count call-shape alias.
  - progress: Completed `TODO-0660` by deleting the one-use `shouldBuiltinValidateStdNamespacedVectorCountCall` local from `SemanticsValidatorExprCollectionCountCapacity.cpp` and routing that imported canonical count gate through shared `isUnimportedStdNamespacedVectorCompatibilityDirectCall(...)`, so the collection count-capacity path no longer carries a redundant count-specific validation bit.
  - progress: Completed `TODO-0659` by routing the remaining dispatch-side canonical `vector/count` call-shape probe in `SemanticsValidatorExprCollectionDispatchSetup.cpp` through shared `isStdNamespacedVectorCompatibilityDirectCall(...)`, so that file no longer carries its own raw stdlib count-path classifier.
  - progress: Completed `TODO-0658` by deleting the dead `isStdNamespacedVectorCountCall` field from the broader collection dispatch setup, so `SemanticsValidatorExprCollectionDispatchSetup.cpp` now derives that canonical count call-shape locally at its remaining dispatch-side use sites instead of storing it in the private setup struct.
  - progress: Completed `TODO-0657` by deleting the dead `isStdNamespacedVectorCountCall` field from the count-capacity context, so `SemanticsValidatorExprCollectionCountCapacity.cpp` now derives that canonical count call-shape locally from the current expr instead of threading a dedicated bit through `SemanticsValidatorExpr.cpp` and the private count-capacity struct.
  - progress: Completed `TODO-0656` by deleting the dead `shouldBuiltinValidateStdNamespacedVectorCountCall` plumbing from the collection dispatch setup and count-capacity context, so `SemanticsValidatorExprCollectionCountCapacity.cpp` now derives that imported canonical count gate locally instead of threading a dedicated validation bit through private structs and `SemanticsValidatorExpr.cpp`.
  - progress: Completed `TODO-0655` by folding the collection-side canonical `vector/count` method-resolution sequencing in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared local `tryResolveCountMethod`, so the collection path no longer keeps the repeated `resolveCountMethod(true/false)` plus `handledOut` control flow open-coded.
  - progress: Completed `TODO-0654` by folding the resolved and unresolved expr builtin canonical `vector/count` branches in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `tryValidateVectorCountBuiltinPath`, so the builtin path no longer keeps those two `validateVectorCountBuiltinPath(...)` entry branches split apart.
  - progress: Completed `TODO-0653` by folding the collection-side canonical `vector/count` call-shape gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared local `matchesCountMethodCallShape`, so `resolveCountMethod(...)` no longer keeps that split single-arg/non-single-arg eligibility logic open-coded.
  - progress: Completed `TODO-0652` by folding the collection-side canonical `vector/count` vector-helper/method/body-target fallback chain in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared local `resolveCountMethodTarget`, so `resolveCountMethod(...)` now reuses one resolver instead of spelling that count-specific fallback sequence inline.
  - progress: Completed `TODO-0651` by folding the resolved and unresolved expr builtin canonical `vector/count` paths in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` through one shared local `validateVectorCountBuiltinPath(bool allowBareMapRewrite)`, so the resolved canonical branch now reuses the same path helper as the unresolved fallback instead of calling the raw validator directly.
  - progress: Completed `TODO-0650` by folding the unresolved expr builtin canonical `vector/count` fallback branch body in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `validateVectorCountBuiltinFallback`, so the remaining fallback branch now reduces to a single call instead of carrying inline rewrite-or-validate control flow.
  - progress: Completed `TODO-0649` by folding the bare-map rewrite step inside the expr builtin canonical `vector/count` fallback in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `tryRewriteBareMapCountBuiltinFallback`, so the remaining fallback branch now keeps only the high-level rewrite-or-validate control flow instead of carrying that branch-local rewrite sequence inline.
  - progress: Completed `TODO-0648` by folding the unresolved expr builtin canonical `vector/count` fallback gate in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `shouldValidateVectorCountBuiltinFallback`, so the remaining builtin count fallback handling no longer keeps that long compatibility predicate open-coded at the branch site.
  - progress: Completed `TODO-0647` by folding the duplicated expr builtin canonical `vector/count` validation body in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `validateVectorCountBuiltinCall`, so the surviving resolved-method and builtin-fallback count branches now reuse one validator instead of carrying copied validation logic.
  - progress: Completed `TODO-0646` by adding shared `isImportedResolvedStdNamespacedVectorCompatibilityDirectCall(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the expr builtin canonical `vector/count` plus `vector/capacity` direct-call checks in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` through it, so the builtin path no longer carries count-specific direct-call locals or duplicate imported-and-resolved direct-call shapes.
  - progress: Completed `TODO-0645` by folding the collection-side canonical `vector/capacity` method-target fallback chain in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared local `resolveCapacityMethodTarget`, so the surviving capacity method path now reuses one resolver instead of spelling the same vector-helper-to-method-target fallback chain twice.
  - progress: Completed `TODO-0644` by hoisting the duplicated missing-method state in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared local `methodResolvedMissing`, so the surviving collection-side canonical `vector/capacity` promotion/failure flow now reuses one resolution-miss check instead of recomputing it twice.
  - progress: Completed `TODO-0643` by folding the duplicated expr builtin `vector/capacity` validation body in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` into one shared local `validateVectorCapacityBuiltinCall`, so the surviving resolved-method and builtin-fallback capacity branches now reuse one validator instead of carrying copied lambda bodies.
  - progress: Completed `TODO-0642` by adding shared `isImportedStdNamespacedVectorCompatibilityDirectCall(...)` and `isUnimportedStdNamespacedVectorCompatibilityDirectCall(...)` helpers in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining expr-side canonical `vector/capacity` imported-helper visibility handling in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through them, so those two surviving capacity paths no longer spell the import gate separately.
  - progress: Completed `TODO-0641` by adding shared predicate `isStdNamespacedVectorCompatibilityDirectCall(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining expr-side canonical `vector/capacity` direct-call checks in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those two surviving capacity paths no longer duplicate the raw `!expr.isMethodCall && isStdNamespacedVectorCompatibilityHelperPath(...)` probe.
  - progress: Completed `TODO-0640` by deleting the one-use `isDirectStdNamespacedVectorCapacityBuiltinCall` local from `SemanticsValidatorExprCountCapacityMapBuiltins.cpp`, so the surviving direct canonical `vector/capacity` builtin branch now uses the direct shared helper-path/imported-helper check inline instead of carrying a redundant builtin-call alias.
  - progress: Completed `TODO-0639` by deleting the one-use `isStdNamespacedVectorCapacityCall` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining canonical `vector/capacity` method gate now uses the direct shared helper-path check inline instead of carrying a redundant capacity call-shape alias.
  - progress: Completed `TODO-0638` by deleting the one-use `shouldBuiltinValidateStdNamespacedVectorCapacityCall` local from `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the remaining canonical `vector/capacity` method gate now uses the direct imported helper check inline instead of carrying a redundant validation bit.
  - progress: Completed `TODO-0637` by adding shared predicate `isStdNamespacedVectorCompatibilityHelperPath(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the expr-side canonical `vector/capacity` call-shape checks in `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those two surviving capacity paths no longer duplicate the raw stdlib `resolveCalleePath(expr).rfind(...)` probe.
  - progress: Completed `TODO-0636` by deleting the one-use `shouldBuiltinValidateStdNamespacedVectorCapacityCall` local from `SemanticsValidatorExprCountCapacityMapBuiltins.cpp`, so the surviving direct builtin `vector/capacity` guard now uses a direct imported canonical-callee-path check instead of carrying a redundant validation bit.
  - progress: Completed `TODO-0635` by deleting the one-use `isStdNamespacedVectorCapacityCall` local from `SemanticsValidatorExprCountCapacityMapBuiltins.cpp`, so the remaining canonical `vector/capacity` fallback branch now uses a direct negated stdlib-callee-path check instead of carrying a redundant capacity call-shape bit.
  - progress: Completed `TODO-0634` by deleting the one-use infer-side `isDirectBuiltinCountCapacityCapacityCall` wrapper from `SemanticsValidatorInferCollectionDispatchSetup.cpp`, so the remaining infer direct capacity flag now uses the shared capacity/direct-target pieces directly while the shared direct single-arg helper stays in place.
  - progress: Completed `TODO-0633` by folding the infer-side direct single-argument `vector/capacity` gate in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `isDirectBuiltinSingleArgCountCapacityCapacityCall`, so the remaining infer direct single-arg capacity flag now reuses the named direct single-arg classifier instead of spelling the condition inline.
  - progress: Completed `TODO-0632` by folding the infer-side `vector/capacity` direct-target gate in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `inferBuiltinCapacityCallTargetsDirectReceiver`, so the remaining infer direct capacity and direct single-arg capacity flags now reuse the same resolved-target condition instead of spelling it separately.
  - progress: Completed `TODO-0631` by folding the infer-side single-argument `vector/capacity` gate in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `isInferBuiltinSingleArgCapacityLike`, so the remaining infer dispatch and direct single-arg capacity flags now reuse the same single-arg classifier instead of spelling it separately.
  - progress: Completed `TODO-0630` by folding the duplicated infer-side builtin `vector/capacity` control-flow base in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `isInferBuiltinCapacityLike`, so the remaining infer capacity dispatch and direct-call guards now reuse the same call-shape/visibility classifier instead of repeating it.
  - progress: Completed `TODO-0629` by folding the duplicated infer-side canonical `vector/capacity` helper-visibility guard in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `stdNamespacedVectorCapacityHelperAvailableForInfer`, so the remaining infer capacity guards now reuse one availability check instead of repeating the same imported-helper condition twice.
  - progress: Completed `TODO-0628` by folding the duplicated infer-side canonical `vector/capacity` callee-path probe in `SemanticsValidatorInferCollectionDispatchSetup.cpp` into one shared local `callsStdNamespacedVectorCapacityHelper`, so the remaining infer capacity guards now reuse one path classifier instead of repeating the same inline check twice.
  - progress: Completed `TODO-0627` by deleting the infer-side `isStdNamespacedVectorCapacityCall` local from `SemanticsValidatorInferCollectionDispatchSetup.cpp` and switching its two remaining capacity guards to direct canonical-callee-path checks, so the infer dispatch path now carries one fewer raw stdlib `vector/capacity` call-shape local.
  - progress: Completed `TODO-0626` by deleting the redundant infer-side `shouldBuiltinValidateStdNamespacedVectorCapacityCall` bit from `SemanticsValidatorInferCollectionDispatchSetup.cpp`, removing its now-unused imported-definition alias, and switching the remaining infer capacity guards to direct imported canonical-helper checks, so the infer dispatch path now carries one fewer dedicated stdlib `vector/capacity` validation local.
  - progress: Completed `TODO-0625` by deleting the redundant `isStdNamespacedVectorCapacityCall` field from `ExprCollectionDispatchSetup`, replacing its remaining uses in `SemanticsValidatorExprCollectionDispatchSetup.cpp` with one local canonical-callee-path guard, and tightening source-lock coverage around the stale setup plumbing, so the broader collection-dispatch path now carries one fewer raw stdlib `vector/capacity` call-shape bit.
  - progress: Completed `TODO-0624` by deleting the redundant `shouldBuiltinValidateStdNamespacedVectorCapacityCall` field from `ExprCollectionDispatchSetup`, removing its dead assignment plus now-unused imported-definition alias from `SemanticsValidatorExprCollectionDispatchSetup.cpp`, and tightening source-lock coverage around the stale setup plumbing, so the broader collection-dispatch path now carries one fewer dead stdlib `vector/capacity` validation bit.
  - progress: Completed `TODO-0623` by deleting the redundant `isStdNamespacedVectorCapacityCall` field from `ExprCollectionCountCapacityDispatchContext`, switching `SemanticsValidatorExprCollectionCountCapacity.cpp` to a direct canonical-callee-path guard at its remaining use site, and removing the stale handoff from `SemanticsValidatorExpr.cpp`, so the broader count-capacity dispatch path now carries one fewer raw stdlib `vector/capacity` call-shape bit.
  - progress: Completed `TODO-0622` by deleting the redundant `shouldBuiltinValidateStdNamespacedVectorCapacityCall` field from `ExprCollectionCountCapacityDispatchContext`, switching `SemanticsValidatorExprCollectionCountCapacity.cpp` to a direct imported capacity-helper visibility guard at its remaining use site, and removing the stale handoff from `SemanticsValidatorExpr.cpp`, so the broader count-capacity dispatch path now carries one fewer stdlib `vector/capacity` validation bit.
  - progress: Completed `TODO-0621` by deleting the redundant `isStdNamespacedVectorCapacityCall` field from `ExprCountCapacityMapBuiltinContext`, switching `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` to a direct canonical-callee-path guard at its remaining fallback use site, and removing the stale setup/call-site plumbing, so the count/capacity builtin context now carries one fewer raw stdlib `vector/capacity` call-shape bit.
  - progress: Completed `TODO-0620` by deleting the redundant `shouldBuiltinValidateStdNamespacedVectorCapacityCall` field from `ExprCountCapacityMapBuiltinContext`, switching `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` to a direct imported canonical-callee-path guard at its two remaining use sites, and removing the stale setup/call-site plumbing, so the count/capacity builtin context now carries one fewer stdlib `vector/capacity` validation bit.
  - progress: Completed `TODO-0619` by deleting the redundant `shouldBuiltinValidateStdNamespacedVectorCountCall` field from `ExprCountCapacityMapBuiltinContext`, switching `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` to a direct imported canonical-callee-path guard at its two remaining use sites, and removing the stale setup/call-site plumbing, so the count/capacity builtin context now carries one fewer stdlib `vector/count` validation bit.
  - progress: Completed `TODO-0618` by deleting the redundant `isStdNamespacedVectorCountCall` field from `ExprCountCapacityMapBuiltinContext`, switching `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` to the direct `resolveCalleePath(expr)` check at its only remaining use site, and removing the stale setup/call-site plumbing, so the count/capacity builtin context now carries one fewer count-specific flag.
  - progress: Completed `TODO-0617` by deleting the single-use `hasStdNamespacedVectorCountDefinition` alias from `SemanticsValidatorExprCollectionDispatchSetup.cpp` and inlining its `hasImportedDefinitionPath("/std/collections/vector/count")` check into `setupOut.shouldBuiltinValidateStdNamespacedVectorCountCall`, so the dispatch-setup path now carries one fewer count-specific local.
  - progress: Completed `TODO-0616` by switching the count-capacity `vector/count` map-target reject in `SemanticsValidatorExprCollectionCountCapacity.cpp` from a duplicated declared/imported pair to `hasResolvableDefinitionPath("/std/collections/vector/count")`, so that site now reuses the file’s generic definition-visibility helper instead of spelling the pair inline.
  - progress: Completed `TODO-0615` by switching the late-call `vector/count` vector-like reject in `SemanticsValidatorExprLateCallCompatibility.cpp` from duplicated declared/imported checks to `hasVisibleDefinitionPath("/std/collections/vector/count")`, so that site now uses the generic visibility surface instead of a count-specific visibility pair.
  - progress: Completed `TODO-0614` by deleting the duplicated `hasDeclaredStdNamespacedVectorCountHelper` / `hasImportedStdNamespacedVectorCountHelper` locals from `SemanticsValidatorExprLateCallCompatibility.cpp` and `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the two remaining `vector/count` compatibility sites now inline their declared/imported helper checks directly instead of carrying repeated local state.
  - progress: Completed `TODO-0613` by deleting `StdNamespacedVectorCompatibilityHelperState` from `SemanticsVectorCompatibilityHelpers.h` and replacing its two remaining `vector/count` uses with direct declared/imported helper locals in `SemanticsValidatorExprLateCallCompatibility.cpp` and `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared header no longer carries a count-specific state wrapper.
  - progress: Completed `TODO-0612` by deleting the last shared `classifyNonVectorCountTargetDiagnosticMessage(...)` method from `SemanticsVectorCompatibilityHelpers.h` and inlining its two remaining `vector/count` reject paths in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp`, so the shared helper-state surface is now plain state instead of behavior-bearing count-specific logic.
  - progress: Completed `TODO-0611` by deleting the single-use `classifyCountMapTargetDiagnosticMessage(...)` method from `SemanticsVectorCompatibilityHelpers.h` and folding its logic into `classifyNonVectorCountTargetDiagnosticMessage(...)`, so the shared `vector/count` helper-state API no longer carries an internal map-target forwarding layer.
  - progress: Completed `TODO-0610` by deleting the single-use `classifyVectorLikeCountTargetDiagnosticMessage(...)` method from `SemanticsVectorCompatibilityHelpers.h` and inlining its late-call reject branch in `SemanticsValidatorExprLateCallCompatibility.cpp`, so the shared `vector/count` helper-state surface no longer owns a one-off vector-like diagnostic entry point.
  - progress: Completed `TODO-0609` by deleting `makeStdNamespacedVectorCompatibilityHelperState(...)` from `SemanticsVectorCompatibilityHelpers.h` and switching the remaining `vector/count` compatibility sites to direct `StdNamespacedVectorCompatibilityHelperState{...}` construction, so the shared header now exposes one fewer non-behavioral factory helper.
  - progress: Completed `TODO-0608` by deleting the trivial `lacksDeclaredHelper()` and `lacksVisibleHelper()` accessors from `SemanticsVectorCompatibilityHelpers.h` and inlining those checks into the remaining `vector/count` helper-state entry points, so the shared compatibility API now exposes only behavior-bearing helpers instead of private boolean wrappers.
  - progress: Completed `TODO-0607` by deleting the single-use `rejectsVectorLikeTargetWithoutVisibleHelper(...)` and `rejectsWrapperMapTargetWithoutDeclaredHelper(...)` wrappers from `SemanticsVectorCompatibilityHelpers.h` and inlining those checks into the surviving `vector/count` helper-state entry points, so the shared compatibility surface now exposes fewer private predicate layers.
  - progress: Completed `TODO-0606` by deleting the internal enum/free-function `vector/count` map-target translation layer from `SemanticsVectorCompatibilityHelpers.h` and making `StdNamespacedVectorCompatibilityHelperState::classifyCountMapTargetDiagnosticMessage(...)` compute its string result directly, so the shared helper-state API no longer depends on a private diagnostic enum shim.
  - progress: Completed `TODO-0605` by deleting the dead four-argument `StdNamespacedVectorCompatibilityHelperState::classifyCountTargetDiagnosticMessage(...)` entry from `SemanticsVectorCompatibilityHelpers.h` and extending the IR source-lock harness to read that helper header directly, so the unused `vector/count` helper-state layer now stays absent both at call sites and at its former definition site.
  - progress: Completed `TODO-0604` by moving the remaining late-call stdlib `vector/count` vector-like reject onto `StdNamespacedVectorCompatibilityHelperState::classifyVectorLikeCountTargetDiagnosticMessage(...)`, so `SemanticsValidatorExprLateCallCompatibility.cpp` no longer uses the broader four-argument count-target helper at that site.
  - progress: Completed `TODO-0603` by routing the remaining stdlib `vector/count` non-vector reject flow in `SemanticsValidatorExprCollectionCountCapacity.cpp` through `StdNamespacedVectorCompatibilityHelperState::classifyNonVectorCountTargetDiagnosticMessage(...)`, so the count-capacity side now shares the same non-vector helper-state path as `SemanticsValidatorExprLateCallCompatibility.cpp` instead of using a separate four-argument count-target call.
  - progress: Completed `TODO-0602` by folding the split stdlib `vector/count` wrapper-map and map-target reject branches in `SemanticsValidatorExprCollectionCountCapacity.cpp` into one shared `classifyCountTargetDiagnosticMessage(...)` call on `StdNamespacedVectorCompatibilityHelperState`, so that file no longer carries a standalone wrapper-only reject path ahead of the shared count-target diagnostic flow.
  - progress: Completed `TODO-0601` by moving the late-call stdlib `vector/count` non-vector fallback message into `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing `SemanticsValidatorExprLateCallCompatibility.cpp` through `classifyNonVectorCountTargetDiagnosticMessage(...)`, so that file no longer carries a bespoke “shared map-target message or else `count requires vector target`” branch.
  - progress: Completed `TODO-0600` by moving the remaining stdlib `vector/count` wrapper-map reject onto `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing both `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through the four-argument `classifyCountTargetDiagnosticMessage(...)` shape, so those files no longer mix helper-state-based map-target diagnostics with a standalone wrapper-map reject branch.
  - progress: Completed `TODO-0599` by moving the remaining shared stdlib `vector/count` reject-message decision onto `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing both `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through `classifyCountTargetDiagnosticMessage(...)`, so those files no longer spell the final count-target reject/message flow separately.
  - progress: Completed `TODO-0598` by moving the shared stdlib `vector/count` map-target diagnostic flow onto `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing both `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through the new state methods, so those files no longer call standalone count-map classifier/message helpers.
  - progress: Completed `TODO-0597` by adding shared reject predicates to `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining split `vector/count` target-reject booleans in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through them, so those files no longer spell those helper-state control-flow checks independently.
  - progress: Completed `TODO-0596` by adding shared `classifyStdNamespacedVectorCountMapTargetDiagnosticMessage(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining split `vector/count` map-target message-classification flow in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those files no longer perform the classifier-then-message sequence independently.
  - progress: Completed `TODO-0595` by adding shared `vectorCompatibilityCountMapTargetDiagnosticMessage(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining split `vector/count` map-target reject handling in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those files no longer translate the shared count-map diagnostic enum independently.
  - progress: Completed `TODO-0594` by adding shared `StdNamespacedVectorCompatibilityHelperState` in `SemanticsVectorCompatibilityHelpers.h` and routing the surviving stdlib `vector/count` helper-state handling in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so those files no longer maintain separate declared/imported/visible helper state shapes.
  - progress: Completed `TODO-0593` by adding shared `classifyStdNamespacedVectorCountMapTargetDiagnostic(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the split `vector/count` map-target compatibility branches in `SemanticsValidatorExprLateCallCompatibility.cpp` and `SemanticsValidatorExprCollectionCountCapacity.cpp` through it, so that those two files no longer classify the same stdlib map-target case independently.
  - progress: Completed `TODO-0592` by deleting dead `prefersCanonicalVectorCountAliasDefinition` plumbing from the count-capacity dispatch context path, so that the remaining `vector/count` compatibility cleanup no longer threads another unused alias-preference flag through `SemanticsValidatorExpr.cpp`.
  - progress: Completed `TODO-0591` by deleting dead `hasStdNamespacedVectorCountAliasDefinition` plumbing from the collection dispatch setup/count-capacity context path, so that the remaining `vector/count` compatibility cleanup no longer threads unused alias-definition state through `SemanticsValidatorExpr.cpp`.
  - progress: Completed `TODO-0590` by routing the remaining canonical `vector/capacity` unknown-target diagnostics in `SemanticsValidatorExprLateCallCompatibility.cpp` and `SemanticsValidatorExprCollectionDispatchSetup.cpp` through shared `vectorCompatibilityUnknownCallTargetDiagnostic(...)`, so that those capacity compatibility branches no longer duplicate the literal stdlib vector call-target string.
  - progress: Completed `TODO-0589` by adding shared `vectorCompatibilityUnknownCallTargetDiagnostic(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the remaining canonical `vector/count` unknown-target diagnostics through it, so that those count compatibility branches no longer duplicate the literal stdlib vector call-target string.
  - progress: Completed `TODO-0588` by hoisting the late-call stdlib `vector/count` map-target reject in `SemanticsValidatorExprLateCallCompatibility.cpp` into local `rejectsStdNamespacedVectorCountMapTargetAsUnknownCallTarget`, so that the non-vector map-target path no longer leaves that canonical `unknown call target` classification as a raw inline branch.
  - progress: Completed `TODO-0587` by hoisting the remaining stdlib `vector/count` map-target imported-or-missing-helper state in `SemanticsValidatorExprCollectionCountCapacity.cpp` into local `rejectsStdNamespacedVectorCountMapTargetForImportedOrMissingDeclaredHelper`, so that the non-vector map-target reject path no longer repeats that inline declared/imported helper condition.
  - progress: Completed `TODO-0586` by hoisting the shared visible-helper state in the `/std/collections/vector/count` late-call compatibility branch into local `lacksVisibleStdNamespacedVectorCountHelper`, so that the remaining vector-target reject path no longer repeats that inline declared/imported helper check.
  - progress: Completed `TODO-0585` by hoisting shared declared-helper state for the remaining stdlib vector-count map-target rejects in `SemanticsValidatorExprCollectionCountCapacity.cpp` into local `lacksDeclaredStdNamespacedVectorCountHelper`, so that the wrapper-map and map-target paths no longer repeat that inline declared-helper lookup.
  - progress: Completed `TODO-0584` by hoisting the shared vector-family classification inside the `/std/collections/vector/count` late-call compatibility branch into local `resolvesVectorLikeCountTarget`, so that the remaining non-vector and visible-helper reject gates no longer repeat that inline experimental-vector pairing.
  - progress: Completed `TODO-0583` by hoisting the stdlib vector-count wrapper-map reject in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `rejectsStdNamespacedVectorCountWrapperMapTargetWithoutDeclaredHelper`, so that the remaining wrapper-map path no longer repeats that inline declared-helper gate.
  - progress: Completed `TODO-0582` by hoisting the late-call canonical vector-count helper-visibility reject in `SemanticsValidatorExprLateCallCompatibility.cpp` into shared local `rejectsVectorCountTargetWithoutVisibleHelper`, so that the remaining vector-target branch no longer repeats that inline visibility gate.
  - progress: Completed `TODO-0581` by hoisting the surviving stdlib vector-count map-target reject gate in `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `rejectsStdNamespacedVectorCountMapTargetAsNonVector`, so that the count map-target path no longer repeats that inline helper-visibility condition.
  - progress: Completed `TODO-0580` by hoisting the late-call non-vector classification inside the `/std/collections/vector/count` compatibility branch into shared local `resolvesNonVectorCountTarget`, so that remaining count target handling no longer repeats that inline target-shape check.
  - progress: Completed `TODO-0579` by hoisting the surviving stdlib vector-count map-target classification inside `SemanticsValidatorExprCollectionCountCapacity.cpp` into shared local `resolvesStdNamespacedVectorCountMapTarget`, so that remaining count target handling no longer repeats that inline compatibility probe.
  - progress: Completed `TODO-0578` by folding the duplicated post-validation map-target probe inside the `/std/collections/vector/count` late-call compatibility branch into shared local `resolvesMapAfterValidation`, so the remaining non-vector count path no longer repeats that inline compatibility check.
  - progress: Completed `TODO-0577` by routing the duplicated `count requires vector target` diagnostics in `SemanticsValidatorExprLateCallCompatibility.cpp` and `SemanticsValidatorExprCollectionCountCapacity.cpp` through shared helper `vectorCompatibilityRequiresVectorTargetDiagnostic(...)`, so the remaining count target-diagnostic ownership no longer depends on duplicated string literals.
  - progress: Completed `TODO-0576` by introducing shared helper `vectorCompatibilityRequiresVectorTargetDiagnostic(...)` in `SemanticsVectorCompatibilityHelpers.h` and routing the duplicated `capacity requires vector target` diagnostics in `SemanticsValidatorExprLateCallCompatibility.cpp` plus `SemanticsValidatorExprCountCapacityMapBuiltins.cpp` through it, so that remaining capacity target-diagnostic ownership no longer depends on duplicated string literals.
  - progress: Completed `TODO-0575` by folding the duplicated array/map `unknown call target` returns inside the `/std/collections/vector/capacity` late-call compatibility branch into one shared `resolvesNonVectorCollectionLikeTarget` gate, so the remaining non-vector capacity path now has one collection-like reject before the final `capacity requires vector target` diagnostic.
  - progress: Completed `TODO-0574` by deleting the duplicate vector-receiver no-helper reject from the `/std/collections/vector/capacity` branch in `SemanticsValidatorExprLateCallCompatibility.cpp`, so that canonical `unknown call target` handling now stays anchored on the earlier dispatch-setup path.
  - progress: Completed `TODO-0573` by deleting the duplicate `/std/collections/vector/count` no-helper reject branch from `SemanticsValidatorExprDirectCollectionFallbacks.cpp`, so that compatibility rejection now stays anchored on the surviving late-call compatibility path in `SemanticsValidatorExprLateCallCompatibility.cpp`.
  - progress: Completed `TODO-0572` by deleting the single-use `ExprDirectCollectionFallbackContext` wrapper, so `validateExprDirectCollectionFallbacks` now takes `BuiltinCollectionDispatchResolvers` directly and the remaining `/std/collections/vector/count` no-helper reject branch no longer depends on a dedicated fallback context type.
  - progress: Completed `TODO-0571` by deleting the single-use `isStdNamespacedVectorCountCall` field from `ExprDirectCollectionFallbackContext`, so the remaining `/std/collections/vector/count` no-helper reject branch in `SemanticsValidatorExprDirectCollectionFallbacks.cpp` now uses its direct canonical callee-path check instead of dedicated fallback-context plumbing.
  - progress: Completed `TODO-0570` by deleting the single-use `resolveStringTarget` and `resolveArrayTarget` aliases from `SemanticsValidatorExprDirectCollectionFallbacks.cpp`, so the remaining `/std/collections/vector/count` no-helper reject branch now uses the dispatch resolver surface directly instead of temporary alias state.
  - progress: Completed `TODO-0569` by deleting the single-use `resolvesExperimentalVectorValueReceiverForBareAccess` helper from `SemanticsValidatorExprDirectCollectionFallbacks.cpp`, so the experimental-vector bare `at`/`at_unsafe` receiver detection now lives directly in the fallback branch instead of a temporary helper wrapper.
  - progress: Completed `TODO-0568` by deleting the single-use `bareVectorAccessMethodMissingSamePathHelper` local from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining bare vector `at`/`at_unsafe` same-path-helper rejection now lives in one direct inline branch instead of a temporary helper wrapper.
  - progress: Completed `TODO-0567` by hoisting the duplicated experimental-vector canonical fallback condition in `SemanticsValidatorExprMethodResolution.cpp` into shared local `shouldRewriteExperimentalVectorCompatibilityMethodTargetToCanonical`, so the two surviving pre-resolution and post-resolution fallback branches now share one compatibility predicate while keeping their explicit rewrite assignments.
  - progress: Completed `TODO-0566` by deleting `isExperimentalVectorCompatibilityMethodTarget(...)` from `SemanticsValidatorExprMethodResolution.cpp`, so the two surviving experimental-vector canonical fallback branches now own their direct target-prefix checks instead of routing through a shared vector-specific classifier helper.
  - progress: Completed `TODO-0565` by deleting `rewriteEligibleExperimentalVectorCompatibilityMethodTargetToCanonical` from `SemanticsValidatorExprMethodResolution.cpp`, so the two surviving experimental-vector fallback sites now own their explicit canonical rewrite branches instead of routing through a shared vector-specific helper.
  - progress: Completed `TODO-0564` by making `rewriteEligibleExperimentalVectorCompatibilityMethodTargetToCanonical` in `SemanticsValidatorExprMethodResolution.cpp` a pure return-value helper, so the two surviving vector fallback sites now assign from the shared rewrite instead of passing mutable targets by reference.
  - progress: Completed `TODO-0563` by deleting the outer `canonicalVectorCompatibilityMethodTarget` and `hasVisibleCanonicalVectorCompatibilityMethodTarget` locals from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining shared vector fallback helper now owns its canonical target and visibility check directly.
  - progress: Completed `TODO-0562` by deleting `rewriteExperimentalVectorCompatibilityMethodTargetToCanonical` from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining shared vector fallback helper now owns both eligibility and experimental-vector canonical rewrite conditions directly.
  - progress: Completed `TODO-0561` by deleting the paired eligibility locals `vectorMethodTargetMissing` and `resolvedVectorCompatibilityMethodTargetEligible` from `SemanticsValidatorExprMethodResolution.cpp`, so both surviving vector fallback sites now pass their eligibility conditions inline to the shared rewrite-branch helper.
  - progress: Completed `TODO-0560` by hoisting the shared “eligible target then canonical rewrite” branch shape in `SemanticsValidatorExprMethodResolution.cpp` into local `rewriteEligibleExperimentalVectorCompatibilityMethodTargetToCanonical`, so the pre-resolution and post-resolution paths now keep only their distinct eligibility locals.
  - progress: Completed `TODO-0559` by hoisting the post-resolution vector compatibility rewrite guard in `SemanticsValidatorExprMethodResolution.cpp` into shared local `resolvedVectorCompatibilityMethodTargetEligible`, so the surviving post-resolution fallback now keeps only the rewrite call in its branch body.
  - progress: Completed `TODO-0558` by hoisting the pre-resolution vector method-target missing check in `SemanticsValidatorExprMethodResolution.cpp` into shared local `vectorMethodTargetMissing`, so the surviving pre-resolution fallback now keeps only the rewrite call in its branch body.
  - progress: Completed `TODO-0557` by hoisting the surviving experimental-vector-to-canonical target rewrite in `SemanticsValidatorExprMethodResolution.cpp` into shared local `rewriteExperimentalVectorCompatibilityMethodTargetToCanonical`, so the pre-resolution and post-resolution fallback sites now keep only their site-specific outer guards.
  - progress: Completed `TODO-0556` by hoisting the duplicated experimental-vector method-target prefix test in `SemanticsValidatorExprMethodResolution.cpp` into shared `isExperimentalVectorCompatibilityMethodTarget(...)`, so the two surviving canonical vector fallback branches now reuse one compatibility-path classifier instead of duplicating inline prefix checks.
  - progress: Completed `TODO-0555` by hoisting the shared canonical stdlib vector visibility check into `hasVisibleCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp`, so the two surviving experimental-vector fallback branches now reuse one canonical-visibility gate while still keeping their explicit target-prefix checks inline.
  - progress: Completed `TODO-0554` by deleting `shouldPreferCanonicalVectorCompatibilityMethodTarget` from `SemanticsValidatorExprMethodResolution.cpp`, so the last experimental-vector-to-canonical method fallback condition now lives directly at the two remaining pre-resolution and post-resolution rewrite sites instead of behind a single-use predicate helper.
  - progress: Completed `TODO-0553` by deleting the helper-local `targetsExperimentalVectorCompatibilityPath` alias from `shouldPreferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp`, so the remaining predicate is now just one direct returned expression over the experimental-vector target prefixes plus canonical visibility.
  - progress: Completed `TODO-0552` by deleting the helper-local `hasVisibleCanonicalVectorCompatibilityTarget` alias from `shouldPreferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp`, so the remaining predicate now keeps just one experimental-vector path classifier and inlines the canonical visibility check in its returned condition.
  - progress: Completed `TODO-0551` by replacing `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp` with shared predicate `shouldPreferCanonicalVectorCompatibilityMethodTarget` plus direct caller-owned rewrites, so the remaining experimental-vector canonical fallback logic now separates the compatibility decision from the actual target assignment.
  - progress: Completed `TODO-0550` by hoisting the canonical stdlib vector visibility check inside `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp` into shared local `hasVisibleCanonicalVectorCompatibilityTarget`, so the helper’s remaining branch now composes one experimental-vector compatibility-path flag with one canonical-visibility flag instead of mixing both checks inline.
  - progress: Completed `TODO-0549` by hoisting the experimental-vector prefix test inside `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp` into shared local `targetsExperimentalVectorCompatibilityPath`, so the helper no longer keeps the remaining compatibility-path classifier as an open-coded inline boolean expression.
  - progress: Completed `TODO-0548` by hoisting the helper-local canonical stdlib vector path inside `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp`, so its remaining visibility check and return now share one `canonicalTarget` string instead of duplicating `"/std/collections/vector/" + expr.name` inline.
  - progress: Completed `TODO-0547` by making `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp` a pure rewrite helper that returns the preferred target string instead of mutating a reference, so the two surviving experimental-vector fallback sites now share one functional rewrite path instead of stateful in-place target updates.
  - progress: Completed `TODO-0546` by deleting the redundant canonical-target parameter from `preferCanonicalVectorCompatibilityMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp`, so the two surviving experimental-vector fallback sites now share one fixed `/std/collections/vector/` rewrite target instead of passing separate equivalent target expressions into the helper.
  - progress: Completed `TODO-0545` by folding the two surviving experimental-vector-to-canonical method fallback branches in `SemanticsValidatorExprMethodResolution.cpp` into one shared local helper, so pre-resolution and post-resolution vector compatibility rewrites now route through the same fallback logic instead of duplicate inline branches.
  - progress: Completed `TODO-0544` by moving the shared vector helper-name predicate into `src/semantics/SemanticsVectorCompatibilityHelpers.h`, so `SemanticsValidatorExprMethodResolution.cpp` and `SemanticsValidatorExprVectorHelpers.cpp` now reuse one compatibility-name classifier instead of carrying duplicated helper-name lists.
  - progress: Completed `TODO-0543` by hoisting `isVectorCompatibilityMethod` earlier in `SemanticsValidatorExprMethodResolution.cpp` and deleting the lingering `hasVisibleCanonicalVectorHelperForMethod` alias, so the early namespace-empty builtin vector receiver gate now reuses the same compatibility-method classifier as the later vector helper-routing branch while the experimental-vector fallback sites keep canonical helper visibility inline.
  - progress: Completed `TODO-0542` by deleting the single-use `hasCanonicalOnlyVectorHelper` local from `SemanticsValidatorExprMethodResolution.cpp`, so the builtin vector receiver check now uses its canonical-only helper visibility condition inline.
  - progress: Completed `TODO-0541` by deleting the single-use `normalizedMethodNamespace` local from `SemanticsValidatorExprMethodResolution.cpp`, so the surviving vector-compatible method gate now uses direct explicit namespace comparisons instead of normalized namespace state.
  - progress: Completed `TODO-0540` by hoisting the repeated canonical stdlib vector visibility check in `SemanticsValidatorExprMethodResolution.cpp` into shared local `hasVisibleCanonicalVectorHelperForMethod`, so the two remaining experimental-vector fallback sites now reuse one visibility condition instead of duplicating the same inline check.
  - progress: Completed `TODO-0539` by deleting the file-scope `isVectorCompatibilityMethodName(...)` helper from `SemanticsValidatorExprMethodResolution.cpp`, so the surviving vector-compatible method branches now share one local `isVectorCompatibilityMethod` state inside `validateExprMethodCallTarget(...)`.
  - progress: Completed `TODO-0538` by deleting the single-use `hasExplicitVectorCompatibilityNamespace(...)` helper from `SemanticsValidatorExprMethodResolution.cpp`, so the surviving vector-compatible method gate now uses its direct namespace inequality check inline.
  - progress: Completed `TODO-0537` by deleting the `canonicalVectorMethodTarget` local from `SemanticsValidatorExprMethodResolution.cpp`, so the two remaining canonical vector fallback sites now use the direct `"/std/collections/vector/" + expr.name` path inline instead of a shared local alias.
  - progress: Completed `TODO-0536` by deleting `hasVisibleCanonicalVectorMethodTarget` from `SemanticsValidatorExprMethodResolution.cpp`, so the two remaining canonical vector fallback sites now use the direct canonical visibility condition inline instead of a shared local alias.
  - progress: Completed `TODO-0535` by deleting the single-use `isExperimentalVectorCompatibilityMethodTarget(...)` helper from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining pre-resolution experimental-vector fallback now uses its one direct `vectorMethodTarget.rfind("/std/collections/experimental_vector/", 0)` guard inline.
  - progress: Completed `TODO-0534` by deleting the single-use `isExperimentalVectorCompatibilityResolvedMethodTarget(...)` helper from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining post-resolution canonical fallback now uses its one direct `resolved.rfind("/std/collections/experimental_vector/Vector__", 0)` guard inline.
  - progress: Completed `TODO-0533` by deleting `preferVisibleCanonicalVectorMethodTarget(...)` from `SemanticsValidatorExprMethodResolution.cpp`, so both remaining experimental-vector fallback sites now use the already-hoisted canonical target and visibility state directly instead of routing through a single-use helper.
  - progress: Completed `TODO-0532` by hoisting canonical vector-target visibility in `SemanticsValidatorExprMethodResolution.cpp` into one shared boolean, so `preferVisibleCanonicalVectorMethodTarget(...)` no longer rechecks canonical helper visibility inline.
  - progress: Completed `TODO-0531` by hoisting the canonical `/std/collections/vector/` method target string in `SemanticsValidatorExprMethodResolution.cpp` out of `preferVisibleCanonicalVectorMethodTarget(...)`, so the remaining canonical-preference helper now reuses one surrounding path value instead of rebuilding it inline.
  - progress: Completed `TODO-0530` by deleting the single-use `preferResolvedVectorCompatibilityMethodTarget` helper from `SemanticsValidatorExprMethodResolution.cpp`, so the surviving post-resolution rewrite now routes directly through the shared predicates plus `preferVisibleCanonicalVectorMethodTarget(...)`.
  - progress: Completed `TODO-0529` by simplifying `preferVisibleCanonicalVectorMethodTarget` in `SemanticsValidatorExprMethodResolution.cpp` to a pure canonical-preference helper, deleting its `requireCurrentTargetMissing` flag and moving the missing-target guard to the single caller that needs it.
  - progress: Completed `TODO-0528` by hoisting the surviving resolved-target experimental-vector classifier in `SemanticsValidatorExprMethodResolution.cpp` into one shared file-scope predicate, deleting the last inline `resolved.rfind("/std/collections/experimental_vector/Vector__", 0)` test from the post-resolution compatibility helper.
  - progress: Completed `TODO-0527` by hoisting the surviving experimental-vector target classifier in `SemanticsValidatorExprMethodResolution.cpp` into one shared file-scope predicate, deleting the last inline `isExperimentalVectorMethodTarget` local from the canonical fallback helper.
  - progress: Completed `TODO-0526` by folding the surviving experimental-vector post-resolution method gate in `SemanticsValidatorExprMethodResolution.cpp` into one shared helper, deleting the last open-coded `resolved.rfind("/std/collections/experimental_vector/Vector__", 0)` compatibility branch in that method-resolution path.
  - progress: Completed `TODO-0525` by folding the duplicated experimental-vector-to-canonical method fallback in `SemanticsValidatorExprMethodResolution.cpp` into one shared helper, deleting the last repeated inline `canonicalVectorMethodTarget` rewrite block in that method-resolution path.
  - progress: Completed `TODO-0524` by deleting the temporary `hasExplicitVectorCompatibilityNamespace` and `isVectorCompatibilityMethodName` booleans from `SemanticsValidatorExprMethodResolution.cpp`, so the remaining vector-compatible method cluster now routes through shared file-scope predicates instead of ad-hoc local aliases.
  - progress: Completed `TODO-0523` by deleting the redundant preferred-target fast-path from `SemanticsValidatorExprMethodResolution.cpp`, so vector-compatible method-call resolution now routes call-receiver helper selection through `resolveVectorHelperMethodTarget(...)` instead of a separate pre-resolution `/vector` branch.
  - progress: Completed `TODO-0522` by deleting the rooted `/vector/{count,capacity,at,at_unsafe}` pre-reject from `SemanticsValidatorExprMethodResolution.cpp`, so explicit alias-method acceptance/rejection now stays owned by the remaining helper-routing path instead of a redundant early unknown-method branch.
  - progress: Completed `TODO-0521` by folding the surviving named-versus-positional initial receiver branch in `SemanticsValidatorExprVectorHelpers.cpp` into one shared selector helper, deleting the last open-coded initial receiver branch in that helper-routing path.
  - progress: Completed `TODO-0520` by hoisting the surviving positional initial receiver sequence in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last open-coded positional initial receiver call in that helper-routing path.
  - progress: Completed `TODO-0519` by hoisting the surviving named initial receiver sequence in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last open-coded sequential named receiver-resolution calls in that helper-routing path.
  - progress: Completed `TODO-0518` by deleting the single-use `tryResolveInitialReceiver` wrapper in `SemanticsValidatorExprVectorHelpers.cpp`, simplifying the surviving initial receiver control flow to the already-hoisted named and primary receiver helpers.
  - progress: Completed `TODO-0517` by hoisting the surviving named fallback receiver probe in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last nested helper declaration in that helper-routing path.
  - progress: Completed `TODO-0516` by folding the surviving `resolvedReceiver` state mutation in `SemanticsValidatorExprVectorHelpers.cpp` behind one shared local helper, deleting the last repeated direct state writes in that helper-routing path.
  - progress: Completed `TODO-0515` by folding the surviving reordered receiver probe in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last open-coded reordered probe branch in that helper-routing path.
  - progress: Completed `TODO-0514` by folding the surviving initial named-versus-positional receiver resolution in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last open-coded `if (hasNamedArgs)` control-flow fork in that helper-routing path.
  - progress: Completed `TODO-0513` by hoisting the surviving named `values` receiver resolution in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last inline named receiver-resolution branch in that helper-routing path.
  - progress: Completed `TODO-0512` by folding the surviving “scan remaining receiver args” loop in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last duplicated trailing receiver-scan loop in that helper-routing path.
  - progress: Completed `TODO-0511` by hoisting the surviving primary receiver probe in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last duplicated `resolvedReceiver = tryResolveReceiverIndex(0)` branch in that helper-routing path.
  - progress: Completed `TODO-0510` by folding the surviving named-argument receiver fallback branch in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local helper, deleting the last repeated `!hasNamedValuesReceiver && !resolvedReceiver` branch cluster in that helper-routing path.
  - progress: Completed `TODO-0509` by folding the surviving positional reordered-receiver gate in `SemanticsValidatorExprVectorHelpers.cpp` into one shared helper predicate, deleting the last open-coded reordered-receiver probe branch in that helper-routing path.
  - progress: Completed `TODO-0508` by hoisting the surviving named `values` receiver scan in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local index/flag pair, deleting the last inline `hasValuesNamedReceiver` scan branch in that helper-routing path.
  - progress: Completed `TODO-0507` by hoisting the surviving canonical vector helper-resolution `defMap_.find(resolved) == defMap_.end()` gate in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local, deleting the last repeated inline resolution-missing check in that helper-routing path.
  - progress: Completed `TODO-0506` by folding the surviving canonical direct-call receiver-family branch in `SemanticsValidatorExprVectorHelpers.cpp` into one shared receiver-compatibility predicate, deleting the last open-coded `vector`/`experimental_vector` receiver check in that helper-routing path.
  - progress: Completed `TODO-0505` by hoisting the surviving canonical count/capacity named-argument exception in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local gate, deleting the last repeated inline named-argument exception cluster in that vector helper-routing path.
  - progress: Completed `TODO-0504` by folding the surviving canonical `/std/collections/vector/*` method-call acceptance branch in `SemanticsValidatorExprVectorHelpers.cpp` into one shared helper predicate, deleting the last open-coded method-call gate in that vector helper-routing path.
  - progress: Completed `TODO-0503` by hoisting the repeated canonical `/std/collections/vector/*` direct-call condition in `SemanticsValidatorExprVectorHelpers.cpp` into one shared local gate, deleting the last repeated inline direct-call predicate cluster in that helper-routing path.
  - progress: Completed `TODO-0502` by folding the surviving canonical `/std/collections/vector/*` direct-call gate in `SemanticsValidatorExprVectorHelpers.cpp` into shared helper predicates, deleting the last local alias plus the repeated inline direct-call condition cluster in that vector helper-routing path.
  - progress: Completed `TODO-0501` by deleting the one-use `isStdNamespacedVectorCanonicalHelperCall` alias from `SemanticsValidatorExprVectorHelpers.cpp`, so the remaining canonical vector helper-routing cluster now inlines the direct-call shape check instead of carrying another vector-specific boolean.
  - progress: Completed `TODO-0500` by deleting the one-use `shouldProbeVectorHelperReceiver` alias from `SemanticsValidatorExprVectorHelpers.cpp`, so the canonical vector helper-routing probe now inlines that condition directly at the receiver-resolution site instead of carrying another vector-specific boolean.
  - progress: Completed `TODO-0499` by deleting the one-use `allowStdNamespacedUserReceiverProbe` alias from `SemanticsValidatorExprVectorHelpers.cpp`, so the remaining canonical vector helper-routing cluster now inlines the named-argument probe condition instead of carrying another vector-specific boolean.
  - progress: Completed `TODO-0498` by deleting the one-use `isExplicitStdNamespacedVectorCompatibilityMethod` alias from `SemanticsValidatorExprVectorHelpers.cpp`, so canonical vector method-call acceptance now inlines `expr.isMethodCall && isStdNamespacedVectorCanonicalCompatibilityHelper` instead of carrying a dedicated vector-specific boolean.
  - progress: Completed `TODO-0497` by deleting the one-use `builtinCompatibleReceiver` and `hasCompatibleDeclaredHelper` aliases from the canonical vector receiver-shape guard in `SemanticsValidatorExprVectorHelpers.cpp`, so that remaining `/std/collections/vector/*` direct-call receiver check now stays in one branch instead of splitting vector-specific subconditions into separate locals.
  - progress: Completed `TODO-0496` by hoisting `hasNamedArguments(expr.argNames)` to one shared `hasNamedArgs` value in `SemanticsValidatorExprVectorHelpers.cpp`, so the remaining canonical vector named-argument probe cluster no longer recomputes the same expression in two separate local branches.
  - progress: Completed `TODO-0495` by deleting the redundant `hasVisibleStdNamespacedVectorCanonicalHelper` boolean from `SemanticsValidatorExprVectorHelpers.cpp`, so canonical vector direct-call rejection now checks `hasVisibleDefinitionPath(resolved)` directly instead of carrying a one-use vector-specific visibility alias.
  - progress: Completed `TODO-0494` by replacing the remaining exact canonical vector method-path list in `SemanticsValidatorExprVectorHelpers.cpp` with one shared namespaced-helper classifier, so canonical `count`/`capacity`/`at`/`at_unsafe` method handling now follows the same generic helper-shape check already used for direct calls.
  - progress: Completed `TODO-0493` by deleting the last single-use `getDirectVectorHelperCompatibilityPath(...)` canonical direct-call shim from `SemanticsValidatorExprVectorHelpers.cpp` and `SemanticsValidatorInferCollectionCompatibility.cpp`, so canonical `/std/collections/vector/*` direct-call rejection now stays on the generic unknown-target branch instead of a vector-specific helper-path owner.
  - progress: Removed semantic method-resolution fallback from canonical stdlib vector helpers back to `/vector/*` alias paths in `SemanticsValidatorExprMethodResolution.cpp`, and added semantics coverage for canonical helper success plus explicit `/vector/count` method-call rejection.
  - progress: Removed method-call fallback that treated `/std/collections/vectorCount` and `/std/collections/vectorCapacity` alias helpers as valid providers for `values.count()` / `values.capacity()`, and added semantics coverage that alias-only helpers now reject with `unknown method` diagnostics.
  - progress: Extended semantics coverage to include canonical `values.capacity()` success and explicit `values./vector/capacity()` alias rejection when only canonical stdlib helpers exist.
  - progress: Extended semantics coverage to include canonical `values.at(...)` / `values.at_unsafe(...)` success and explicit `values./vector/at(...)` / `values./vector/at_unsafe(...)` alias rejection when only canonical stdlib helpers exist.
  - progress: Removed alias-dependent gating from vector method prevalidation so canonical `/std/collections/vector/at` and `/std/collections/vector/at_unsafe` integer-index diagnostics stay enforced even when `/vector/*` aliases exist, with coverage for alias-shadow scenarios.
  - progress: Removed explicit `values./vector/...` method-namespace compatibility for vector `count`/`capacity`/`at`/`at_unsafe` receivers and added coverage that those method forms now reject with `unknown method` even when `/vector/*` helpers are declared.
  - progress: Pruned `/vector/count` and `/vector/capacity` alias-path checks from explicit vector count/capacity method-path validation and added alias-declared rejection coverage for `values./vector/at(...)` and `values./vector/at_unsafe(...)`.
  - progress: Removed production wrapper-routing classifiers that treated `/std/collections/vectorCount` and `/std/collections/vectorCapacity` as direct vector builtins in semantics + lowerer helper-resolution/monomorph compatibility paths; direct canonical handling now stays on `/std/collections/vector/count` and `/std/collections/vector/capacity` (with experimental-vector canonical rewrites preserved).
  - progress: Removed the remaining `/std/collections/vectorXxx` wrapper-name routing for vector access/mutator helpers in production lowerer helper classification plus semantics compatibility/monomorph bridge tables, so canonical `/std/collections/vector/*` and experimental-vector helper paths are the only direct vector helper routes.
  - progress: Deleted the last semantics-only wrapper compatibility surface that accepted `/std/collections/vectorAt`/`/std/collections/vectorAtUnsafe`, and removed the monomorph exception that allowed templated `auto` parameters for `/std/collections/vectorXxx` wrapper definitions.
  - progress: Removed production `/vector/count` and `/vector/capacity` alias routing from lowerer vector helper alias resolution/receiver-probe classification and from semantics builtin count-capacity dispatch return-kind + method-call handling, including pre-dispatch canonical rewrite from `/vector/count` to `/std/collections/vector/count`.
  - progress: Removed remaining semantics dispatch/setup compatibility scaffolding that depended on `/vector/count` alias canonicalization, restricted late-call vector count/capacity compatibility checks to canonical `/std/collections/vector/*` paths, and switched unresolved `capacity()` builtin promotion to canonical `/std/collections/vector/capacity`.
  - progress: Removed remaining emitter + lowerer count/capacity alias hardcoding for `/vector/*` in direct-call type inference and removed-helper inline filtering; canonical `/std/collections/vector/count` and `/std/collections/vector/capacity` are now the only explicit vector count/capacity helper paths in those production classifiers.
  - progress: Pruned vector count/capacity `/vector/*` alias entries from template-monomorph compatibility mapping and semantic compatibility descriptor tables (including removed-helper descriptor list), and removed method-target compatibility guards that still treated `/vector/count` and `/vector/capacity` as distinct paths.
  - progress: Removed remaining `/vector/at` and `/vector/at_unsafe` alias routing from semantics direct-fallback/return-kind dispatch, emitter vector-access type inference/classification, lowerer explicit-vector-access classifiers, and template-compatibility alias tables so vector access helper routing is canonicalized on `/std/collections/vector/at` and `/std/collections/vector/at_unsafe`.
  - progress: Removed remaining `/vector/*` mutator alias mapping (`push`/`pop`/`reserve`/`clear`/`remove_at`/`remove_swap`) from experimental-vector helper canonicalization and vector compatibility descriptor aliases, and pruned emitter/lowerer direct-call alias filters so production mutator routing stays on canonical `/std/collections/vector/*` helper paths.
  - progress: Removed another cross-stage `/vector/*` compatibility subsystem by deleting alias candidate expansion/fallback from template-monomorph, pre-dispatch return-kind inference, struct-return path helpers, and emitter metadata candidate lookup; direct `/vector/*` paths now stay isolated instead of auto-bridging to canonical `/std/collections/vector/*`.
  - progress: Pruned additional `/vector/*` compatibility fallback from semantics method-target alias candidate expansion plus lowerer/emitter setup helper path preference (`preferVectorStdlibHelperPath`, setup collection candidate builders, and receiver-target method lookup), so canonical `/std/collections/vector/*` is preferred without back-routing to `/vector/*`.
  - progress: Removed another struct-return/type-inference compatibility family that cross-expanded `/vector/*` and `/std/collections/vector/*` in semantics + emitter collection helper candidate builders and return-inference method-path selection, keeping direct `/vector/*` lookup isolated while canonical stdlib vector paths remain authoritative.
  - progress: Canonicalized struct-return method candidate routing in semantics + lowerer and aligned emitter vector-access candidate pruning to `/std/collections/vector/*`, removing remaining fallback branches that still treated `/vector/*` as an automatic bridge in this struct-return inference path family.
  - progress: Removed `/vector/*` alias fallback from effect-free collection method candidate selection and helper-path preference/candidate expansion, so this effect-free semantics path now prefers canonical `/std/collections/vector/*` without back-routing through compatibility aliases.
  - progress: Removed initializer-time fallback that rewrote unresolved canonical `/std/collections/vector/count|capacity` calls back to `/vector/*` during binding validation and local-auto call inference, and updated preferred initializer helper resolution to no longer prefer `/vector/*` aliases over canonical stdlib vector helpers.
  - progress: Removed remaining template-monomorph `/vector/*` helper-path auto-bridge in `preferVectorStdlibHelperPath`/`preferVectorStdlibTemplatePath` and aligned pointer-like call candidate expansion to keep explicit `/vector/*` isolated while canonical `/std/collections/vector/*` paths no longer back-expand to `/vector/*`.
  - progress: Removed another cross-stage canonical-backfallback compatibility family by deleting `/std/collections/vector/* -> /array/*` fallback from semantics, template monomorph, emitter, and lowerer helper-path preference/candidate builders, so explicit canonical vector helper lookup now stays isolated instead of downshifting to `/array/*`.
  - progress: Removed another multi-stage canonical-backfallback family by deleting remaining `/std/collections/vector/* -> /array/*` fallback branches in semantics pre-dispatch + struct-return inference, emitter metadata/setup return inference + collection type helpers, and lowerer setup-type helper resolution/candidate builders; canonical stdlib vector helper lookup now stays isolated through these inference/classification paths.
  - progress: Removed another vector alias-compatibility subsystem in expression helper resolution by making `preferredBareVectorHelperTarget` canonical-first, dropping canonical stdlib helper fallback checks that still treated `/vector/*` helper declarations as equivalent for experimental-vector access (`at`/`at_unsafe`), and deleting late `/vector/* -> /std/collections/vector/*` fallback rewrite from `resolveExprVectorHelperCall`.
  - progress: Removed a broad dead-branch compatibility cleanup family by deleting no-op `/vector/*` and `/std/collections/vector/*` isolation stubs from semantics/emitter/lowerer/template helper candidate and preference builders now that those branches no longer perform fallback mutations.
  - progress: Removed `/vector/*` alias fallback from collection-access method targeting by deleting experimental-vector access fallback branches that rewrote to `/vector/<helper>` in `SemanticsValidatorExprCollectionAccess.cpp`, and tightened collection-access validation to require canonical `/std/collections/vector/<helper>` visibility instead of accepting `/vector/<helper>` declarations.
  - progress: Removed another lowerer + semantic-product compatibility subsystem by deleting `/vector/*` fallback rewrites from bare vector helper emission (`count`/`capacity`/`at`/`at_unsafe`) and by pruning `/vector/*` bridge-path classification in semantics/lowerer snapshot plumbing so bridge choices now treat canonical `/std/collections/vector/*` and experimental-vector paths as authoritative.
  - progress: Removed another inference/emission compatibility subsystem by disabling std-namespaced vector access fallback to `/vector/*` definitions in infer dispatch setup, canonicalizing infer late-fallback vector helper routing directly to `/std/collections/vector/*`, and pruning rooted `/vector/*` mutator alias recognition from lowerer statement helper resolution and vector-flow receiver normalization paths.
  - progress: Removed another rooted `/vector/*` compatibility family by deleting alias-only vector helper acceptance in method-target resolution (including explicit helper-path compatibility and vector-family classifier checks), pruning `/vector/*` acceptance in late unknown-target vector helper fallback, dropping `/vector/at*` from lowerer removed-helper filtering, narrowing setup-type direct-call guardrails to canonical `/std/collections/vector/*`, and removing named-argument/body-argument scaffolding that still treated `/vector/*` as a required compatibility surface.
  - progress: Removed another semantics+emitter vector-mutator compatibility family by deleting explicit `/vector/*` mutator call/method path handling and std-namespaced-to-alias fallback from statement helper validation, restricting emitter explicit-request filtering to canonical `/std/collections/vector/*` helper paths, tightening emitter method-call count/capacity compatibility checks to canonical vector helpers, and pruning pre-dispatch normalization branches that still treated bare `vector/*` helper tokens as compatibility candidates.
  - progress: Removed another vector helper rewrite/classification compatibility subsystem by pruning `/vector/*` visibility preference branches from semantics vector helper rewrite + builtin-name checks (`preferredBareVectorHelperTarget`, bare-vector helper rewrites, and vector helper visibility guards) and deleting emitter method-resolution parsing/classification branches that still treated rooted `vector/*` method names as a removed-collection alias family.
  - progress: Removed another infer+monomorph rooted-vector compatibility family by dropping `vector/*` removed-helper parsing in collection compatibility resolution (`getVectorStatementHelperName`/`getDirectVectorHelperCompatibilityPath`) and template-monomorph alias normalization (`isExplicitRemovedCollectionMethodAlias`, `preferVectorStdlibTemplatePath`, `isExplicitCollectionCompatibilityAliasPath`), while aligning statement helper user-target filtering to canonical `/std/collections/vector/*` paths.
  - progress: Removed another named-argument compatibility fallback by stopping unresolved explicit `/vector/at` and `/vector/at_unsafe` alias calls from being classified as builtin access helpers in semantic named-argument validation, so removed aliases now surface unknown-target diagnostics instead of builtin named-argument diagnostics; added semantics + C++ emitter coverage for both aliases.
  - progress: Completed child slice `TODO-0412` (archived in `docs/todo_finished.md`): shared semantics/lowerer access/helper recognizers no longer classify removed rooted `/vector/*` helper spellings as builtin vector helper metadata, while canonical `/std/collections/vector/*` spellings stay accepted.
  - progress: Completed child slice `TODO-0413` (archived in `docs/todo_finished.md`): shared collection constructor recognizers no longer classify removed rooted `/vector/vector` as a builtin vector constructor, while the lowerer still accepts canonical `/std/collections/vector/vector`.
  - progress: Completed child slice `TODO-0414` (archived in `docs/todo_finished.md`): shared semantics/lowerer/emitter simple-call helpers no longer treat removed rooted `/vector/vector` as a builtin `vector(...)` token, while bare `vector(...)` calls stay accepted.
  - progress: Completed child slice `TODO-0415` (archived in `docs/todo_finished.md`): emitter vector-mutator helper classification no longer treats removed rooted `/vector/*` mutator spellings as builtin helpers, while canonical `/std/collections/vector/*` mutator paths stay accepted.
  - progress: Completed child slice `TODO-0416` (archived in `docs/todo_finished.md`): semantics removed-alias helper classifiers no longer parse removed rooted `/vector/*` call/method spellings, while the remaining `/array/*` and canonical `/std/collections/vector/*` helper checks stay covered.
  - progress: Completed child slice `TODO-0417` (archived in `docs/todo_finished.md`): lowerer count-access classifiers/helpers no longer parse rooted `/vector/count` as a vector helper alias or compatibility name, while canonical `/std/collections/vector/count` handling stays covered through bundled classifier tests.
  - progress: Completed child slice `TODO-0418` (archived in `docs/todo_finished.md`): lowerer setup-type collection helper classification no longer parses removed rooted `/vector/count` through `resolveVectorHelperAliasName(...)`/`getNamespacedCollectionHelperName(...)`, while canonical `/std/collections/vector/count` setup-type helper detection stays covered.
  - progress: Completed child slice `TODO-0419` (archived in `docs/todo_finished.md`): lowerer setup-type removed-method alias classification no longer treats rooted `/vector/count` as an explicit removed vector method alias, while `/array/count` and canonical `/std/collections/vector/count` remain covered.
  - progress: Completed child slice `TODO-0420` (archived in `docs/todo_finished.md`): semantics named-argument builtin validation no longer treats rooted `/vector/push|reserve|remove_at|remove_swap` spellings as statement-only builtin helpers, while canonical `/std/collections/vector/*` and `/array/*` named-argument mutator diagnostics remain covered.
  - progress: Completed child slice `TODO-0421` (archived in `docs/todo_finished.md`): lowerer setup-type return-kind resolution dropped the dead explicit vector access removed-alias lambda that still carried rooted `/vector/at*` branches, and focused helper coverage now locks rooted plus canonical explicit slash-method vector access rejection even when the receiver is a `vector<i32>(...)` constructor call.
  - progress: Completed child slice `TODO-0422` (archived in `docs/todo_finished.md`): lowerer setup-type receiver-target method lookup no longer lets rooted `/vector/count` slash-method paths win by same-path lookup on vector receivers, while `/array/count` compatibility and canonical `/std/collections/vector/count` lookup remain covered.
  - progress: Completed child slice `TODO-0423` (archived in `docs/todo_finished.md`): semantics method-target resolution and infer fallback no longer let explicit rooted `/vector/count` or `/vector/capacity` slash-method requests resolve on vector-family receivers, and focused semantics coverage now locks rooted slash-method rejection for wrapper-vector chains and method-call sugar auto inference.
  - progress: Completed child slice `TODO-0424` (archived in `docs/todo_finished.md`): semantics method-target resolution and infer fallback no longer let explicit rooted `/vector/at` or `/vector/at_unsafe` slash-method requests resolve on vector-family receivers, and focused semantics coverage now locks rooted access-alias rejection even when same-path alias helpers are declared.
  - progress: Completed child slice `TODO-0425` (archived in `docs/todo_finished.md`): semantics late unknown-target fallback no longer replays explicit rooted `/vector/at` or `/vector/at_unsafe` slash-method requests as bare access helpers, and focused semantics coverage now locks exact `unknown method: /vector/at*` diagnostics both with canonical-only helpers and with same-path alias helpers declared.
  - progress: Completed child slice `TODO-0426` (archived in `docs/todo_finished.md`): semantics late direct-collection fallback no longer lets unresolved explicit `/vector/at` or `/vector/at_unsafe` direct calls continue into downstream compatibility rewrites, and focused wrapper-vector coverage now locks exact `unknown call target: /vector/at` diagnostics while canonical direct `/std/collections/vector/at*` paths remain covered.
  - progress: Completed child slice `TODO-0427` (archived in `docs/todo_finished.md`): semantics collection-access builtin validation dropped its dead rooted `/vector/at*` exemption after earlier direct-call guards took ownership of those diagnostics, and focused direct-call field/tag coverage now locks exact rooted `unknown call target: /vector/at` results instead of generic fallback errors.
  - progress: Completed child slice `TODO-0428` (archived in `docs/todo_finished.md`): semantics named-argument builtin validation dropped its dead rooted `/vector/at*` carveout after earlier direct-call guards took ownership of those diagnostics, and focused named-argument coverage now locks exact rooted `unknown call target: /vector/at` rejection even when canonical `/std/collections/vector/at` helpers are declared.
  - progress: Completed child slice `TODO-0429` (archived in `docs/todo_finished.md`): semantics method-target resolution no longer lets rooted `./vector/count()` slash-methods fall back to receiver-native `count` methods on local `string`, `array`, or `map` values when no same-path `/vector/count` helper exists, and focused semantics coverage now locks exact receiver-specific `unknown method` diagnostics for those local receivers.
  - progress: Completed child slice `TODO-0430` (archived in `docs/todo_finished.md`): semantics method-target resolution no longer lets wrapper-call rooted `./vector/count()` slash-methods fall back to temporary receiver-native `count` methods on `string`, `array`, or `map` return values when no same-path `/vector/count` helper exists, and focused wrapper-temporary semantics coverage now locks exact receiver-specific `unknown method` diagnostics while allowing a receiver-compatible same-path array helper.
  - progress: Completed child slice `TODO-0431` (archived in `docs/todo_finished.md`): semantics method-target resolution no longer lets wrapper-call rooted `./vector/capacity()` slash-methods fall back to temporary receiver-native `capacity` methods on `string`, `array`, or `map` return values when no same-path `/vector/capacity` helper exists, and focused wrapper-temporary semantics coverage now locks exact receiver-specific `unknown method` diagnostics while allowing a receiver-compatible same-path array helper.
  - progress: Completed child slice `TODO-0432` (archived in `docs/todo_finished.md`): semantics direct-call count/capacity resolution no longer lets rooted `/vector/count(...)` treat a wrapper-returned builtin vector as an implicit builtin alias when no same-path `/vector/count` helper exists, and focused wrapper-temporary semantics coverage now locks exact rooted `unknown call target: /vector/count` rejection while allowing a receiver-compatible same-path vector helper.
  - progress: Completed child slice `TODO-0433` (archived in `docs/todo_finished.md`): semantics direct-call count resolution no longer lets rooted `/vector/count(...)` treat a local builtin vector binding as an implicit builtin alias when no same-path `/vector/count` helper exists, and focused semantics coverage now locks exact rooted `unknown call target: /vector/count` rejection while existing same-path helper coverage on builtin vector targets remains intact.
  - progress: Completed child slice `TODO-0434` (archived in `docs/todo_finished.md`): semantics direct-call count/capacity resolution no longer lets rooted `/vector/capacity(...)` treat local or wrapper-returned builtin vectors as implicit builtin aliases when no same-path `/vector/capacity` helper exists, and focused semantics coverage now locks exact rooted `unknown call target: /vector/capacity` rejection while same-path helper coverage remains intact on builtin vector targets.
  - progress: Completed child slice `TODO-0435` (archived in `docs/todo_finished.md`): direct auto-inference no longer treats rooted `/vector/count(...)` or `/vector/capacity(...)` calls on builtin vector targets as inferable builtin aliases when no same-path helper exists, and focused auto-inference semantics coverage now locks exact rooted `unknown call target` diagnostics while same-path helper inference remains covered.
  - progress: Completed child slice `TODO-0436` (archived in `docs/todo_finished.md`): direct auto-inference no longer lets rooted `/vector/count(...)` piggyback on receiver-native `count` for non-vector builtin targets when no same-path `/vector/count` helper exists, and focused auto-inference semantics coverage now locks exact rooted `unknown call target: /vector/count` diagnostics for map, string, and array receivers while same-path helper inference remains covered.
  - progress: Completed child slice `TODO-0437` (archived in `docs/todo_finished.md`): semantics direct-call count validation no longer lets rooted `/vector/count(...)` wrapper calls fall through to non-vector receiver-native `count` targets when no same-path `/vector/count` helper exists, and focused wrapper semantics coverage now locks exact rooted `unknown call target: /vector/count` diagnostics for map, string, and array wrapper receivers.
  - progress: Completed child slice `TODO-0438` (archived in `docs/todo_finished.md`): template monomorph experimental constructor rewriting no longer treats rooted `/vector` as equivalent to canonical `/std/collections/vector/vector`, and focused semantics plus source-lock coverage now keep canonical constructor-helper rewriting intact while rooted `/vector/vector(...)` stays on exact `unknown call target` diagnostics.
  - progress: Completed child slice `TODO-0439` (archived in `docs/todo_finished.md`): initializer graph-binding inference no longer treats rooted `/vector/vector(...)` spellings as collection-constructor bypasses, and focused semantics plus source-lock coverage now keep canonical stdlib auto-binding constructor cases intact while rooted alias auto bindings stay on exact `unknown call target: /vector/vector` diagnostics.
  - progress: Completed child slice `TODO-0440` (archived in `docs/todo_finished.md`): statement binding validation no longer treats canonical `/std/collections/vector/vector(...)` initializers as mismatches for explicit builtin `vector<T>` bindings, and focused semantics plus source-lock coverage now keep positional and named-argument explicit builtin-vector bindings on canonical stdlib constructor success while the removed special-case helper stays deleted.
  - progress: Completed child slice `TODO-0441` (archived in `docs/todo_finished.md`): build-call resolution no longer rewrites canonical `/std/collections/vector/vector(...)` calls onto legacy arity helpers like `/std/collections/vectorPair`, and focused semantics plus source-lock coverage now keep explicit canonical constructor helper definitions working while missing canonical constructors stay on exact `unknown call target: /std/collections/vector/vector` diagnostics even when arity helpers are present.
  - progress: Completed child slice `TODO-0442` (archived in `docs/todo_finished.md`): template monomorph type resolution no longer rewrites canonical `/std/collections/vector/vector(...)` calls onto legacy arity helpers like `/std/collections/vectorPair`, and focused type-resolution parity plus source-lock coverage now keep missing canonical constructors on exact `unknown call target: /std/collections/vector/vector` diagnostics even when vector arity helpers are defined in source.
  - progress: Completed child slice `TODO-0443` (archived in `docs/todo_finished.md`): build-initializer collection binding inference no longer force-classifies canonical `/std/collections/vector/vector(...)` calls as builtin `vector<T>` ahead of generic helper-return analysis, and focused semantics plus source-lock coverage now keep `[auto]` bindings on same-path templated vector-constructor helpers aligned with the helper’s actual return type.
  - progress: Completed child slice `TODO-0444` (archived in `docs/todo_finished.md`): collection-call inference no longer force-classifies canonical `/std/collections/vector/vector(...)` calls as builtin vector receivers/template sources, and focused semantics plus source-lock coverage now keep same-path constructor helpers on their actual non-vector method diagnostics instead of vector helper routing.
  - progress: Completed child slice `TODO-0445` (archived in `docs/todo_finished.md`): expression vector-helper method routing no longer force-classifies canonical `/std/collections/vector/vector<T>(...)` calls as vector-family receivers ahead of generic helper-return analysis, and focused semantics plus source-lock coverage now keep same-path templated constructor helpers on their actual non-vector method diagnostics instead of vector helper routing.
  - progress: Completed child slice `TODO-0446` (archived in `docs/todo_finished.md`): statement vector-helper receiver probing no longer force-classifies canonical `/std/collections/vector/vector<T>(...)` calls as vector-family receivers ahead of normal statement validation, and focused semantics plus source-lock coverage now keep explicit same-path `push` helpers working while bare `push(...)` on templated constructor helpers stays on `push requires vector binding` diagnostics instead of vector-helper routing.
  - progress: Completed child slice `TODO-0447` (archived in `docs/todo_finished.md`): expression fallback diagnostics no longer treat canonical `/std/collections/vector/vector(...)` calls as exempt “real vector ctors” inside `SemanticsValidatorExpr.cpp`, and focused semantics plus source-lock coverage now keep same-path templated constructor helpers on receiver-specific non-vector `capacity()` diagnostics instead of vector-specific fallback handling.
  - progress: Completed child slice `TODO-0448` (archived in `docs/todo_finished.md`): template-monomorph experimental constructor rewrites no longer redirect canonical `/std/collections/vector/vector(...)` calls onto `/std/collections/experimental_vector/vector`, and focused semantics plus source-lock coverage now keep canonical auto-binding constructor calls on their same-path helper return type even when an experimental constructor helper is also defined.
  - progress: Completed child slice `TODO-0449` (archived in `docs/todo_finished.md`): template-monomorph type resolution no longer carries a vector-specific canonical constructor fast-path in `rewriteCanonicalCollectionConstructorPath(...)`, and focused semantics plus source-lock coverage now keep canonical auto-binding constructor calls on the correct same-path overload family without that vector-only branch.
  - progress: Completed child slice `TODO-0450` (archived in `docs/todo_finished.md`): expression vector-helper method routing no longer force-classifies `/std/collections/experimental_vector/vector<T>(...)` calls as experimental-vector receivers ahead of generic helper-return analysis, and focused semantics plus source-lock coverage now keep same-path templated experimental constructor helpers on their actual non-vector method diagnostics instead of vector helper routing.
  - progress: Completed child slice `TODO-0451` (archived in `docs/todo_finished.md`): statement vector-helper receiver probing no longer force-classifies `/std/collections/experimental_vector/vector<T>(...)` calls as experimental-vector receivers ahead of normal statement validation, and focused semantics plus source-lock coverage now keep explicit same-path `push` helpers working while bare `push(...)` on templated experimental constructor helpers stays on `push requires vector binding` diagnostics.
  - progress: Completed child slice `TODO-0452` (archived in `docs/todo_finished.md`): collection-call inference no longer lets same-path canonical or experimental vector constructor helpers override an already-inferred non-collection return type, and focused semantics plus source-lock coverage now keep `.count()` on those helpers on receiver-specific non-vector diagnostics while native/C++ vector constructor conformance expectations were aligned with the current canonical helper surface.
  - progress: Completed child slice `TODO-0453` (archived in `docs/todo_finished.md`): template-monomorph type resolution no longer rewrites exact `import /std/collections/vector` bindings onto `/std/collections/vector/vector`; that slice intentionally removed the old monomorph fallback so any later exact-import support would need to come from explicit import-surface wiring rather than silent canonical-constructor rewrites.
  - progress: Completed child slice `TODO-0454` (archived in `docs/todo_finished.md`): expression method resolution no longer short-circuits canonical `./std/collections/vector/count(...)` slash-method calls on builtin vector receivers through a dedicated builtin-mismatch shim, and focused semantics plus source-lock coverage now keep same-path helper auto inference on the helper return type while surrounding return mismatches stay generic.
  - progress: Completed child slice `TODO-0455` (archived in `docs/todo_finished.md`): expression method resolution no longer treats canonical `./std/collections/vector/capacity(...)` slash-method calls on builtin vector receivers as a missing-helper special case, and focused semantics plus source-lock coverage now keep same-path helper auto inference on the helper return type while surrounding return mismatches stay generic.
  - progress: Completed child slice `TODO-0456` (archived in `docs/todo_finished.md`): method-target resolution no longer promotes canonical `./std/collections/vector/count()` and `./std/collections/vector/capacity()` slash-methods on builtin vector receivers to builtin targets before same-path resolution, and focused semantics plus source-lock coverage now keep rooted `/vector/*` helpers from satisfying canonical slash-method calls while leaving canonical access helper routing intact.
  - progress: Completed child slice `TODO-0457` (archived in `docs/todo_finished.md`): method-target resolution no longer promotes canonical `./std/collections/vector/at()` and `./std/collections/vector/at_unsafe()` slash-methods on builtin vector receivers to builtin targets before same-path resolution, and focused semantics plus source-lock coverage now keep same-path helper inference on the helper return type while preserving the separate imported-helper fallback for unresolved canonical access routing.
  - progress: Completed child slice `TODO-0458` (archived in `docs/todo_finished.md`): method-target resolution no longer treats explicit canonical slash-method `./std/collections/vector/at()` and `./std/collections/vector/at_unsafe()` requests as builtin imports when only the imported stdlib helper is available, and focused semantics plus source-lock coverage now keep those explicit method forms on imported-helper validation while bare imported access routing remains intact.
  - progress: Completed child slice `TODO-0459` (archived in `docs/todo_finished.md`): method-target resolution no longer treats bare imported `values.at(...)` and `values.at_unsafe(...)` vector access methods as builtin fallbacks for `/std/collections/vector/at*`, and focused semantics plus source-lock coverage now keep imported vector method-call access on ordinary helper validation for both local and wrapper vector receivers.
  - progress: Completed child slice `TODO-0460` (archived in `docs/todo_finished.md`): method-target resolution no longer lets wrapper-returned non-vector receivers satisfy rooted `./vector/count()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep wrapper `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when `/vector/count(...)` helpers are declared.
  - progress: Completed child slice `TODO-0461` (archived in `docs/todo_finished.md`): method-target resolution no longer lets wrapper-returned non-vector receivers satisfy rooted `./vector/capacity()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep wrapper `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when `/vector/capacity(...)` helpers are declared.
  - progress: Completed child slice `TODO-0462` (archived in `docs/todo_finished.md`): method-target resolution no longer lets local non-vector receivers satisfy rooted `./vector/count()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep local `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when `/vector/count(...)` helpers are declared.
  - progress: Completed child slice `TODO-0463` (archived in `docs/todo_finished.md`): method-target resolution no longer lets local non-vector receivers satisfy canonical `./std/collections/vector/capacity()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep local `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when canonical `/std/collections/vector/capacity(...)` helpers are declared.
  - progress: Completed child slice `TODO-0464` (archived in `docs/todo_finished.md`): method-target resolution no longer lets local non-vector receivers satisfy canonical `./std/collections/vector/count()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep local `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when canonical `/std/collections/vector/count(...)` helpers are declared.
  - progress: Completed child slice `TODO-0465` (archived in `docs/todo_finished.md`): method-target resolution no longer lets wrapper-returned `map` receivers satisfy canonical `./std/collections/vector/count()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep wrapper `map` receivers on `unknown method: /map/count` even when canonical `/std/collections/vector/count(...)` helpers are declared.
  - progress: Completed child slice `TODO-0466` (archived in `docs/todo_finished.md`): method-target resolution no longer lets wrapper-returned non-vector receivers satisfy canonical `./std/collections/vector/capacity()` slash-methods through same-path helper routing, and focused semantics plus C++ emitter source-lock coverage now keep wrapper `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when canonical `/std/collections/vector/capacity(...)` helpers are declared.
  - progress: Completed child slice `TODO-0467` (archived in `docs/todo_finished.md`): method-target resolution no longer lets typed non-vector receivers satisfy canonical `./std/collections/vector/capacity()` slash-methods through the remaining typed-receiver helper-routing fallback, and focused source-lock coverage now keeps the old `count|capacity` typed branch deleted while existing semantics plus C++ emitter coverage continue to hold local and wrapper `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics.
  - progress: Completed child slice `TODO-0468` (archived in `docs/todo_finished.md`): method-target resolution no longer lets typed non-vector receivers satisfy canonical `./std/collections/vector/count()` slash-methods through the last typed-receiver helper-routing fallback, and focused semantics plus C++ emitter source-lock coverage now keep local and wrapper `string`/`map`/`array` receivers on receiver-specific `unknown method` diagnostics even when canonical `/std/collections/vector/count(...)` helpers are declared.
  - progress: Completed child slice `TODO-0469` (archived in `docs/todo_finished.md`): canonical `./std/collections/vector/count()` slash-methods on `string`/`map`/`array` receivers no longer get forced onto receiver-family `unknown method` diagnostics when a same-path helper exists, and focused semantics plus C++/native backend source-lock coverage now keep declared canonical helpers runnable while missing helpers fail on the canonical `/std/collections/vector/count` path.
  - progress: Completed child slice `TODO-0470` (archived in `docs/todo_finished.md`): method-target resolution no longer uses the leftover canonical `/std/collections/vector/count` call-receiver shim for chained builtin-vector direct calls, and focused semantics plus source-lock coverage now keep wrapper-vector `/std/collections/vector/count(...).tag()` on exact `unknown call target` rejection without regressing same-path helper chaining.
  - progress: Completed child slice `TODO-0471` (archived in `docs/todo_finished.md`): method-target resolution no longer uses the leftover canonical `/std/collections/vector/capacity` call-receiver shim for chained builtin-vector direct calls, and focused semantics plus source-lock coverage now keep wrapper-vector `/std/collections/vector/capacity(...).tag()` on exact `unknown call target` rejection without regressing same-path helper chaining.
  - progress: Completed child slice `TODO-0472` (archived in `docs/todo_finished.md`): canonical `/std/collections/vector/count` method sugar for `string`/`array`/`map` receivers now routes through the ordinary `count` receiver cases instead of a dedicated pre-resolution receiver-family branch, while focused semantics coverage keeps same-path helper success and wrong-family helper rejection on the canonical `unknown method` diagnostic.
  - progress: Completed child slice `TODO-0473` (archived in `docs/todo_finished.md`): rooted `./vector/count()` on builtin vector-family receivers no longer gets blocked by the old shared rooted-vector receiver gate, and focused semantics plus source-lock coverage now keep a real `/vector/count([vector<i32>] ...)` helper runnable while canonical-only fallback stays on exact `unknown method: /vector/count`.
  - progress: Completed child slice `TODO-0474` (archived in `docs/todo_finished.md`): rooted `./vector/capacity()` on builtin vector-family receivers no longer gets blocked by the old shared rooted-vector receiver gate, and focused semantics plus source-lock coverage now keep a real `/vector/capacity([vector<i32>] ...)` helper runnable while canonical-only fallback stays on exact `unknown method: /vector/capacity`.
  - progress: Completed child slice `TODO-0478` (archived in `docs/todo_finished.md`): late call compatibility no longer short-circuits imported canonical `/std/collections/vector/count` and `/std/collections/vector/capacity` direct calls before the dedicated count/capacity builtin validator runs, focused exact-import semantics coverage now includes canonical `capacity(...)`, and source-lock coverage asserts that the deleted imported-vector fast-path branches stay absent.
  - progress: Completed child slice `TODO-0479` (archived in `docs/todo_finished.md`): direct count/capacity inference no longer carries the dead rooted-vector builtin-alias rejection lambda in `SemanticsValidatorInferCollectionDirectCountCapacity.cpp`, and focused infer source-lock coverage now asserts that this leftover `/vector/*` guard stays deleted while the remaining rooted non-vector rejection path remains explicit.
  - progress: Completed child slice `TODO-0480` (archived in `docs/todo_finished.md`): method-target resolution no longer hard-blocks rooted `./vector/at_unsafe()` on builtin vector-family receivers when a receiver-compatible same-path `/vector/at_unsafe(...)` helper exists, focused semantics coverage now flips that alias-helper case from rejection to success, C++ emitter struct-forwarding now runs through the same-path helper, and source-lock coverage asserts that the old catch-all rooted `at_unsafe` gate is replaced by an explicit same-path branch.
  - progress: Completed child slice `TODO-0481` (archived in `docs/todo_finished.md`): infer method resolution no longer treats rooted `vector/at_unsafe` as part of the builtin vector-family rejection gate, focused semantics coverage now locks both positive and negative `[auto]` inference on `values./vector/at_unsafe(...)`, and infer source-lock coverage asserts that the old `normalizedMethodPath == "vector/at_unsafe"` gate stays absent.
  - progress: Completed child slice `TODO-0482` (archived in `docs/todo_finished.md`): direct count/capacity inference no longer carries the infer-only rooted `/vector/count` non-vector rejection lambda in `SemanticsValidatorInferCollectionDirectCountCapacity.cpp`, and focused infer source-lock coverage now asserts that this leftover rooted-count shim stays deleted so direct-call rejection remains owned by the normal validator path.
  - progress: Completed child slice `TODO-0483` (archived in `docs/todo_finished.md`): direct count/capacity validation no longer carries the shared receiver-type-specific rooted `/vector/count|capacity` rejection lambda in `SemanticsValidatorExprCollectionCountCapacity.cpp`, and focused expression source-lock coverage now asserts that the old call-only shim is gone while the remaining rooted `/vector/count` and `/vector/capacity` path-level rejections stay explicit.
  - progress: Completed child slice `TODO-0484` (archived in `docs/todo_finished.md`): direct count/capacity validation no longer carries separate explicit rooted `/vector/count` and `/vector/capacity` path-level rejection blocks in `SemanticsValidatorExprCollectionCountCapacity.cpp`, and focused expression source-lock coverage now asserts that those duplicated helper-specific branches are replaced by one shared rooted direct-call guard.
  - progress: Completed child slice `TODO-0485` (archived in `docs/todo_finished.md`): infer method resolution no longer treats rooted `vector/count`, `vector/capacity`, or `vector/at` as part of the builtin vector-family rejection gate in `SemanticsValidatorInferMethodResolution.cpp`, focused semantics coverage now locks both positive and negative `[auto]` inference on `values./vector/count()`, `values./vector/capacity()`, and `values./vector/at(...)`, and infer source-lock coverage asserts that the old rooted infer gate stays absent.
  - progress: Completed child slice `TODO-0486` (archived in `docs/todo_finished.md`): expression method resolution no longer carries the leftover `explicitVectorCountMethodMissingSamePathHelper` shim for canonical `./std/collections/vector/count()` calls on builtin vector receivers in `SemanticsValidatorExprMethodResolution.cpp`, and focused expression source-lock coverage now asserts that this count-specific canonical same-path helper guard stays absent.
  - progress: Completed child slice `TODO-0487` (archived in `docs/todo_finished.md`): method-target resolution now routes rooted `/vector/count`, `/vector/capacity`, `/vector/at`, and `/vector/at_unsafe` receiver-family handling through one shared rooted-helper branch in `SemanticsValidatorExprMethodTargetResolution.cpp`, and focused expression source-lock coverage now asserts that the old per-helper vector-family branches stay deleted.
  - progress: Completed child slice `TODO-0488` (archived in `docs/todo_finished.md`): direct count/capacity validation now routes rooted removed-alias call detection through shared `getRemovedRootedVectorDirectCallPath(...)` compatibility logic from `SemanticsValidatorInferCollectionCompatibility.cpp` instead of a file-local lambda in `SemanticsValidatorExprCollectionCountCapacity.cpp`, and focused expression source-lock coverage now asserts that the local rooted direct-call shim stays deleted while the shared helper call remains in use.
  - progress: Completed child slice `TODO-0489` (archived in `docs/todo_finished.md`): direct access fallback now routes rooted `/vector/at` and `/vector/at_unsafe` removed-alias call detection through the shared `getRemovedRootedVectorDirectCallPath(...)` compatibility helper in `SemanticsValidatorInferCollectionCompatibility.cpp` instead of a file-local explicit-path branch in `SemanticsValidatorExprDirectCollectionFallbacks.cpp`, and focused expression source-lock coverage now asserts that the local rooted direct-call branch stays deleted while the shared helper call remains in use.
  - progress: Completed child slice `TODO-0490` (archived in `docs/todo_finished.md`): direct count/capacity validation and direct access fallback now share `getRemovedRootedVectorDirectCallDiagnostic(...)` from `SemanticsValidatorInferCollectionCompatibility.cpp` instead of assembling final rooted `/vector/*` `unknown call target` diagnostics at each call site, and focused expression source-lock coverage now asserts that both validators use the shared diagnostic helper while the old per-file rooted string-comparison branches stay deleted.
  - progress: Completed child slice `TODO-0491` (archived in `docs/todo_finished.md`): rooted direct-call removed-alias diagnostics now flow through one unfiltered `getRemovedRootedVectorDirectCallDiagnostic(expr)` helper in `SemanticsValidatorInferCollectionCompatibility.cpp` instead of passing helper-name filters from each validator, and focused source-lock coverage now asserts that both direct-call validators use the no-argument helper while the deleted per-caller helper-name filtering stays absent.
  - progress: Completed child slice `TODO-0492` (archived in `docs/todo_finished.md`): method-compatibility setup no longer duplicates rooted `/vector/*` direct-call rejection through `getDirectVectorHelperCompatibilityPath(expr)` in `SemanticsValidatorExprMethodCompatibilitySetup.cpp`, leaving `SemanticsValidatorExprVectorHelpers.cpp` as the direct-call owner for that removed-alias diagnostic path, and focused source-lock coverage now asserts that the setup fragment keeps the old vector compatibility branch deleted.
  - progress: Completed child slice `TODO-0475` (archived in `docs/todo_finished.md`): rooted `./vector/at()` on builtin vector-family receivers no longer gets blocked by the remaining shared rooted-vector receiver gate, and focused semantics plus source-lock coverage now keep a real `/vector/at([vector<i32>] values, [i32] index)` helper runnable while canonical-only fallback stays on exact `unknown method: /vector/at`.

- [ ] TODO-0406: Split production semantics APIs from testing snapshots
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 15
  - depends_on: TODO-0405
  - scope: Move snapshot-for-testing surface area out of production semantics interfaces and into explicit `include/primec/testing/*` facades so runtime/compiler consumers only depend on production contracts.
  - acceptance:
    - Production-facing semantics headers stop exposing snapshot-only structs/methods used exclusively by tests.
    - Existing semantics/IR snapshot tests compile and pass through testing facades without adding new `tests -> src` include-layer allowlist entries.
    - At least one snapshot-only compatibility path in production semantics code is deleted rather than relocated.
  - stop_rule: If removal breaks unrelated runtime/backend callsites, split into two leaves (facade introduction first, production API pruning second) before continuing.

- [~] TODO-0405: Introduce unified semantic-product index builder
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 15
  - depends_on: TODO-0404
  - scope: Replace ad-hoc lowerer semantic-product lookup-map construction with one typed `SemanticProductIndex` builder and shared lookup API for direct/method/bridge/binding/local-auto/query/try/on_error families.
  - file_todos:
    - [x] `TODO-0405-F01` files: `/Users/chrgre01/src/PrimeStruct/src/ir_lowerer/IrLowererSemanticProductTargetAdapters.{h,cpp}`, `/Users/chrgre01/src/PrimeStruct/include/primec/testing/ir_lowerer_helpers/IrLowererSemanticProductTargetAdapters.h`; fix: introduce a typed `SemanticProductIndex` plus `SemanticProductIndexBuilder`, wire adapter construction through that builder for direct/method/bridge/binding/local-auto/query/try/on_error family indices, and route semantic lookup helpers through shared index-backed lookup paths.
    - [x] `TODO-0405-F02` files: `/Users/chrgre01/src/PrimeStruct/src/ir_lowerer/IrLowererSemanticProductTargetAdapters.cpp`, `/Users/chrgre01/src/PrimeStruct/tests/unit/test_ir_pipeline_validation_ir_lowerer_call_helpers_source_delegation_stays_stable.cpp`, `/Users/chrgre01/src/PrimeStruct/tests/unit/test_ir_pipeline_backends_graph_contexts.h`; fix: remove transitional duplicated legacy adapter-map population/fallback paths and migrate downstream checks to consume `SemanticProductIndex` directly.
    - [x] `TODO-0405-F03` files: `/Users/chrgre01/src/PrimeStruct/tests/unit/test_ir_pipeline_backends_registry.cpp`, `/Users/chrgre01/src/PrimeStruct/tests/unit/test_semantics_definition_partitioner.cpp`; fix: add/refresh deterministic parity coverage across worker counts (`1`, `2`, `4`) for the unified index path.
  - acceptance:
    - Lowerer semantic-product adapter paths consume one shared index builder instead of duplicating per-family map assembly logic.
    - Determinism parity coverage keeps semantic-product output and diagnostics stable across worker counts (`1`, `2`, `4`) on existing stress fixtures.
    - Semantic memory benchmark reports show non-regression for the semantic-product/lowering path, with measurable allocation or runtime improvement in at least one tracked fixture.
  - stop_rule: If measurable benchmark impact is not achieved after two attempts, archive this leaf as low-value per rule 16 and replace it with a different Group 15 hotspot.

- [ ] TODO-0403: Keep queue, lanes, and snapshots synchronized
  - owner: ai
  - created_at: 2026-04-13
  - phase: Cross-cutting
  - depends_on: TODO-0401, TODO-0402
  - scope: Keep this TODO log internally consistent as Group 14/15 leaf tasks are added, split, completed, and archived.
  - acceptance:
    - Every TODO ID listed in `Ready Now`, `Immediate Next 10`, `Priority Lanes`, and `Execution Queue` has a matching task block.
    - Archived IDs are removed from open sections in the same change that moves their task blocks to `docs/todo_finished.md`.
    - Coverage snapshot tables reflect only currently open IDs.
  - stop_rule: If synchronization touches more than one logical workstream, split updates into focused queue-only follow-up tasks.

- [ ] TODO-0402: Group 15 completion tracker (semantic memory + multithread substrate)
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 15
  - scope: Define and execute remaining measurable leaf slices after archived P0/P1/P2 work to ship memory/runtime wins with strict regression control.
  - acceptance:
    - Remaining Group 15 leaf tasks are listed with stable IDs and explicit metric baselines/targets.
    - Each new leaf includes concrete validation commands, including release build/test and focused benchmark or memory checks.
    - Completed Group 15 leaves are moved to `docs/todo_finished.md` with evidence notes.
  - stop_rule: If a leaf cannot demonstrate measurable impact within one code-affecting commit, split it into smaller measurable leaves before implementation.
  - notes: Prior Group 15 slices `[P0-17]` through `[P0-28]`, `[P1-01]` through `[P1-03]`, and `[P2-14]` through `[P2-42]` are already archived in `docs/todo_finished.md` (April 12-13, 2026).

- [ ] TODO-0401: Group 14 completion tracker (SoA end-state cleanup)
  - owner: ai
  - created_at: 2026-04-13
  - phase: Group 14
  - scope: Track and close Group 14 child leaves required to finish stdlib-authoritative collection behavior and remove compatibility scaffolding; implementation work lives in child TODOs.
  - acceptance:
    - Active Group 14 implementation leaves (currently TODO-0408 and TODO-0409) stay explicit with bounded scope, dependencies, and verification steps.
    - This tracker is only completed after all Group 14 child leaves are archived in `docs/todo_finished.md` with evidence notes.
    - At least one leaf is always `Ready Now` with no unmet TODO dependencies.
  - stop_rule: If a leaf is too large for one commit plus focused conformance updates, split it before implementation.
  - notes: Current active Group 14 child leaves are TODO-0408 and TODO-0409. Prior Group 14 slices `[S2-01]` through `[S2-05]`, `[S3-01]` through `[S3-132]`, and `[S4-01a1]` through `[S4-03]` are already archived in `docs/todo_finished.md` (April 12-13, 2026).
