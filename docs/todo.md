# PrimeStruct Spec ↔ Code TODO

Legend:
  ○ Not started
  ◐ Started
  ✓ Finished

Finished items are periodically moved to `docs/todo_finished.md`.

**Types & Semantics**
Collection strategy note: keep `array` as a language-core collection, but migrate `vector` and `map` to stdlib `.prime` implementations so users can build equivalent containers. Develop the language/runtime substrate far enough that `soa_vector` can follow the same end-state rather than remaining a permanent compiler-owned collection. Progress is tracked by the slices below plus the archived finished slice bullets in `docs/todo_finished.md`.
Vector/map de-builtinization roadmap note: the target state is that, aside from minimal runtime/memory substrate, C++ compiler/backend code does not know collection semantics for `vector` or `map`; that behavior should live in stdlib `.prime` files.
SoA de-builtinization roadmap note: the target state for `soa_vector` is also a stdlib `.prime` implementation. Any temporary compiler/runtime support should be reduced to generic substrate only: field-layout/codegen/introspection, column storage, field-view borrowing/invalidation, and allocation primitives that do not mention `soa_vector` in C++ source. The public `/std/collections/soa_vector/*` behavior should eventually live entirely in `.prime`.
Type ownership note: the canonical `core` vs `hybrid` vs `stdlib-owned` matrix now lives in `docs/PrimeStruct.md` under "Type Ownership Model". Keep roadmap slices aligned to that table: fixed-width scalars, `string`, `array<T>`, `Pointer<T>`, and `Reference<T>` are core substrate; `Result<T, Error>`, `File<Mode>`, `Buffer<T>`, and `/std/gfx/*` are hybrid surfaces; `Maybe<T>`, `vector<T>`, `map<K, V>`, and target-state `soa_vector<T>` are stdlib-owned public surfaces.
Execution order note: use this sequence and resist bridge churn outside it.
1. Finish only the runtime/ownership substrate that self-contained `.prime` containers genuinely need.
2. Keep landing real `.prime` container behavior under the experimental namespaces until VM/native/C++ parity is good enough to trust the implementations.
3. Replace wrapper-only canonical `/std/collections/vector/*` and `/std/collections/map/*` entry points with those real `.prime` implementations.
4. Delete compiler-owned compatibility handling and helper routing from semantics first, then IR lowering, then emitters/backends.
5. Leave only allocation/error/drop substrate in C++.
SoA rollout note: follow the same end-state as `vector`/`map`, but use a tighter vertical-slice sequence. First lock the `soa_vector` contract and invalidation rules. Then land only the minimum substrate needed for one real `.prime` implementation under an experimental namespace. Let that experimental implementation drive the next substrate slices, then route canonical `/std/collections/soa_vector/*` names to it, and finally delete all `soa_vector`-named compiler/backend/runtime paths.
Map-first rollout note: `map` should be deleted from compiler-owned code first because experimental `map` already has a real `.prime` storage/runtime implementation; `vector` should follow once the remaining ownership/drop substrate is in place.
Map replacement note: the next serious `map` milestone is constructor/type-surface migration from builtin `map<K, V>` onto the real experimental `.prime` `Map<K, V>` implementation. Do not add more helper-routing micro-slices unless they directly unblock that migration or deletion of a concrete compiler-owned path.
Deletion gating note: once a direct compatibility/fallback cleanup slice is stabilized, prefer the next real canonical `.prime` replacement or compiler-path deletion milestone over more narrow bridge cleanup. Do not keep peeling compatibility sub-slices when the next real de-builtinization step is ready.
Stdlib container substrate note: the items below are the hard prerequisites for moving `vector`/`map` into self-contained `.prime` implementations instead of extending compiler-owned collection behavior. Keep them only when they describe substrate the `.prime` containers genuinely need. Apply the same rule to `soa_vector`: do not add more compiler-owned SoA behavior unless it directly unblocks the minimum substrate, experimental `.prime` bring-up, or deletion of an existing compiler-owned path.
Hybrid-surface note: do not treat `Result`, `File`, `Buffer`, or `/std/gfx/*` follow-up work as candidates for full de-builtinization in the same sense as `vector`/`map`; the target there is a smaller builtin substrate with stdlib-owned public surface, not zero compiler/runtime knowledge.
Arg-pack status note: completed parser/body-API work and the finished IR element-kind slices are archived in `docs/todo_finished.md`. Keep only the started runtime-materialization slices and remaining gaps here.
- ✓ Publish and maintain a single doc-visible type ownership matrix (`core` vs `hybrid` vs `stdlib-owned`) across the design docs and TODO roadmap so future de-builtinization slices delete compiler special cases instead of introducing more path-name routing. Completed: `docs/PrimeStruct.md` now carries the canonical matrix and this roadmap note points back to it.
- ✓ Remove the remaining `Maybe(value)` name-based constructor rewrite so `Maybe<T>` is now documented and validated as an explicit stdlib-owned optional surface over `Maybe<T>()`, `some<T>(value)`, and `none<T>()`.
- ◐ Refactor `Result<T, Error>` toward the documented hybrid end-state: keep `?` propagation/minimal layout builtin, but move more constructors/helpers and error-domain behavior into stdlib `.prime`. Progress: `/std/file/*` now provides `.prime`-authored `fileReadEof()`, `fileErrorStatus(err)`, `fileErrorIsEof(err)`, and `fileErrorResult<T>(err)` helpers so FileError-domain result construction no longer needs ad hoc raw packing at call sites and stdlib callers such as `/std/image/*` no longer need hard-coded EOF literals, the public `/FileError/why([FileError] err)` wrapper now lives in stdlib `.prime` while intrinsic file-error string mapping remains the low-level substrate behind `Result.why(...)` / `err.why()`, `/std/image/*` now provides `.prime`-authored `imageReadUnsupported()`, `imageWriteUnsupported()`, `imageInvalidOperation()`, `imageErrorStatus(err)`, and `imageErrorResult<T>(err)` helpers so `ImageError` result construction no longer needs raw code packing at call sites, and the public `/ImageError/why([ImageError] err)` wrapper now lives in stdlib `.prime` for explicit type-owned access to the current image error strings, `/std/collections/*` now provides `.prime`-authored `containerErrorStatus(err)` alongside `containerErrorResult<T>(err)` so status-only `ContainerError` results no longer need manual `err.code` packing at call sites and now also exposes a public `/ContainerError/why([ContainerError] err)` wrapper for explicit type-owned access to the stable container error strings, and both `/std/gfx/*` plus `/std/gfx/experimental/*` now provide `.prime`-authored `gfxErrorStatus(err)` alongside `gfxErrorResult<T>(err)` so status-only `GfxError` results no longer need manual `err.code` packing inside graphics helpers.
- ◐ Add a stdlib-authored `File<Mode>` facade over the intrinsic file substrate so the public file helper surface is library-owned even though host I/O remains builtin/effect-gated. Progress: `/std/file/*` now exposes `.prime`-authored `/File/read_byte(...)`, zero-to-five-value heterogenous `/File/write(...)` and `/File/write_line(...)` overload families, `/File/write_byte(...)`, `/File/write_bytes(...)`, `/File/flush(...)`, and `/File/close<Mode>(...)` wrappers, and imported method/free-call sugar now prefers those stdlib helpers over direct builtin `/file/*` routing for `read_byte`, `write`, `write_line`, `write_byte`, `write_bytes`, `flush`, and `close` while `File<Mode>(path)` and broader multi-value `write(...)` / `write_line(...)` arities remain builtin substrate.
- ✓ Extend the stdlib-authored `File<Mode>` facade to explicit single-value numeric text helpers (`/File/write<Mode, T>(value)` / `/File/write_line<Mode, T>(value)`) so imported `/std/file/*` covers a minimal `.prime`-authored text-write slice without reintroducing compiler-owned routing.
- ✓ Extend the stdlib-authored `File<Mode>` facade from explicit single-value numeric text helpers to single-value string writes once file-write lowering can carry string-valued helper parameters without falling back to builtin-only entry points.
- ✓ Split the remaining multi-value `File<Mode>` facade work into an explicit prerequisite before changing the public helper surface: helper families may now reuse the same public path when their exact parameter counts differ, and template monomorphization rewrites those overloads onto stable internal paths before later validation so stdlib-authored `write(...)` / `write_line(...)` can grow past single-value wrappers without reintroducing compiler-owned routing.
- ✓ After that prerequisite lands, extend the stdlib-authored `File<Mode>` facade from single-value helpers to a zero-to-five-value heterogenous `write(...)` and `write_line(...)` overload family without reintroducing compiler-owned routing. Completed: `/std/file/*` now exposes multi-value `.prime` wrappers for both slash-call and imported method syntax across the common zero-to-five-value arities while still lowering through the intrinsic file builtin path.
- ○ Extend the stdlib-authored `File<Mode>` facade beyond the current zero-to-five-value heterogenous `write(...)` and `write_line(...)` overload family only if broader arities still prove worthwhile after the common imported call sites stop depending on builtin-only routing.
- ✓ Extend the stdlib-authored `File<Mode>` facade to explicit slash-call `/File/close<Mode>(...)` once helper lowering can safely disarm the original handle binding instead of only a copied receiver.
- ✓ Route imported `file.close()` receiver syntax through the stdlib-authored `/File/close<Mode>(...)` helper once method-call helper selection can bind that templated wrapper without falling back to builtin-only routing.
- ◐ Add a stdlib-authored GPU resource facade for `Buffer<T>` and `/std/gfx/*` over the intrinsic dispatch/device substrate so graphics/public buffer APIs are primarily documented and implemented in `.prime`. Progress: canonical and experimental gfx packages now expose `.prime`-authored `Buffer<T>.count()`, `Buffer<T>.empty()`, `Buffer<T>.is_valid()`, `Buffer<T>.readback()`, and compute-only `Buffer<T>.load(...)` / `Buffer<T>.store(...)` helpers plus the preferred imported constructor-shaped `Buffer<T>(count)` allocation surface and explicit slash-call `Buffer/allocate<T>(count)` / `Buffer/upload(...)` / `Buffer/load(...)` / `Buffer/store(...)` helpers so public buffer inspection, host-side allocation/readback/upload, and compute storage access no longer need direct raw fields or builtin `buffer(...)` / `readback(...)` / `upload(...)` / `buffer_load(...)` / `buffer_store(...)` routing at call sites.
- ✓ Extend the stdlib-authored `Buffer<T>` facade from slash-call `Buffer/allocate<T>(count)` to the final preferred constructor-shaped allocation surface once canonical and experimental gfx packages settle whether that should be `Buffer<T>(count)` or another type-owned form without reintroducing compiler-owned public routing. Completed: imported canonical and experimental `Buffer<T>(count)` expressions now rewrite onto the matching stdlib allocation helpers while labeled raw `Buffer<T>([token] ..., [elementCount] ...)` construction stays direct struct initialization.
- ✓ Extend the stdlib-authored `Buffer<T>` facade to compute-only storage access helpers once generic `.prime` wrappers can express `buffer_load(...)` / `buffer_store(...)` without falling back to compiler-owned public entry points.
- ◐ Variadic arg-pack backend/runtime materialization slice: land backend/runtime support for concrete `[args<T>]` parameters plus their read-only `count`/`at`/indexing body API incrementally so stdlib `.prime` variadic helpers can execute without compiler-owned vector/map constructor ladders. Progress: the legacy C++ emitter now materializes trailing `[args<T>]` parameters as `std::vector<T>` values, supports direct variadic calls plus mixed explicit-prefix + final-spread forwarding, and executes `count`/`at`/`at_unsafe`/indexing body access on those packs including downstream method resolution on indexed values.
- ◐ Variadic arg-pack IR-backed materialization slice: lower concrete `[args<T>]` parameters plus their read-only `count`/`at`/indexing body API through IR/VM/native incrementally so stdlib `.prime` variadic helpers execute on the primary IR-backed backends as well. Progress: IR-backed lowering now materializes trailing numeric/bool/string `[args<T>]` parameters as array-like locals for direct positional calls, pure `[spread] values` forwarding of an existing pack reuses that local representation, mixed explicit-prefix + final-spread forwarding now rebuilds numeric/bool/string packs from known-size sources, and VM/native coverage exercises `count` plus checked indexing on those packs including downstream string method resolution. Struct packs now cover direct calls plus pure/mixed spread forwarding for `count`, checked `at`, and `[]`/method-style indexed access including downstream field/helper resolution. `Result<T, Error>` packs preserve indexed `Result.why(...)` / `?` behavior across the same forwarding modes, status-only `Result<Error>` packs preserve indexed `Result.error(...)` / `Result.why(...)` behavior across those same forwarding modes, `FileError` packs preserve indexed downstream `why()` mapping across direct/pure/mixed forwarding, `Reference<FileError>` packs preserve indexed downstream `dereference(...).why()` mapping across those same forwarding modes, `Pointer<FileError>` packs preserve indexed downstream `dereference(...).why()` mapping across those same forwarding modes, `Reference<Result<T, Error>>` packs preserve indexed downstream `dereference(...)`, `try(...)`, and `Result.why(...)` access across those same forwarding modes, status-only `Reference<Result<Error>>` packs preserve indexed downstream `dereference(...)`, `Result.error(...)`, and `Result.why(...)` access across those same forwarding modes, `Pointer<Result<T, Error>>` packs preserve indexed downstream `dereference(...)`, `try(...)`, and `Result.why(...)` access across those same forwarding modes, and status-only `Pointer<Result<Error>>` packs preserve indexed downstream `dereference(...)`, `Result.error(...)`, and `Result.why(...)` access across those same forwarding modes. `File<Mode>` packs preserve indexed downstream file-handle access across those same forwarding modes, `Reference<File<Mode>>` packs preserve indexed downstream file-handle access alongside explicit `dereference(...)` receiver forms across those same forwarding modes plus helper-style `at(values, i).write*()` / `values.at(i).flush()` receivers, canonical free-builtin `at([values] values, [index] i).write*()` / `.flush()` receivers, and direct indexed `read_byte(...)` `?` inference plus canonical free-builtin `at([values] values, [index] i).read_byte(...)` `?`, `Pointer<File<Mode>>` packs preserve indexed downstream file-handle access alongside explicit `dereference(...)` receiver forms across those same forwarding modes plus helper-style `at(values, i).write*()` / `values.at(i).flush()` receivers, canonical free-builtin `at([values] values, [index] i).write*()` / `.flush()` receivers, and direct indexed `read_byte(...)` `?` inference plus canonical free-builtin `at([values] values, [index] i).read_byte(...)` `?`, `Buffer<T>` packs preserve indexed downstream `buffer_load(...)` and `buffer_store(...)` on the IR/VM GPU path across direct/pure/mixed forwarding, `array<T>`, `Reference<array<T>>`, `Pointer<array<T>>`, `vector<T>`, `Reference<vector<T>>`, `Pointer<vector<T>>`, `soa_vector<T>` (for the current empty/header-only runtime shape), `Reference<soa_vector<T>>`, `Pointer<soa_vector<T>>`, `map<K, V>`, `Reference<map<K, V>>`, and `Pointer<map<K, V>>` packs now preserve indexed downstream `count()` resolution across direct/pure/mixed forwarding, and those same empty/header-only value, borrowed, and pointer `soa_vector` packs also preserve direct indexed `[]` / `at()` / `at_unsafe()` body access feeding that same downstream `count()` surface across those forwarding modes. Those same map value, borrowed, and pointer packs preserve indexed downstream `contains()` / `at()` / `at_unsafe()` lookup access across those same forwarding modes, borrowed/pointer map packs also preserve that same lookup and count surface through explicit indexed `dereference(...)` receiver wrappers, and those same value, borrowed, and pointer map packs also preserve indexed downstream `tryAt(...)` payload-kind inference for `auto` bindings. Borrowed/pointer array and vector packs now preserve direct indexed checked/unchecked access alongside explicit indexed `dereference(...)` receiver wrappers, `vector<T>` packs also preserve indexed downstream statement-mutator access, and borrowed/pointer vector packs preserve helper-style `capacity(at(values, i))`, `values.at(i).capacity()`, plus explicit indexed `dereference(...)` receiver wrappers across those same forwarding modes. Scalar `Pointer<T>` plus scalar `Reference<T>` packs preserve indexed downstream `dereference(...)` across those same forwarding modes, and struct `Pointer<T>` plus struct `Reference<T>` packs now preserve indexed downstream field/helper access across direct/pure/mixed forwarding. Remaining unsupported non-string element kinds remain follow-up work.
  Latest checkpoint: canonical free-builtin `at([values] values, [index] i)` on wrapped borrowed/pointer `File<Mode>` arg-packs now preserves `write*()` / `flush()` receivers plus `read_byte(...)` `?` inference across direct calls plus pure/mixed spread forwarding, while wrapped `FileError` free-builtin named access remains on the existing named-argument rejection path.
- ✓ Variadic arg-pack IR-backed experimental stdlib `Map<K, V>` value-pack count slice: explicit struct locals materialized from indexed `[args<Map<K, V>>]` access now preserve full struct-slot copies through IR/VM/native lowering, and callable-definition `[args<Map<K, V>>]` locals retain struct slot counts for downstream lowering, so canonical `/std/collections/map/count(...)` works across direct, pure-spread, and mixed forwarding of experimental stdlib map value packs.
- ✓ Variadic arg-pack pointer-backed uninitialized scalar-storage slice: `[args<Pointer<uninitialized<i32>>>]` parameters now validate in semantics, preserve uninitialized-storage metadata through variadic matching and indexed `at|at_unsafe|[]` dereference resolution, and execute indexed `init(...)` plus `take(dereference(...))` across direct, pure-spread, and mixed forwarding on IR/VM/native/C++ emitter paths, while `args<Reference<uninitialized<i32>>>` remains a follow-up.
Arg-pack completion note: do not keep another catch-all "remaining non-string element kinds" umbrella once only scattered gaps remain. Add new arg-pack TODOs only for concrete unsupported element kinds backed by a reproducible semantics/lowering/backend failure.
Detailed finished arg-pack IR element-kind checkpoints were moved to `docs/todo_finished.md`. Keep this file focused on the two started materialization slices plus any truly remaining unsupported kinds.
Ownership/drop status note: completed guard and container-error-contract checkpoints are archived in `docs/todo_finished.md`. Keep the live file on the remaining non-trivial destruction and lifetime substrate work.
- ✓ Ownership/drop resize/remove substrate slice: implement removed-element destruction plus survivor compaction/swap semantics for non-trivial vector elements once the pointer-backed uninitialized-slot and move/copy substrate is ready. Completed: experimental `Vector<T>` now stores `Pointer<uninitialized<T>>` slots in `.prime`, explicitly `drop(...)`s removed/cleared elements, compacts `remove_at(...)` survivors with `take(...)` / `init(...)`, and swaps `remove_swap(...)` survivors through that same ownership-preserving path instead of rejecting non-trivial element types behind builtin vector gates.
- ○ Ownership/drop reallocation slice: extend the same non-trivial move semantics to the remaining canonical wrapper-backed vector/map growth paths now that experimental `Vector<T>` and experimental `.prime` `Map<K, V>` storage no longer depend on builtin relocation gates.
- ○ Implement container element drop behavior in vector/map runtimes once the ownership contract is fixed.
Stdlib container bring-up note: land a real `.prime` implementation under an experimental path/name first, prove parity there, replace the canonical wrapper-backed names with those real implementations, then remove the builtin/compiler-owned implementation.
Container bring-up status note: completed runtime-contract, experimental parity, and wrapper-routing checkpoints are archived in `docs/todo_finished.md`.
- ◐ Experimental stdlib `vector` storage/runtime slice: land a read-only `.prime`-owned storage/runtime path for `/std/collections/experimental_vector/*` with constructors/count/capacity/access backed by heap pointers. Progress: `/std/collections/experimental_vector/*` now returns a real experimental `Vector<T>` record backed by pointer-based `.prime` storage, and that storage now uses `Pointer<uninitialized<T>>` slots plus stdlib-owned `borrow(...)` access so shared C++/VM/native conformance covers constructors through `vectorOct`, `vectorCount`, `vectorCapacity`, `vectorAt`, and `vectorAtUnsafe` even for non-trivial experimental element flows.
- ◐ Experimental stdlib `vector` growth helper slice: add `/std/collections/experimental_vector/vectorReserve` and `vectorPush` parity on the pointer-backed `.prime` storage path. Progress: experimental vector now grows via stdlib-owned allocate-and-move logic built on `take(...)` / `init(...)` instead of raw buffer reallocation, and shared C++/VM/native conformance covers `vectorReserve`, `vectorPush`, and element preservation across non-trivial reserve-then-grow sequences.
- ✓ Experimental stdlib `vector` variadic constructor slice: replace `vectorSingle|Pair|Triple|...` helper ladders in `/std/collections/experimental_vector/*` with one variadic `.prime` constructor built on trailing `[args<T>]` parameters. Completed: `/std/collections/experimental_vector/vector<T>(values...)` now materializes pointer-backed `Vector<T>` values directly from arg packs, and the fixed-arity compatibility helpers forward to that variadic constructor without depending on builtin vector ownership-validation probes for non-trivial element types.
- ✓ Experimental stdlib `vector` per-slot ownership/drop substrate slice: implement true element destruction, overwrite cleanup, and safe buffer lifetime for pointer-backed experimental vector storage once uninitialized slot and move/copy substrate work is available. Completed: experimental `Vector<T>` now owns `Pointer<uninitialized<T>>` backing storage through `.prime` `Move`/`Destroy` hooks, explicitly drops removed/cleared elements, and moves survivors between slots/buffers through `take(...)` / `init(...)` so non-trivial element lifetimes no longer depend on compiler-owned vector helper gates.
- ✓ Experimental stdlib `map` variadic constructor slice: replace `mapSingle|Double|Triple|...` helper ladders in `/std/collections/experimental_map/*` with `entry(key, value)` plus one variadic `.prime` `map(entries...)` constructor built on trailing `[args<Entry<K, V>>]` parameters. Completed: `/std/collections/experimental_map/*` now exposes public `Entry<K, V>`, `entry(key, value)`, and `map<K, V>(entries...)` helpers, and the fixed-arity compatibility constructors forward to that variadic constructor so experimental map construction no longer needs duplicated helper ladders.
- ✓ Experimental stdlib `map` ownership/drop substrate slice: implement key/value destruction, overwrite cleanup, and safe storage lifetime for `.prime` `Map<K, V>` once the vector per-slot ownership/drop and non-trivial reallocation substrate is ready, so experimental map storage no longer hides blocked ownership work inside the helper/runtime parity item above. Completed: experimental `Map<K, V>` now stores keys/payloads in parallel experimental `Vector<K>` / `Vector<V>` records, inherits non-trivial growth through that pointer-backed `take(...)` / `init(...)` vector path, and overwrites existing values by explicitly `drop(...)`ing then `init(...)`ing the payload slot so shared semantics + C++/VM/native coverage now pins overwrite cleanup plus scope-exit destruction for non-trivial map values.
Map rollout note: read the remaining `map` items in this order: replace canonical helper routing with the real experimental `.prime` implementation, finish canonical constructor/type-surface migration, then delete the remaining compiler-owned semantics/IR/emitter paths. Completed parity, wrapper-routing, and fallback-removal checkpoints are archived in `docs/todo_finished.md`.
- ◐ Replace wrapper-backed canonical `/std/collections/map/*` helper routing with the real experimental `.prime` map implementation once map parity is proven. Progress: canonical and wrapper-layer `count|contains|tryAt|at|at_unsafe` helper calls already rewrite onto `/std/collections/experimental_map/*` for experimental `Map<K, V>` value receivers, direct helper receivers built from canonical or wrapper constructor expressions stay on that same experimental path, borrowed `Reference<Map<K, V>>` receivers now use overload-free canonical `_ref` spellings backed by the real `.prime` implementation, and shared semantics + C++/VM/native coverage pins those value-receiver, helper-receiver, and borrowed-reference paths. Builtin `map<K, V>` wrapper helpers still remain on `collections.prime` until constructor/type-surface migration is complete.
- ◐ Canonical `map` constructor explicit/first-step inference migration slice: move canonical `map<K, V>` constructor spellings onto the real experimental `.prime` `Map<K, V>` implementation for explicit experimental destinations and the first layer of inferred experimental destinations. Progress: canonical and wrapper constructor spellings already rewrite onto `/std/collections/experimental_map/mapNew|mapSingle|mapDouble|mapPair|...` for explicit experimental destinations (`[Map<K, V>]` bindings, explicit returns, parameters, fields, and assignments) and for first-step inferred experimental destinations (`[auto]`, `return<auto>`, helper-wrapped args/returns, and direct helper/method receiver expressions). Shared semantics + C++/VM/native coverage pins those explicit and first-step inferred paths.
- ◐ Canonical `map` broader inference-driven constructor routing slice: finish moving canonical `map<K, V>` constructor routing off `collections.prime` for the remaining non-explicit, non-first-step inference paths once the current experimental constructor cutover is stable. Progress: helper-wrapped and block-bodied inferred-return flows now keep walking nested generic wrapper calls and use context-aware return-binding inference, so broader inferred experimental destinations can keep rewriting canonical constructors onto the experimental `.prime` `Map<K, V>` path instead of collapsing back to builtin `map<K, V>` in those return-driven cases.
- ◐ Canonical `map` broader inferred field/default-target routing slice: extend the same constructor cutover to helper-wrapped inferred struct-field and default-parameter targets once the helper-wrapped/inferred-return slice is stable. Progress: monomorph binding inference now resolves implicit template arguments for unspecialized wrapper calls before reading return types, so inferred struct fields and `auto` default parameters that are seeded by helper-wrapped `mapNew(...)` defaults still rewrite canonical constructors onto the experimental `.prime` `Map<K, V>` path and preserve the existing mismatch diagnostics.
- ◐ Canonical `map` broader wrapped-assignment routing slice: extend the same constructor cutover to assignment targets whose RHS wraps canonical map constructors inside generic helper calls once the helper-wrapped field/default slice is stable. Progress: monomorph rewrite now carries experimental-map assignment target context through nested wrapper call trees, so helper-wrapped canonical constructors on assignment RHS values still rewrite onto the experimental `.prime` `Map<K, V>` path for explicit experimental-map locals and inferred experimental-map struct fields while preserving mismatch diagnostics.
- ◐ Canonical `map` broader wrapped-receiver routing slice: extend the same constructor cutover to helper-wrapped direct receiver expressions for canonical helper and method forms once the helper-wrapped assignment slice is stable. Progress: the monomorph map-helper/method receiver path now uses the nested experimental-map constructor rewrite traversal instead of only rewriting a top-level direct constructor call, so wrapped receiver expressions keep canonical constructors on the experimental `.prime` `Map<K, V>` path for both helper-call and method-call receiver forms while preserving mismatch diagnostics.
- ◐ Canonical `map` broader wrapped-binding routing slice: extend the same constructor cutover to helper-wrapped explicit and inferred local binding initializers once the helper-wrapped receiver slice is stable. Progress: binding-target rewrite now uses the same nested experimental-map constructor traversal as the parameter/assignment/receiver paths, so helper-wrapped canonical constructors in explicit `[Map<K, V>]` and inferred `[auto]` local initializers stay on the experimental `.prime` `Map<K, V>` path and preserve template-conflict diagnostics.
- ◐ Canonical `map` broader Result.ok payload routing slice: extend the same constructor cutover through helper-wrapped `Result.ok(...)` payloads when explicit non-return destinations require `Result<Map<K, V>, Error>` once the helper-wrapped binding slice is stable. Progress: monomorph target-type rewrite now recognizes explicit `Result<Map<K, V>, Error>` bindings and parameters, walks nested wrapper call trees until it finds `Result.ok(...)`, and rewrites canonical map constructors inside that payload onto the experimental `.prime` `Map<K, V>` path while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader Result.ok assignment routing slice: extend the same constructor cutover through helper-wrapped `Result.ok(...)` payloads on assignment targets that require `Result<Map<K, V>, Error>` once the explicit Result.ok payload slice is stable. Progress: assignment-target rewrite now uses the same target-type helper as bindings and parameters, so helper-wrapped `Result.ok(...)` payloads rewrite canonical constructors onto the experimental `.prime` `Map<K, V>` path for explicit `Result<Map<K, V>, Error>` locals and explicit `Result<Map<K, V>, Error>` struct fields while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader inferred Result.ok destination routing slice: extend the same constructor cutover through helper-wrapped `Result.ok(...)` payloads when inferred local-binding, struct-field, and default-parameter targets resolve to `Result<Map<K, V>, Error>` once the Result.ok assignment slice is stable. Progress: monomorph binding inference now treats builtin `Result.ok(...)` payloads as `Result<Payload, _>` values so generic helper wrappers can specialize around experimental-map payloads, and validator-side binding inference now preserves those specialized `Result<...>` return types for inferred local bindings plus inferred struct-field/default-parameter targets while keeping canonical map constructor mismatch diagnostics on wrapped payloads.
- ◐ Canonical `map` broader uninitialized init routing slice: extend the same constructor cutover to helper-wrapped `init(uninitialized<T>, value)` storage targets for `Map<K, V>` and `Result<Map<K, V>, Error>` values once the inferred Result.ok destination slice is stable. Progress: monomorph target-type rewrite now unwraps `uninitialized<T>` storage targets and routes `init(storage, value)` through the same experimental-map target helper, so helper-wrapped canonical map constructors and helper-wrapped `Result.ok(...)` payloads rewrite onto the experimental `.prime` `Map<K, V>` constructor path for uninitialized storage while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader wrapped-dereference assignment routing slice: extend the same constructor cutover to helper-wrapped assignment targets that write through dereference(location(...)) and helper-returned `Reference<T>` / `Pointer<T>` targets once the uninitialized init slice is stable. Progress: monomorph assignment-target resolution now unwraps dereference(...) target types from direct `location(...)` expressions and helper-returned `Reference<T>` / `Pointer<T>` values, so helper-wrapped canonical map constructors and helper-wrapped `Result.ok(...)` payloads rewrite onto the experimental `.prime` `Map<K, V>` constructor path for dereference assignment targets while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader wrapped-dereference init routing slice: extend the same constructor cutover to helper-wrapped `init(...)` storage targets that write through dereference(location(...)) and helper-returned `Reference<uninitialized<T>>` / `Pointer<uninitialized<T>>` values once the wrapped-dereference assignment slice is stable. Progress: validator-side uninitialized-storage resolution and monomorph `init(storage, value)` target rewriting now unwrap dereference(...) storage targets from direct `location(...)` expressions and helper-returned `Reference<uninitialized<T>>` / `Pointer<uninitialized<T>>` values, so helper-wrapped canonical map constructors and helper-wrapped `Result.ok(...)` payloads rewrite onto the experimental `.prime` `Map<K, V>` constructor path for dereferenced uninitialized storage while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader wrapped-dereference field-target routing slice: extend the same constructor cutover to helper-wrapped struct-field targets whose receiver is `dereference(location(...))` or a helper-returned `Reference<Struct>` / `Pointer<Struct>` once the wrapped-dereference init slice is stable. Progress: validator-side struct receiver inference and monomorph field-target resolution now unwrap dereferenced struct-reference receivers, so helper-wrapped canonical map constructors and helper-wrapped `Result.ok(...)` payloads rewrite onto the experimental `.prime` `Map<K, V>` constructor path for dereferenced struct-field assignments while preserving template-conflict diagnostics.
- ◐ Canonical `map` broader wrapped field-storage init routing slice: extend the same constructor cutover to helper-wrapped `init(...)` storage targets that write through `uninitialized<T>` struct fields on direct struct locals plus `dereference(location(...))` and helper-returned `Reference<Struct>` / `Pointer<Struct>` receivers once the wrapped-dereference field-target slice is stable. Progress: validator-side uninitialized-storage resolution now resolves struct-field storage targets from direct struct locals and inferred dereferenced/helper-returned struct receivers, so helper-wrapped canonical map constructors and helper-wrapped `Result.ok(...)` payloads rewrite onto the experimental `.prime` `Map<K, V>` constructor path for `init(holder.storage, ...)` and `init(dereference(...).storage, ...)` while preserving template-conflict diagnostics.
Map constructor migration note: do not keep another catch-all "remaining non-return destination" umbrella here. Add new constructor-routing TODOs only for concrete unreduced destination shapes with a reproducer; otherwise prefer the wrapper-bridge deletion and compiler-path removal milestones below.
- ○ Canonical `map` wrapper-bridge deletion slice: delete the wrapper-only `collections.prime` bridge for canonical `map<K, V>` construction and any remaining constructor-surface fallback once the experimental `.prime` constructor path has full parity.
- ◐ Remove compiler-owned `map` behavior from semantics once canonical stdlib `map` parity is proven. Progress: explicit `/map/count(...)` compatibility spellings now require an explicit `/map/count` definition instead of inheriting canonical `/std/collections/map/count` semantics, bare `count(values)` calls on builtin `map<K, V>` targets now require an imported canonical `/std/collections/map/count` helper or an explicit `/count` or `/map/count` definition, bare `contains(values, key)` calls on builtin `map<K, V>` targets now require an imported canonical `/std/collections/map/contains` helper or an explicit `/contains` or `/map/contains` definition, bare `at(values, key)` plus `at_unsafe(values, key)` calls on builtin `map<K, V>` targets now require imported canonical `/std/collections/map/at*` helpers or explicit `/map/at*` definitions instead of falling back to compiler-owned builtin access semantics, and semantics + C++/VM/native coverage pin deterministic `unknown call target: /map/count`, `unknown call target: /std/collections/map/count`, `unknown call target: /std/collections/map/contains`, and `unknown call target: /std/collections/map/at*` rejects when neither path exists.
- ◐ Remove the remaining compiler-owned `map` helper compatibility spellings from semantics beyond direct `/map/count(...)`. Progress: explicit `/map/contains(...)` compatibility spellings now require an explicit `/map/contains` definition instead of inheriting canonical `/std/collections/map/contains` semantics, semantics + C++/VM/native coverage pin deterministic `unknown call target: /map/contains` rejects when only canonical helpers exist, and explicit `/map/contains` definitions still keep alias precedence.
- ◐ Remove the remaining compiler-owned `map` helper compatibility spellings from semantics beyond direct `/map/count(...)` and `/map/contains(...)`. Progress: explicit `/map/tryAt(...)` compatibility spellings now require an explicit `/map/tryAt` definition instead of inheriting canonical `/std/collections/map/tryAt` semantics, semantics + C++/VM/native coverage pin deterministic `unknown call target: /map/tryAt` rejects when only canonical helpers exist, and explicit `/map/tryAt` definitions still keep alias precedence.
- ◐ Remove the remaining compiler-owned `map` helper compatibility spellings from semantics beyond direct `/map/count(...)`, `/map/contains(...)`, and `/map/tryAt(...)`. Progress: direct `/map/at(...)` and `/map/at_unsafe(...)` compatibility calls now require explicit `/map/at` or `/map/at_unsafe` definitions instead of inheriting canonical `/std/collections/map/at*` semantics, semantics + C++/VM/native coverage pin deterministic `unknown call target: /map/at*` rejects when only canonical helpers exist, and explicit `/map/at*` definitions still keep alias precedence.
- ◐ Remove compiler-owned `map` behavior from IR lowering once canonical stdlib `map` parity is proven. Progress: direct `/map/count(...)`, `/map/contains(...)`, `/map/tryAt(...)`, `/map/at(...)`, and `/map/at_unsafe(...)` compatibility spellings now stop at IR setup return-kind resolution instead of inheriting canonical `/std/collections/map/*` return metadata when only canonical helpers exist.
- ◐ Remove the remaining compiler-owned `map` method/helper routing from IR lowering beyond direct compatibility-call return-kind fallback, including builtin method sugar and canonical helper builtin emission paths. Progress: explicit `/map/count(...)`, `/map/at(...)`, `/map/at_unsafe(...)`, `/std/collections/map/count(...)`, and `/std/collections/map/at*(...)` calls now bypass the non-method builtin count/access fallback plus the canonical receiver-probe return-kind path, the native tail dispatcher now leaves those explicit helper spellings unresolved instead of emitting builtin count/access IR, explicit slash-method `/map/count`, `/map/contains`, `/map/tryAt`, `/map/at*`, `/std/collections/map/count|contains|tryAt|at*` spellings now stay on deterministic same-path `unknown method` diagnostics instead of re-entering cross-path builtin method fallback unless a real same-path helper definition exists, explicit map access helper receivers no longer synthesize builtin value kinds for downstream method lookup when no real helper definition exists, and explicit `/map/tryAt(...)` receiver probes no longer inherit canonical `/std/collections/map/tryAt` return-struct metadata when no real compatibility definition exists, so those spellings only resolve through real definitions and shared IR validation pins the direct-definition, native-dispatch `NotHandled`, slash-method reject, receiver-kind reject, and compatibility-receiver reject contracts.
- ◐ Remove compiler-owned `map` bare-call and bare-method fallback behavior from emitters/backends once canonical stdlib `map` parity is proven. Progress: C++ emitter bare-map method path resolution now prefers canonical `/std/collections/map/count|contains|tryAt` helpers over `/map/*` compatibility aliases when canonical definitions or metadata exist, falls back to `/map/*` only when the canonical helper path is absent, bare `contains(values, key)` call emission now prefers canonical `/std/collections/map/contains` helpers over `/map/contains` aliases before falling back to the compiler builtin, bare `tryAt(values, key)` call emission now prefers canonical `/std/collections/map/tryAt` helpers over `/map/tryAt` aliases while keeping deterministic `unknown call target: /std/collections/map/tryAt` rejects when no helper exists, and bare `values.at(...)` / `values.at_unsafe(...)` method-chain receiver typing now prefers canonical `/std/collections/map/at*` helper return metadata over `/map/at*` aliases before falling back to builtin map-element typing.
- ◐ Remove compiler-owned `map` explicit slash-method and receiver-metadata fallback behavior from emitters/backends once canonical stdlib `map` parity is proven. Progress: slash-method receiver metadata probing for explicit `/map/count|contains|tryAt` plus `/std/collections/map/count|contains|tryAt` spellings now stays on same-path helpers instead of cross-path fallback, direct `tryAt(...).method()` receiver metadata probes now stay on same-path `/map/tryAt` versus `/std/collections/map/tryAt` helpers instead of inheriting cross-path return-struct metadata, direct `at(...).method()` / `at_unsafe(...).method()` receiver probes now prefer same-path `/map/at*` versus `/std/collections/map/at*` helper metadata before falling back to builtin map element typing, and explicit slash-method `values./map/at*(...)` / `values./std/collections/map/at*(...)` method-chain receivers now also prefer same-path helper metadata before falling back to builtin map element typing.
- ◐ Remove the remaining compiler-owned `map` backend builtin-helper emission fallback once canonical replacement is complete. Progress: explicit direct-call `/std/collections/map/at(...)` and `/std/collections/map/at_unsafe(...)` without real same-path helpers now lower to deleted helper stubs in the C++ emitter instead of emitting builtin `ps_map_at*` fallback, explicit slash-method `values./std/collections/map/at*(...)` without real same-path helpers now also lowers to deleted helper stubs instead of emitting builtin `ps_map_at*` fallback, nested direct `count(/std/collections/map/at(...))` receivers now lower to a dedicated deleted receiver stub instead of emitting builtin `ps_map_count(...)` around those missing explicit helper paths, nested slash-method `count(values./std/collections/map/at(...))` receivers now lower to that same deleted receiver stub wrapped around the deleted slash-method access helper instead of falling back to primitive receiver diagnostics, nested direct `contains(/std/collections/map/at(...), key)` receivers now lower to a dedicated deleted receiver stub instead of emitting builtin `ps_map_contains(...)` around those same missing explicit helper paths, and nested slash-method `contains(values./std/collections/map/at(...), key)` receivers now lower through that same deleted receiver stub wrapped around the deleted slash-method access helper instead of synthesizing builtin `ps_map_contains(...)`.
Vector migration note: completed canonical wrapper-routing and early no-import fallback-removal checkpoints are archived in `docs/todo_finished.md`.
- ✓ Replace wrapper-backed canonical `/std/collections/vector/*` helpers with the real experimental `.prime` vector implementation once vector parity and ownership substrate are ready. Completed: canonical `/std/collections/vector/*` plus wrapper-layer `/std/collections/vector*` helper calls now rewrite onto `/std/collections/experimental_vector/*` for experimental `Vector<T>` value receivers, including count/capacity/access and mutating growth/remove flows, and shared semantics + C++/VM/native coverage now pins those explicit helper plus method-call routes while preserving the existing import requirement for canonical helper names.
- ○ Canonical `vector` constructor/type-surface migration slice: move canonical `vector<T>` construction and the canonical value-receiver `/std/collections/vector/*` surface onto the real experimental `.prime` `Vector<T>` implementation once vector parity plus ownership/drop substrate are ready.
- ◐ Remove the remaining no-import builtin fallback for canonical `/std/collections/vector/*` mutator helper spellings once the imported stdlib wrapper path is stable. Progress: canonical `/std/collections/vector/push(...)`, `/std/collections/vector/pop(...)`, `/std/collections/vector/reserve(...)`, `/std/collections/vector/clear(...)`, `/std/collections/vector/remove_at(...)`, and `/std/collections/vector/remove_swap(...)` now require either imported stdlib wrappers or explicit helper definitions in direct-call form, explicit canonical method forms `values./std/collections/vector/clear|remove_at|remove_swap(...)` now deterministically reject with same-path `unknown method` diagnostics when no imported or declared canonical helper exists, imported canonical wrappers accept ordinary named arguments in statement position, and shared semantics/C++ coverage pins the direct-call and method-form reject contracts.
- ◐ Remove the remaining compiler-owned `vector` explicit helper/method routing from emitters/backends beyond explicit canonical slash-method `count()` builtin emission fallback on map and array receivers, including broader expression builtin helper emission fallback. Progress: explicit canonical slash-method `values./std/collections/vector/count()` on string, map, and array receivers without a same-path helper definition now stops in semantics with deterministic `unknown method: /std/collections/vector/count` diagnostics instead of reaching backend deleted-stub routing, bare `count(wrapperVector())` plus direct `/std/collections/vector/count(wrapperVector())` wrapper-temporary vector expressions without imported or declared same-path helpers now stop in semantics with deterministic `unknown call target: /std/collections/vector/count` diagnostics instead of reaching backend deleted-stub routing, bare `capacity(wrapperVector())` plus direct `/std/collections/vector/capacity(wrapperVector())` wrapper-temporary vector expressions without imported or declared same-path helpers now stop in semantics with deterministic `unknown call target: /std/collections/vector/capacity` diagnostics instead of reaching backend deleted-stub routing, bare `wrapperVector().count()` method expressions without imported or declared same-path helpers now stop in semantics with deterministic `unknown method: /vector/count` diagnostics instead of reaching backend deleted-stub routing, bare `wrapperVector().capacity()` method expressions without imported or declared same-path helpers now stop in semantics with deterministic `unknown method: /vector/capacity` diagnostics instead of reaching backend deleted-stub routing, direct compatibility-alias wrapper calls `/vector/count(wrapperVector())` plus `/vector/capacity(wrapperVector())` now require same-path `/vector/*` helpers instead of reaching backend deleted-stub routing or inheriting canonical helper availability, alias direct-call `/vector/count(wrapperText())` plus `/vector/count(wrapperArray())` now stop in semantics with same-path `unknown call target: /vector/count` diagnostics instead of reaching deleted C++ helper stubs, and alias slash-method `wrapperText()./vector/count()` plus `wrapperArray()./vector/count()` now stop in semantics with same-path `unknown method: /vector/count` diagnostics instead of reaching deleted C++ helper stubs.
Compatibility bridge note: the started slices below are temporary mixed-mode stabilizers while stdlib containers come online. Do not add new bridge-only TODOs unless they directly unblock experimental stdlib parity, canonical replacement with the real `.prime` containers, or deletion of a concrete compiler-owned collection path.
Map/vector deletion note: success here means canonical `vector`/`map` behavior lives in `.prime` plus minimal runtime substrate, not that every compatibility edge case gets its own long-lived C++ diagnostic path or that builtin collection knowledge grows inside C++.
- ◐ Vector/map compatibility-alias cleanup slice: finish helper alias classification cleanup so removed compatibility spellings never re-enter builtin fallback. Progress: lowerer/setup/count-access helper alias classifiers now keep removed `/array/*` compatibility spellings out of builtin classification, including `/array/capacity`, and `/array/vector(...)` now deterministically reports `unknown call target: /array/vector` instead of re-entering constructor fallback.
- ◐ Vector/map bridge reject-coverage slice: keep mixed builtin+stdlib compatibility paths on deterministic builtin or canonical diagnostics instead of forwarding through removed helper spellings. Progress: started coverage now locks mutator/count/access, direct vs method vs slash-method access, wrapper-returned receiver typing, canonical-vs-alias precedence, and map value-result fallback behavior across semantics, IR validation, and VM/native/C++ suites.
Bridge parking note: do not split this bridge bucket further unless a specific failure blocks experimental stdlib bring-up, parity harness work, or deletion of an existing compiler-owned collection path.
Minimal vector substrate note: the remaining items below are only worth keeping insofar as they extract the runtime/error/allocation substrate that stdlib `.prime` `vector` needs; they are not goals to preserve compiler-owned `vector` semantics indefinitely.
- ◐ Collection runtime-contract slice: align builtin `capacity()` receiver validation with the documented fixed-size `array<T>` vs dynamic `vector<T>` contract while canonical helper routing is still mixed-mode. Progress: method-form `capacity()` on known collection receivers now routes through builtin validation, so non-vector targets emit deterministic `capacity requires vector target` diagnostics while user-defined `/array/capacity` helpers still take precedence.
- ◐ Collection runtime-contract slice: align builtin `push` receiver-shape diagnostics with the fixed-size `array<T>` vs dynamic `vector<T>` contract while canonical helper routing is still mixed-mode. Progress: builtin `push` now validates receiver shape before heap-effect checks, so fixed-size `array` targets deterministically report `push requires vector binding` in call and method forms.
- ◐ Collection runtime-contract slice: align builtin `reserve` receiver-shape diagnostics with the fixed-size `array<T>` vs dynamic `vector<T>` contract while canonical helper routing is still mixed-mode. Progress: builtin `reserve` now validates receiver shape before heap-effect checks, so fixed-size `array` targets deterministically report `reserve requires vector binding` in call and method forms.
- ◐ Collection runtime-contract slice: enforce deterministic numeric-argument validation for vector mutators while canonical helper routing is still mixed-mode. Progress: builtin `reserve`/`remove_at`/`remove_swap` now reject boolean capacities/indices in both call and method forms with deterministic `requires integer ...` diagnostics.
- ◐ Collection runtime-contract slice: stabilize named-argument diagnostic ordering and receiver probing for builtin mutator calls while canonical helper routing is still mixed-mode. Progress: expression-context mutator calls now prioritize the statement-only diagnostic even with named arguments, and statement-context named-argument calls validate receiver shape first while still finding valid vector receivers when `[values]` is absent.
- ◐ Dynamic vector-runtime substrate slice: lower VM/native vector locals through `count/capacity/data_ptr` records instead of fixed-capacity frame layouts. Progress: vector locals now lower through `count/capacity/data_ptr` records.
- ◐ Dynamic vector-runtime substrate slice: emit deterministic compile-time overflow/limit diagnostics for `reserve` expressions. Progress: compile-time `reserve` diagnostics now fold signed/unsigned `plus`/`minus`/`negate` literal expressions and report deterministic overflow/limit errors.
- ◐ Dynamic vector-runtime substrate slice: replace `push` growth overflow abort behavior with deterministic allocation-failure runtime semantics. Progress: runtime `push` growth overflow paths now emit deterministic allocation-failure diagnostics.
- ◐ Dynamic vector-runtime substrate slice: replace `reserve` growth overflow abort behavior with deterministic allocation-failure runtime semantics. Progress: runtime `reserve` growth overflow paths now emit deterministic allocation-failure diagnostics.
- ◐ Dynamic vector heap-backing substrate slice: route VM/native vector literal `data_ptr` storage to runtime heap allocation instead of frame-local slots. Progress: vector literal lowering now allocates `data_ptr` storage via runtime `HeapAlloc`.
- ◐ Dynamic vector heap-backing substrate slice: route `reserve` growth reallocation paths through runtime heap allocation instead of frame-local max-capacity slots. Progress: `reserve` growth now reallocates element storage through runtime heap allocations.
- ◐ Dynamic vector heap-backing substrate slice: route `push` growth copy/reallocation paths through runtime heap allocation instead of frame-local max-capacity slots. Progress: `push` growth now reallocates and copies element storage through runtime heap allocations.
- ◐ `soa_vector<T>` freeze/stabilize slice: keep the current empty/header-only construction, `count`, validation, and existing helper-precedence behavior working while the new path lands, but do not expand compiler-owned `soa_vector` surface area unless it directly unblocks the items below.
- ○ Lock the v1 `soa_vector` contract before more implementation work: construction forms, `count`, `get`, `ref`, field-view indexing shape, explicit `to_soa` / `to_aos`, and invalidation boundaries after structural mutation.
- ○ Land the minimum compile-time struct-field introspection/codegen needed for one `.prime` `soa_vector` implementation to derive SoA column schemas from `T`.
- ○ Land the minimum column-storage substrate needed for one `.prime` `soa_vector` implementation: multi-column alloc/grow/free/read/write primitives with deterministic allocation-failure behavior.
- ○ Land the minimum borrowed-view / invalidation substrate needed for one `.prime` `soa_vector` implementation so `ref(...)` and field views have language-level semantics independent of compiler-owned `soa_vector` paths.
- ○ Add experimental stdlib `/std/collections/experimental_soa_vector/*` early with empty construction, `count`, SoA-safe type validation, and the smallest `.prime` storage/runtime path that exercises the new substrate.
- ○ Extend experimental stdlib `/std/collections/experimental_soa_vector/*` to non-empty literal construction, `push`, `reserve`, `get`, `ref`, and explicit `to_soa` / `to_aos`, adding only the next substrate pieces that the `.prime` implementation proves it needs.
- ○ Extend experimental stdlib `/std/collections/experimental_soa_vector/*` to field-view indexing (`value.field()[i]`) once the experimental `.prime` implementation reaches that boundary and the next minimal substrate slice is clear.
- ○ Route canonical `/std/collections/soa_vector/*` names through the real experimental `.prime` implementation once parity is proven.
- ○ Delete compiler-owned `soa_vector` helper routing and backend special cases from semantics, IR lowering, emitters, and runtime code until no C++ source mentions `soa_vector`.

**Maintainability**
File-size note: keep production source under `src/` below roughly 700 lines when practical. The items below track the current oversized files as behavior-preserving refactors; prefer extracting coherent helper units over purely mechanical file splits.
- ✓ Split `primec_lib` into smaller subsystem libraries once the semantics and lowering interfaces are explicit. Completed: the build graph now separates shared utilities, frontend passes, and backend/runtime code into explicit subsystem targets; `primec` links the narrower `primec_ir_lib` + `primec_backend_registry_lib` pair directly; doctest binaries link `primec_ir_lib`, `primec_frontend_lib`, or that same narrower backend pair directly; and `primec_lib` remains only as a compatibility umbrella target.
- ✓ Split `primec_backend_lib` further into narrower IR/emitter/runtime subsystem libraries once the remaining transitive dependencies are reduced. Completed: backend-facing implementation now stays on explicit `primec_ir_lib`, `primec_backend_registry_lib`, `primec_backend_emitters_lib`, and `primec_runtime_lib` targets; `primec` and `PrimeStruct_backend_tests` link the narrower IR + registry pair directly; and `primec_backend_lib` remains only as a compatibility umbrella target.
- ✓ Reduce the remaining transitive coupling between `primec_ir_lib` and `primec_codegen_lib` so backend registry/pipeline glue depends on narrower interfaces instead of broad codegen targets. Completed: backend registry glue now links the dedicated `primec_backend_emitters_lib` implementation target instead of the broad `primec_codegen_lib` compatibility umbrella, while `primec_codegen_lib` remains available only as the compatibility umbrella over that narrower emitter implementation target.
- ✓ Continue shrinking backend-registry dependencies so compile-pipeline callers can depend on registry interfaces without pulling unrelated emitter/runtime implementation libraries. Completed: shared VM backend diagnostics now live on the IR side, `primevm` links `primec_ir_lib` + `primec_runtime_lib` directly instead of depending on the backend registry/codegen path, compile-pipeline-driven semantics suites run from a dedicated `primec_ir_lib`-linked doctest binary, and `primec_backend_registry_lib` now keeps emitter/runtime implementation targets private so callers only depend on the registry interface on their direct link surface.
- ◐ Refactor `src/semantics/SemanticsValidatorExpr.cpp` (`2972` lines) below the `700`-line target. Progress: extracted lambda-expression validation into `src/semantics/SemanticsValidatorExprLambda.cpp`, numeric/matrix builtins into `src/semantics/SemanticsValidatorExprNumeric.cpp`, builtin `if(...)` expression validation into `src/semantics/SemanticsValidatorExprControlFlow.cpp`, builtin `block { ... }` expression validation into `src/semantics/SemanticsValidatorExprBlock.cpp`, vector-helper call resolution/compatibility checks into `src/semantics/SemanticsValidatorExprVectorHelpers.cpp`, method-target/static-helper resolution into `src/semantics/SemanticsValidatorExprMethodResolution.cpp`, struct-field/reflection receiver resolution into `src/semantics/SemanticsValidatorExprFieldResolution.cpp`, call-expression body-argument target remapping/validation into `src/semantics/SemanticsValidatorExprBodyArguments.cpp`, pointer/reference method fallback helpers into `src/semantics/SemanticsValidatorExprPointerLike.cpp`, non-collection direct-call receiver-path resolution into `src/semantics/SemanticsValidatorExprReceiverPaths.cpp`, argument-type/struct-compatibility validation helpers into `src/semantics/SemanticsValidatorExprArgumentValidation.cpp`, collection access/get-ref dispatch into `src/semantics/SemanticsValidatorExprCollectionAccess.cpp`, collection access fallback/compatibility validation into `src/semantics/SemanticsValidatorExprCollectionAccessValidation.cpp`, collection count/capacity dispatch into `src/semantics/SemanticsValidatorExprCollectionCountCapacity.cpp`, collection literal validation into `src/semantics/SemanticsValidatorExprCollectionLiterals.cpp`, collection receiver/predicate helpers into `src/semantics/SemanticsValidatorExprCollectionPredicates.cpp`, GPU/buffer builtin validation into `src/semantics/SemanticsValidatorExprGpuBuffer.cpp`, map/SoA builtin validation into `src/semantics/SemanticsValidatorExprMapSoaBuiltins.cpp`, scalar/pointer/memory builtin validation into `src/semantics/SemanticsValidatorExprScalarPointerMemory.cpp`, Result/File helper validation into `src/semantics/SemanticsValidatorExprResultFile.cpp`, `try(...)` builtin validation into `src/semantics/SemanticsValidatorExprTry.cpp`, named-argument builtin-label/access validation into `src/semantics/SemanticsValidatorExprNamedArgumentBuiltins.cpp`, `validateExpr` now routes bare map/vector helper rewrites plus experimental map helper specialization through the shared `src/semantics/SemanticsValidatorCollectionHelperRewrites.cpp` unit instead of keeping another local helper-rewrite island, collection literal handling now delegates fully to `src/semantics/SemanticsValidatorExprCollectionLiterals.cpp` instead of keeping a second inline copy, Result/File builtin validation now delegates to `src/semantics/SemanticsValidatorExprResultFile.cpp`, named-argument builtin rejection now delegates to `src/semantics/SemanticsValidatorExprNamedArgumentBuiltins.cpp`, `try(...)` builtin validation now delegates to `src/semantics/SemanticsValidatorExprTry.cpp` instead of staying inline in `validateExpr`, GPU/buffer builtin validation now delegates to `src/semantics/SemanticsValidatorExprGpuBuffer.cpp` instead of keeping local element-type helper lambdas and builtin branches in `validateExpr`, convert/print/pointer/memory builtin validation now delegates to `src/semantics/SemanticsValidatorExprScalarPointerMemory.cpp` instead of keeping a second inline builtin island in `validateExpr`, SoA conversion/access validation now delegates to `src/semantics/SemanticsValidatorExprMapSoaBuiltins.cpp` instead of keeping inline `to_soa`/`to_aos`, `soa_vector` `get`/`ref`, and field-view branches in `validateExpr`, early map `contains` validation now delegates to `src/semantics/SemanticsValidatorExprMapSoaBuiltins.cpp` with shared bare-map operand selection instead of keeping a duplicate inline pre-dispatch block in `validateExpr`, early builtin access fallback validation now delegates to `src/semantics/SemanticsValidatorExprCollectionAccessValidation.cpp` while deferring map-like direct-call receivers back to the dedicated bare-map access handling, the shared collection predicate helpers now also own the duplicated map-like bare-access receiver probe instead of keeping two local query walkers in `validateExpr`, the builtin access receiver selector now reuses a shared member instead of staying duplicated across Expr/method-resolution/infer collection helpers, and the remaining type-namespace field/method plus reflection-target dispatch now reuses the existing `src/semantics/SemanticsValidatorExprFieldResolution.cpp` member helpers instead of keeping duplicate local copies.
- ◐ Refactor `src/semantics/SemanticsValidatorInfer.cpp` (`4188` lines) below the `700`-line target. Progress: extracted match/if/block return-kind inference into `src/semantics/SemanticsValidatorInferControlFlow.cpp`, collection type/template call-target helpers into `src/semantics/SemanticsValidatorInferCollections.cpp`, builtin collection method/access return-kind dispatch into `src/semantics/SemanticsValidatorInferCollectionDispatch.cpp`, builtin collection count/capacity dispatch into `src/semantics/SemanticsValidatorInferCollectionCountCapacity.cpp` plus `src/semantics/SemanticsValidatorInferCollectionDirectCountCapacity.cpp`, definition/statement return-kind inference helpers plus legacy `inferDefinitionReturnKind(...)` into `src/semantics/SemanticsValidatorInferDefinition.cpp`, graph-based return-kind orchestration plus graph-local auto-binding capture into `src/semantics/SemanticsValidatorInferGraph.cpp`, method-call path resolution into `src/semantics/SemanticsValidatorInferMethodResolution.cpp`, struct-return-path inference into `src/semantics/SemanticsValidatorInferStructReturn.cpp`, top-level numeric/reference/uninitialized inference utilities into `src/semantics/SemanticsValidatorInferUtility.cpp`, args-pack plus experimental-map target-resolution helpers into `src/semantics/SemanticsValidatorInferTargetResolution.cpp`, and infer-side collection compatibility plus canonical experimental-map rewrite probes now reuse the shared helper members instead of keeping duplicate local tables and rewrite lambdas.
- ◐ Refactor `src/semantics/TemplateMonomorph.cpp` (`3161` lines) below the `700`-line target. Progress: extracted templated fallback type-inference helpers into `src/semantics/TemplateMonomorphFallbackTypeInference.h`, method-call target resolution helpers into `src/semantics/TemplateMonomorphMethodTargets.h`, call/return binding inference into `src/semantics/TemplateMonomorphBindingCallInference.h`, and block-bodied binding inference into `src/semantics/TemplateMonomorphBindingBlockInference.h`.
- ✓ Refactor `src/semantics/SemanticsValidatorPasses.cpp` (`2737` lines) below the `700`-line target. Completed: extracted effects/capabilities resolution helpers into `src/semantics/SemanticsValidatorPassesEffects.cpp`, definition/execution intra-body diagnostic collection helpers into `src/semantics/SemanticsValidatorPassesDiagnostics.cpp`, moved definition validation orchestration into `src/semantics/SemanticsValidatorPassesDefinitions.cpp`, moved effect-free summary/expression/statement validation into `src/semantics/SemanticsValidatorPassesEffectFree.cpp`, moved execution validation orchestration into `src/semantics/SemanticsValidatorPassesExecutions.cpp`, moved omitted-initializer plus zero-arg-constructor validation into `src/semantics/SemanticsValidatorPassesOmittedInitializers.cpp`, moved uninitialized-storage definite-state validation into `src/semantics/SemanticsValidatorPassesUninitialized.cpp`, and moved struct-layout validation into `src/semantics/SemanticsValidatorPassesStructLayouts.cpp`, leaving the original file focused on the small remaining orchestration/lookup helpers.
- ✓ Refactor `src/semantics/SemanticsValidatorStatement.cpp` (`1515` lines) below the `700`-line target. Completed: extracted vector helper statement validation into `src/semantics/SemanticsValidatorStatementVectorHelpers.cpp`, extracted container drop/relocation helper validation into `src/semantics/SemanticsValidatorStatementContainerHelpers.cpp`, extracted string/printability statement helpers into `src/semantics/SemanticsValidatorStatementPrintability.cpp`, extracted return-statement validation into `src/semantics/SemanticsValidatorStatementReturns.cpp`, extracted loop/conditional statement validation into `src/semantics/SemanticsValidatorStatementControlFlow.cpp`, extracted binding initialization and borrow-validation statement checks into `src/semantics/SemanticsValidatorStatementBindings.cpp`, extracted path-space/compute/buffer builtin statement validation into `src/semantics/SemanticsValidatorStatementBuiltins.cpp`, and extracted body-argument target remapping plus block validation into `src/semantics/SemanticsValidatorStatementBodyArguments.cpp`, leaving the original file focused on top-level statement orchestration and builtin entry handling.
- ✓ Refactor `src/Vm.cpp` (`2950` lines) below the `700`-line target. Completed: extracted shared heap allocation and indirect-address helpers into `src/VmHeapHelpers.cpp`, extracted source-breakpoint resolution plus mapped stack-trace formatting helpers into `src/VmDebugHelpers.cpp`, extracted debug-session setup/start methods into `src/VmDebugSessionSetup.cpp`, extracted debug-session continue/breakpoint/pause/hook/snapshot methods into `src/VmDebugSession.cpp`, extracted debug-session instruction stepping into `src/VmDebugSessionInstruction.cpp`, extracted shared print/file I/O opcode handlers into `src/VmIoHelpers.cpp`, and moved the main VM execution loop into `src/VmExecution.cpp`, leaving `src/Vm.cpp` as the small public wrapper surface.
- ✓ Refactor `src/VmExecution.cpp` (`875` lines) below the `700`-line target. Completed: extracted arithmetic/compare/convert execution handling into `src/VmExecutionNumeric.cpp`, leaving `src/VmExecution.cpp` focused on stack/local/heap setup, control flow, calls/returns, I/O dispatch, and string helpers.
- ✓ Extract source-breakpoint resolution and mapped stack-trace formatting helpers from `Vm.cpp` into dedicated helper unit(s). Completed: `resolveSourceBreakpoints(...)` and `VmDebugSession::appendMappedStackTrace(...)` now live in `src/VmDebugHelpers.cpp`, leaving `Vm.cpp` focused on execution and session control flow.
- ✓ Extract shared file/print I/O opcode helpers from `Vm.cpp` into dedicated helper unit(s). Completed: print and file opcode families now route through shared `vm_detail::handlePrintOpcode(...)` and `vm_detail::handleFileOpcode(...)` helpers in `src/VmIoHelpers.cpp`, so `Vm::execute(...)` and `VmDebugSession::stepInstruction(...)` share one implementation for those behaviors.
- ◐ Refactor `src/semantics/SemanticsValidatorBuild.cpp` (`2353` lines) below the `700`-line target. Progress: extracted per-definition transform validation and struct/public registration checks from `buildDefinitionMaps()` into `src/semantics/SemanticsValidatorBuildTransforms.cpp`, extracted import-alias/import-diagnostic handling into `src/semantics/SemanticsValidatorBuildImports.cpp`, extracted struct-return resolution plus return-kind diagnostics/setup into `src/semantics/SemanticsValidatorBuildReturnKinds.cpp`, extracted lifecycle-helper validation into `src/semantics/SemanticsValidatorBuildLifecycle.cpp`, moved `buildParameters()` into `src/semantics/SemanticsValidatorBuildParameters.cpp`, moved callee-path rewriting/resolution into `src/semantics/SemanticsValidatorBuildCallResolution.cpp`, moved struct-field binding plus SoA envelope validation into `src/semantics/SemanticsValidatorBuildStructFields.cpp`, moved uninitialized-storage binding resolution into `src/semantics/SemanticsValidatorBuildUninitialized.cpp`, moved the small block/param/return-kind/graph-local utility helpers into `src/semantics/SemanticsValidatorBuildUtility.cpp`, moved direct-call binding inference into `src/semantics/SemanticsValidatorBuildDirectCallBinding.cpp`, and moved initializer-type inference into `src/semantics/SemanticsValidatorBuildInitializerInference.cpp`, leaving the original file focused on top-level build-map orchestration.
- ✓ Refactor `src/wasm_emitter/WasmEmitter.cpp` (`2306` lines) below the `700`-line target. Completed: extracted Wasm module section encoding helpers into `src/wasm_emitter/WasmEmitterSections.cpp`, extracted WASI runtime detection/context setup plus data-segment staging into `src/wasm_emitter/WasmEmitterRuntimeContext.cpp`, extracted the `emitWasi*` helper cluster into `src/wasm_emitter/WasmEmitterWasiHelpers.cpp`, extracted structured control-flow lowering into `src/wasm_emitter/WasmEmitterControlFlow.cpp`, extracted local-layout plus function-body assembly into `src/wasm_emitter/WasmEmitterFunctionBodies.cpp`, extracted module assembly plus export-name/return-kind helpers into `src/wasm_emitter/WasmEmitterModule.cpp`, extracted print/file/argv opcode lowering into `src/wasm_emitter/WasmEmitterIo.cpp`, and shared internal Wasm type/layout structs through `src/wasm_emitter/WasmEmitterInternal.h`, leaving the original file focused on generic opcode lowering glue.
- ○ Refactor `src/ir_lowerer/IrLowererSetupTypeHelpers.cpp` (`1989` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererLowerInferenceSetup.cpp` (`1976` lines) below the `700`-line target.
- ○ Refactor `src/semantics/SemanticsHelpersCore.cpp` (`1973` lines) below the `700`-line target.
- ○ Refactor `src/emitter/EmitterHelpersBuiltins.cpp` (`1695` lines) below the `700`-line target.
- ○ Refactor `src/native_emitter/NativeEmitterInternals.h` (`1619` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererFlowHelpers.cpp` (`1568` lines) below the `700`-line target.
- ○ Refactor `src/emitter/EmitterExprCalls.h` (`1512` lines) below the `700`-line target.
- ○ Refactor `src/emitter/EmitterEmitSetup.h` (`1458` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererOperatorConversionsAndCallsHelpers.cpp` (`1427` lines) below the `700`-line target.
- ○ Refactor `src/IrToCppEmitter.cpp` (`1421` lines) below the `700`-line target.
- ○ Refactor `src/parser/ParserCore.cpp` (`1289` lines) below the `700`-line target.
- ○ Refactor `src/parser/ParserLists.cpp` (`1245` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererStatementBindingHelpers.cpp` (`1190` lines) below the `700`-line target.
- ○ Refactor `src/VmDebugDap.cpp` (`1104` lines) below the `700`-line target.
- ○ Refactor `src/parser/ParserExpr.cpp` (`1083` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererStructTypeHelpers.cpp` (`1058` lines) below the `700`-line target.
- ○ Refactor `src/text_filter/TextFilterPipelinePass.cpp` (`1037` lines) below the `700`-line target.
- ○ Refactor `src/ImportResolver.cpp` (`1010` lines) below the `700`-line target.
- ○ Refactor `src/native_emitter/NativeEmitterHelpers.cpp` (`991` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererOperatorClampMinMaxTrigHelpers.cpp` (`953` lines) below the `700`-line target.
- ○ Refactor `src/IrPrinter.cpp` (`943` lines) below the `700`-line target.
- ○ Refactor `src/semantics/SemanticsValidator.cpp` (`938` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererUninitializedTypeHelpers.cpp` (`924` lines) below the `700`-line target.
- ○ Refactor `src/text_filter/TextFilterPipelineEnvelope.cpp` (`906` lines) below the `700`-line target.
- ○ Refactor `src/primevm_main.cpp` (`868` lines) below the `700`-line target.
- ○ Refactor `src/emitter/EmitterHelpersTypes.cpp` (`857` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererOperatorArcHyperbolicHelpers.cpp` (`828` lines) below the `700`-line target.
- ○ Refactor `src/native_emitter/NativeEmitterEmit.cpp` (`822` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererSetupInferenceHelpers.cpp` (`815` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererResultHelpers.cpp` (`805` lines) below the `700`-line target.
- ○ Refactor `src/IrToGlslEmitter.cpp` (`793` lines) below the `700`-line target.
- ○ Refactor `src/text_filter/TextFilterHelpers.cpp` (`789` lines) below the `700`-line target.
- ○ Refactor `src/glsl_emitter/GlslEmitterExpr.cpp` (`788` lines) below the `700`-line target.
- ○ Refactor `src/semantics/SemanticsHelpersValidation.cpp` (`783` lines) below the `700`-line target.
- ○ Refactor `src/emitter/EmitterEmitBody.h` (`781` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererStatementCallHelpers.cpp` (`768` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererHelpers.cpp` (`745` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererLowerEmitExpr.h` (`735` lines) below the `700`-line target.
- ○ Refactor `src/ir_lowerer/IrLowererCallHelpers.h` (`725` lines) below the `700`-line target.

**Architecture follow-up (type-resolution dependency graph / SCC solver)**
Type-resolution graph rollout note: keep the graph migration blocked on the remaining collection-helper/runtime predecessor items so solver work does not target moving call-target semantics. Near-term graph work should stop at a return-kind pilot plus parity; query, CT-eval, parallelization, and broad inference migration stay deferred until that path is stable.
Staged graph rollout note: land the graph builder plus deterministic dump first with no solver behavior change; if stable node IDs, edge insertion order, or dump snapshots drift unexpectedly, stop before enabling graph-driven inference. Then ship a return-kind-only pilot behind `--type-resolver=graph`, keep legacy inference as the default until parity coverage shows matching diagnostics/IR except for explicitly accepted fixes, and use that parity gate as the rollback check before any broader expansion. Only after the pilot is default-on and still has a working `--type-resolver=legacy` rollback path should graph solving expand to template/local inference, then query and CT-eval consumers; remove the legacy path only after those later stages no longer need the rollback gate.
- ✓ Add a `DiagnosticSink` abstraction shared by compile pipeline, semantics, lowering, and both CLIs. Progress: `DiagnosticSink`/`DiagnosticSinkReport` now back shared parser/semantic/lowering diagnostics, `IrPreparationFailure` and both CLI entrypoints consume the same report shape, and compile-pipeline/semantics adapters no longer translate between bespoke diagnostic record structs.
- ✓ Extract per-definition `ValidationContext` objects from `SemanticsValidator` and remove shared mutable traversal fields. Progress: `SemanticsValidator` now caches base validation contexts per definition and execution, scopes a single `currentValidationContext_` through validation, and no longer carries parallel mutable traversal fields for effects, moves, borrow ends, or definition metadata.
- ✓ Add a short staged rollout note that names the implementation checkpoints (builder/dump, return-kind pilot, parity, template/local expansion, query and CT-eval follow-ups, legacy removal) and the rollback gates between them. Progress: the Architecture follow-up section now spells out the ordered builder/dump → return-kind pilot → parity/default flip → template/local expansion → query/CT-eval → legacy removal stages and the rollback gate that must hold between each stage.
- ✓ Add a short design note in `docs/PrimeStruct.md` defining the graph model and invariants: node kinds (definition return-kind, call-site type constraints, local `auto` constraints), edge kinds (dependency vs requirement), legal-cycle policy (SCC fixed-point) vs illegal-cycle policy (deterministic diagnostic), and deterministic ordering guarantees. Progress: `docs/PrimeStruct.md` now defines the planned internal graph boundary, node and edge classes, SCC-vs-illegal-cycle policy, and the stable ordering guarantees that later builder/dump/solver slices must preserve.
- ✓ Introduce a new semantics-internal `TypeResolutionGraph` data model and builder (no behavior change): build nodes/edges from canonical AST after semantic transforms/template monomorphization, with stable node IDs and stable edge insertion order. Progress: semantics now has an internal `TypeResolutionGraph` with definition-return, call-constraint, and local-auto nodes plus dependency/requirement edges, and the testing helper path builds it from the canonical post-transform/post-monomorphization AST with deterministic node IDs and edge order.
- ✓ Add a deterministic graph dump hook for tests/tooling (`--dump-stage=type-graph` or equivalent) and snapshot tests that lock graph shape/order for representative programs (simple call chain, mutual recursion, namespace imports, templates). Progress: the compile pipeline and both CLIs now expose deterministic `type-graph`/`type_graph` dumps through the shared formatter, and snapshot-style tests lock representative simple-call, mutual-recursion, namespace-import, and template-specialization graph shape and order.
- ✓ Implement reusable SCC utilities (Tarjan/Kosaraju) in a standalone unit with unit tests for acyclic, single-cycle, multi-cycle, and disconnected graphs. Progress: semantics now has a standalone Tarjan-based SCC utility with deterministic component/member ordering, a stable testing helper API, and focused unit coverage for acyclic, single-cycle, multi-cycle, and disconnected graphs.
- ✓ Implement a condensation-DAG builder on top of the SCC utilities, with deterministic ordering tests. Progress: semantics now has a standalone condensation-DAG utility layered on the SCC helper, with deduplicated inter-component edges, deterministic topological traversal order, a stable testing snapshot API, and focused ordering tests for collapsed cycles and disconnected roots.
- ✓ Land a first vertical solver slice for definition return-kind inference only: run SCC fixed-point over return constraints, preserve current diagnostics, and gate the new path behind a temporary opt-in flag (`--type-resolver=graph`). Progress: the validator now accepts `--type-resolver=graph`, orders return-kind inference through the type-resolution graph plus SCC/condensation traversal, and keeps the current recursive-annotation, unresolved, and conflicting-return diagnostics stable while the graph path remains opt-in.
- ✓ Add parity harness tests that run legacy vs graph return-kind inference on the same corpus and assert identical diagnostics/IR for supported cases; include explicit expected-diff tests where legacy behavior is intentionally corrected. Progress: IR-pipeline parity helpers now compare compile-pipeline diagnostics plus serialized VM IR across `legacy` and `graph` resolver modes for a shared corpus, and the expected-diff coverage now locks grounded mutual recursion as a deliberate graph-only fixed-point success while legacy still reports the explicit-annotation diagnostic.
- ✓ Flip return-kind inference default to graph mode once parity is stable; keep a temporary fallback flag (`--type-resolver=legacy`) for rollback during stabilization. Progress: shared `Options` and `Semantics` entrypoints now default to the graph resolver, semantics test helpers follow the same default path, and explicit `--type-resolver=legacy` / `typeResolver=\"legacy\"` overrides remain available as the temporary rollback path with focused default-vs-legacy regressions.
- ✓ Upgrade graph-mode return-kind fallback to deterministic illegal-cycle diagnostics that name unresolved SCC members and lock the intentional diff versus legacy. Progress: unresolved graph SCCs now fail with stable cycle-member messages/spans instead of the old per-definition fallback, and parity coverage now snapshots the graph-only recursive-cycle diagnostic as an explicit expected diff from legacy.
- ✓ Land the first post-stability graph expansion slice for local `auto` inference on direct call initializers, keeping deterministic diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: graph mode now records resolved direct-call `local_auto` sites from the type-resolution graph and reuses those binding types during validation, while legacy remains available as an explicit rollback path and parity coverage now includes struct/collection direct-call auto bindings.
- ✓ Extend graph local `auto` solving beyond direct call initializers to block/control-flow-valued initializers, keeping deterministic diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: graph mode now walks definition bodies in deterministic source order, captures supported block/`if`/`match` local-`auto` sites alongside the existing direct-call slice, and reuses those binding types during validation while explicit `--type-resolver=legacy` remains the rollback path.
- ✓ Extend graph local `auto` solving to omitted-envelope inference, keeping deterministic diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: graph mode now reuses graph-collected omitted-envelope bindings for struct-field resolution via owner-scoped lookup keys, so omitted field envelopes follow the same deterministic graph cache as local `auto` sites while explicit `--type-resolver=legacy` remains the rollback path.
- ✓ Land the first graph-backed inference-query cleanup slice: replace the remaining ad hoc inferred-envelope query call sites with a shared graph-derived lookup helper in graph mode, while preserving diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: shared `inferDefinitionReturnBinding(...)` queries now run under the callee definition context so graph-mode local/omitted inference caches are visible during query-time return binding reconstruction, and the remaining inline query walkers in `SemanticsValidator.cpp`/`SemanticsValidatorExpr.cpp` now delegate to that shared helper instead of rebuilding callee locals separately.
- ✓ Extract a shared graph-backed type-text query helper for call-site result/map receiver probing so `SemanticsValidator.cpp` and `SemanticsValidatorExpr.cpp` stop carrying parallel direct map-constructor and envelope-type walkers, while preserving diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: `SemanticsValidatorInferCollections.cpp` now owns shared graph-aware query-time type-text inference for envelope-wrapped/direct-map/direct-call probes, `resolveResultTypeForExpr(...)` consumes that helper for map/result receiver queries, and `SemanticsValidatorExpr.cpp` now calls the shared collection-query helpers with explicit validation context instead of keeping a second copy of the same walkers.
- ✓ Extract the same shared graph-backed type-text query helper into `SemanticsValidatorInfer.cpp` so inference-time collection/map probing stops carrying the remaining parallel direct map-constructor and envelope-type walkers before any broader CT-eval/query expansion, while preserving diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: the inference-side collection/map probing block in `SemanticsValidatorInfer.cpp` now delegates to the shared `resolveCallCollectionTypePath(...)` and `resolveCallCollectionTemplateArgs(...)` helpers instead of carrying its own direct-map constructor and type-text walkers, and parity coverage now includes helper-returned map value inference through inferred `at(...)` result kinds.
- ✓ Land the first CT-eval/query interaction slice for the graph resolver: route compile-time evaluation collection/result/map receiver probing through the shared graph-backed query helpers instead of legacy-only inference islands, while preserving deterministic diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: the compile-time struct/query probe path in `inferStructReturnPath(...)` now delegates collection/map receiver probing to the shared graph-backed helper stack instead of keeping a legacy-only direct-map/type-text island, and parity coverage now includes helper-returned map value inference through that compile-time probe path.
- ✓ Extract the remaining CT-eval/query receiver-classifier closures from `inferStructReturnPath(...)` into the shared builtin-collection dispatch/resolver path so args-pack/vector/string/map receiver probing stops depending on a separate local classification island, while preserving deterministic diagnostics and the temporary `--type-resolver=legacy` rollback path. Progress: `SemanticsValidatorInferCollections.cpp` now builds the shared builtin receiver-classifier bundle for args-pack/array/vector/string/map probing, `inferStructReturnPath(...)` consumes that shared bundle instead of carrying a second local classifier island, and parity coverage now locks nested string helper inference through args-pack, vector, and map receivers.
- ✓ Extract the earlier builtin collection receiver-classifier island from `inferExprReturnKind(...)` into the same shared resolver bundle so return-kind inference and CT-eval/query probing consume one args-pack/vector/string/map classification path before any broader legacy-removal slice. Progress: `inferExprReturnKind(...)` now binds args-pack/array/vector/string/map receiver classifiers from `makeBuiltinCollectionDispatchResolvers(...)` instead of carrying a second local island, and parity coverage now locks auto return-kind inference through shared args-pack/vector/map string-helper receivers.
- ✓ Extract the remaining indexed args-pack / wrapped-pointee collection plumbing shared by `inferExprReturnKind(...)` and `inferStructReturnPath(...)` into the same resolver stack so `soa_vector` and `Buffer` probing stop depending on a second local helper layer before broader legacy removal. Progress: the shared builtin receiver bundle now exports indexed-args-pack / dereferenced / wrapped-pointee resolver hooks plus shared `soa_vector` and `Buffer` target resolvers, `inferExprReturnKind(...)` binds those shared hooks instead of keeping a second local helper layer, and parity coverage now locks borrowed `soa_vector` auto return inference through `to_aos(dereference(values[0i32]))`.
- ✓ Extract the same indexed args-pack / wrapped-pointee builtin receiver plumbing into `SemanticsValidatorExpr.cpp` so validation-time collection, `soa_vector`, and related helper probing stop carrying a parallel resolver island before broader legacy removal. Progress: `SemanticsValidatorExpr.cpp` now binds indexed / dereferenced / wrapped args-pack receiver resolution from `makeBuiltinCollectionDispatchResolvers(...)`, keeps only Expr-specific field-binding and inferred-return fallbacks, and validation coverage now locks borrowed indexed `soa_vector` probing through `to_aos(dereference(values[0i32]))`.
- ✓ Extract the remaining Expr-specific collection/map/string receiver fallbacks in `SemanticsValidatorExpr.cpp` onto shared resolver adapters so validation no longer carries parallel field-binding-aware wrappers before broader legacy removal. Progress: `SemanticsValidatorExpr.cpp` now passes field-binding and inferred-call hooks into `makeBuiltinCollectionDispatchResolvers(...)`, consumes the shared array/vector/`soa_vector`/string/map receiver resolvers directly, and validation coverage now locks field-bound map access through `holder.values.at(1i32).count()`.
- ✓ Extract the remaining experimental-map receiver routing helpers in `SemanticsValidatorExpr.cpp` onto shared map-receiver adapters so canonical/experimental helper redirection stops depending on local field-binding and inferred-return walkers before broader legacy removal. Progress: the shared builtin collection resolver bundle now exports experimental-map receiver and value-map classifiers, `SemanticsValidatorExpr.cpp` consumes those adapters for canonical/experimental helper redirection instead of keeping local field-binding and inferred-return walkers, and validation coverage now locks field-bound `Map<string, i32>` method receivers through `holder.values.tryAt(...)` / `.count()` / `.at(...)`.
- ✓ Consolidate the remaining experimental-map helper path canonicalization and rewrite tables in `SemanticsValidatorExpr.cpp` into shared helper metadata so map helper alias/canonical routing stops depending on local hard-coded path matchers before broader legacy removal. Progress: shared experimental-map helper metadata now owns helper-name/path canonicalization for canonical, alias, wrapper, and experimental helper paths, `SemanticsValidatorExpr.cpp` calls those shared helpers instead of keeping a local hard-coded table, and validation coverage now locks field-bound wrapper helper calls such as `/std/collections/mapCount(holder.values)`.
- ✓ Replace the remaining local experimental-map wrapper/compatibility heuristics in `SemanticsValidatorExpr.cpp` (for example `shouldBuiltinValidateCurrentMapWrapperHelper(...)` and the map compatibility path helpers) with shared helper metadata so map helper fallback rules stop depending on duplicated helper-name conditionals before broader legacy removal. Progress: the shared experimental-map helper descriptor table now drives wrapper-availability checks plus direct/method compatibility-path parsing, `SemanticsValidatorExpr.cpp` calls those shared helpers instead of keeping local helper-name conditionals, and validation coverage now locks removed-path diagnostics on field-bound experimental-map receivers for both `/map/count(...)` and `./std/collections/map/count()`.
- ✓ Extract the remaining removed collection-helper compatibility parsing in `SemanticsValidatorExpr.cpp` (notably `explicitRemovedCollectionMethodPath(...)` and the unnamespaced map-count fallback logic) into shared collection-helper metadata so legacy removed-helper diagnostics stop depending on local string parsing before broader legacy removal. Progress: shared removed-collection helper metadata now owns vector/map compatibility helper whitelists plus explicit removed-path parsing, `SemanticsValidatorExpr.cpp` calls those shared helpers for explicit removed method routing, bare `count(...)` map fallback, and body-argument preservation, and validation coverage now locks field-bound bare `count(holder.values)` plus removed-path diagnostics on field-bound compatibility calls.
- ✓ Extract the remaining bare/body-argument removed map helper parsing in `SemanticsValidatorExpr.cpp` (for example `resolveBareMapCallBodyArgumentTarget(...)` and the remaining direct `count`/`at`/`at_unsafe` helper-name branches) onto shared collection-helper metadata so body-argument remapping stops depending on local helper-name parsing before broader legacy removal. Progress: shared removed-collection metadata now resolves bare and wrapped map body-argument helpers through one helper in `SemanticsValidatorInferCollections.cpp`, `SemanticsValidatorExpr.cpp` calls that shared helper instead of keeping local `count`/`at`/`at_unsafe` parsing lambdas, and validation coverage now locks field-bound bare `at(holder.values, ...) { ... }` remapping.
- ✓ Extract the remaining vector-like body-argument compatibility remapping in `SemanticsValidatorExpr.cpp` (currently the `preferVectorStdlibHelperPath(...)` path rewrite around block arguments and the remaining bare mutator/access helper branches) into shared collection-helper metadata so vector body-argument remapping stops depending on local path rewriting before broader legacy removal. Progress: `SemanticsValidatorExpr.cpp` now calls the shared `preferVectorStdlibHelperPath(...)`, `getVectorStatementHelperName(...)`, and `getDirectVectorHelperCompatibilityPath(...)` helpers instead of keeping local vector mutator/access compatibility tables, and validation coverage now locks array-alias statement diagnostics plus `/vector/push(...) { ... }` block-argument rejection through the shared helper metadata path.
Deferred graph follow-up note: explicit/implicit template inference, broader omitted-envelope inference beyond the first local-auto expansion slices, performance guardrails, optional parallel solve, query/invalidation work, CT-eval interaction, remaining inference-island migration, sustained performance suites, legacy removal, and end-to-end graph conformance expansion stay parked until the return-kind graph path is stable enough for the next broader expansion slice.
