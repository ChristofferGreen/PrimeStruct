# Existing direct tests -> src/private-header dependencies.
# Keep entries narrow and remove them when a stable testing API replaces them.
# A target ending with "/" allows the source file to include any header under that prefix.
